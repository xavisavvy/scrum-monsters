---
phase: 25-lobby-polish-animations
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - client/src/lib/utils/magicWords.test.ts
autonomous: true

must_haves:
  truths:
    - "detectMagicWords returns correct effect types for all 26 magic word categories"
    - "getPrimaryMagicEffect returns first detected effect or null"
    - "extractSpellTargets parses single, comma-separated, and 'and'-separated targets"
    - "extractHoldTarget extracts names from hold/paralyze/freeze patterns"
    - "getSpellWords returns trigger words for a given effect type"
  artifacts:
    - path: "client/src/lib/utils/magicWords.test.ts"
      provides: "Unit test coverage for all exported magicWords functions"
      min_lines: 80
  key_links:
    - from: "client/src/lib/utils/magicWords.test.ts"
      to: "client/src/lib/utils/magicWords.ts"
      via: "direct import of all exported functions"
      pattern: "import.*from.*magicWords"
---

<objective>
Add comprehensive unit test coverage for the magicWords.ts utility module which currently has ZERO tests despite being a critical part of the emote/magic effect system.

Purpose: The emote system has been functional since v1.3 but has no formal test coverage. These tests lock down the existing behavior before any UI enhancements, ensuring regressions are caught. Satisfies LOBBY-01 testing requirement.
Output: magicWords.test.ts with tests for all 5 exported functions
</objective>

<execution_context>
@C:/Users/Preston/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Preston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@client/src/lib/utils/magicWords.ts
@client/src/test/setup.ts
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive unit tests for magicWords utility functions</name>
  <files>client/src/lib/utils/magicWords.test.ts</files>
  <action>
Create `client/src/lib/utils/magicWords.test.ts` with Vitest tests covering all 5 exported functions from magicWords.ts:

**TDD approach:** Since the implementation already exists, write tests that document and lock down existing behavior. Run tests to confirm all pass (GREEN state already — this is "test-after" formalization).

**1. `detectMagicWords(message)`** — Test cases:
- Single magic word detection: `'fire'` -> `['fire']`, `'heal'` -> `['heal']`
- Case insensitivity: `'FIRE'` -> `['fire']`, `'HeAl'` -> `['heal']`
- Word embedded in sentence: `'Time to heal!'` -> `['heal']`
- Multiple effects in one message: `'fire and ice'` -> `['fire', 'ice']`
- No magic words: `'hello world'` -> `[]`
- Empty string: `''` -> `[]`
- Multi-word triggers: `'avada kedavra'` -> `['massacre']`, `'hold person'` -> `['hold']`
- De-duplication: Multiple words mapping to same type (e.g., 'fire flame') returns `['fire']` once
- Overlapping categories: `'freeze'` triggers both `'ice'` (via 'freeze') and `'hold'` (via 'freeze') — verify actual behavior and document it

**2. `getPrimaryMagicEffect(message)`** — Test cases:
- Returns first effect: `'fire and ice'` -> `'fire'`
- Returns null for no effects: `'hello'` -> `null`
- Single effect: `'heal me'` -> `'heal'`

**3. `extractHoldTarget(message)`** — Test cases:
- Hold person pattern: `'hold person bob'` -> `'bob'`
- Paralyze pattern: `'paralyze alice'` -> `'alice'`
- Freeze pattern: `'freeze charlie'` -> `'charlie'`
- No target: `'hold person'` with no name after — verify actual behavior (likely returns empty string or null)
- No matching pattern: `'hello world'` -> `null`

**4. `getSpellWords(effectType)`** — Test cases:
- Known type: `getSpellWords('fire')` -> `['fire', 'flame', 'burn', 'hot', 'inferno']`
- Unknown type: Should return empty array (verify with a cast like `getSpellWords('nonexistent' as any)`)
- Verify returns array reference (not undefined)

**5. `extractSpellTargets(message, spellWords)`** — Test cases:
- Single target: `extractSpellTargets('petrify bob', getSpellWords('petrify'))` -> `['bob']`
- Comma-separated: `'petrify bob, alice, charlie'` -> `['bob', 'alice', 'charlie']`
- And-separated: `'petrify bob and alice'` -> `['bob', 'alice']`
- Self-cast (no targets): `'petrify'` -> `null`
- Mixed separators: `'petrify bob, alice and charlie'` -> `['bob', 'alice', 'charlie']`
- Ampersand separator: `'petrify bob & alice'` -> `['bob', 'alice']`

Use `describe` blocks per function. Import from `@/lib/utils/magicWords` using the path alias. Follow existing test patterns from `useEventSync.test.ts`.
  </action>
  <verify>
Run: `npx vitest run client/src/lib/utils/magicWords.test.ts`
All tests pass. No failures. Coverage shows magicWords.ts functions are exercised.
  </verify>
  <done>
magicWords.test.ts exists with 20+ test cases covering all 5 exported functions. All tests pass green. Edge cases (empty strings, case insensitivity, multi-word triggers, self-cast) are covered.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run client/src/lib/utils/magicWords.test.ts` — all tests pass
2. `npm run check` — no TypeScript errors introduced
3. Test file imports all 5 exported functions from magicWords.ts
</verification>

<success_criteria>
- magicWords.test.ts exists with comprehensive test coverage
- All tests pass on first run (existing behavior locked down)
- No production code changes required (tests document existing behavior)
</success_criteria>

<output>
After completion, create `.planning/phases/25-lobby-polish-animations/25-01-SUMMARY.md`
</output>
