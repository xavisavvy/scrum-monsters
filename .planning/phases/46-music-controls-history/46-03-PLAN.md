---
phase: 46-music-controls-history
plan: 03
type: execute
wave: 3
depends_on: ["46-01", "46-02"]
files_modified:
  - client/src/pages/GamePage.tsx
  - client/src/components/game/BattleScreen.tsx
  - client/src/components/game/Lobby.tsx
  - client/src/components/ui/BossMusicControls.tsx
autonomous: false
requirements: [MUSIC-01, MUSIC-02]
must_haves:
  truths:
    - "MusicControls renders in BOTH the Lobby and every BattleScreen phase that previously showed BossMusicControls"
    - "YoutubeAudioPlayer is mounted exactly once at GamePage and stays mounted across the lobby↔battle transition (the player ref in useAudio is never null when either screen triggers play)"
    - "A host triggering play from the Lobby produces audio on the host AND all peers (no null-player no-op, no desync)"
    - "BossMusicControls.tsx no longer exists and nothing imports it"
  artifacts:
    - path: "client/src/pages/GamePage.tsx"
      provides: "Single persistent YoutubeAudioPlayer mount across phases"
      contains: "YoutubeAudioPlayer"
    - path: "client/src/components/game/Lobby.tsx"
      provides: "MusicControls in the lobby"
      contains: "MusicControls"
  key_links:
    - from: "client/src/pages/GamePage.tsx"
      to: "useAudio.youtubePlayer (via YoutubeAudioPlayer onReady)"
      via: "single mount at common ancestor"
      pattern: "YoutubeAudioPlayer"
    - from: "client/src/components/game/BattleScreen.tsx"
      to: "client/src/components/ui/MusicControls"
      via: "import + render (replaces BossMusicControls)"
      pattern: "MusicControls"
---

<objective>
Wire the pieces together (MUSIC-01): hoist the single `YoutubeAudioPlayer` mount from BattleScreen up to `GamePage` (the common ancestor of Lobby and BattleScreen, which stays mounted across the lobby↔battle transition), swap both `BossMusicControls` sites in BattleScreen for `MusicControls`, add `MusicControls` to the Lobby top-right, and delete the now-dead `BossMusicControls.tsx`.

Purpose: Closes the CRITICAL null-player gap (RESEARCH Pitfall 1) — a single persistent player ref means lobby-initiated playback works and never desyncs. This is the integration wave; it depends on Plan 01 (player wrapper) and Plan 02 (the MusicControls component).
Output: Both screens show working music controls; one player instance; BossMusicControls removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-music-controls-history/46-CONTEXT.md
@.planning/phases/46-music-controls-history/46-RESEARCH.md
@.planning/phases/46-music-controls-history/46-01-SUMMARY.md
@.planning/phases/46-music-controls-history/46-02-SUMMARY.md

<interfaces>
<!-- Mount points and current usage. -->

client/src/pages/GamePage.tsx — common ancestor. renderGamePhase() switches between
  AvatarSelection / Lobby / BattleScreen (lines 266-349). GamePage stays mounted across
  the lobby↔battle transition (URL stable, server-state-driven). Final JSX return is
  lines 351-358: <> PageMeta / renderGamePhase() / TutorialOverlay </>. YoutubeAudioPlayer
  must mount HERE so the player ref persists.

client/src/components/game/BattleScreen.tsx:
  line 12  import { BossMusicControls } from '@/components/ui/BossMusicControls';
  line 13  import { YoutubeAudioPlayer } from '@/components/ui/YoutubeAudioPlayer';
  line 336 <BossMusicControls /> (battle phase, inside absolute top-6 right-6 z-[60] wrapper)
  line 403 <BossMusicControls /> (discussion phase, same wrapper)
  line 822 <YoutubeAudioPlayer /> (the mount to REMOVE from here)

client/src/components/game/Lobby.tsx:
  line 1718-1727 top-right mute button block (absolute top-4 right-4 z-50). MusicControls
  goes adjacent to / in this top-right region. Lobby does NOT currently import either
  MusicControls or YoutubeAudioPlayer.

client/src/components/ui/MusicControls.tsx (Plan 02) — export `function MusicControls()`.
client/src/components/ui/YoutubeAudioPlayer.tsx — export `function YoutubeAudioPlayer()`;
  registers youtubePlayer in useAudio onReady, calls .destroy() on unmount.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hoist YoutubeAudioPlayer to GamePage; swap BattleScreen controls; add to Lobby; delete BossMusicControls</name>
  <files>client/src/pages/GamePage.tsx, client/src/components/game/BattleScreen.tsx, client/src/components/game/Lobby.tsx, client/src/components/ui/BossMusicControls.tsx</files>
  <read_first>
    - client/src/pages/GamePage.tsx (lines 1-30 imports; lines 351-358 final return)
    - client/src/components/game/BattleScreen.tsx (lines 12-13 imports; 327-337 + 394-404 control wrappers; 820-824 player mount)
    - client/src/components/game/Lobby.tsx (lines 1711-1729 top-right region)
    - client/src/components/ui/MusicControls.tsx (Plan 02 — the new component)
    - client/src/components/ui/YoutubeAudioPlayer.tsx (mount/destroy lifecycle — confirm single-instance requirement)
    - .planning/phases/46-music-controls-history/46-RESEARCH.md (Pitfall 1 lines 170-174 single-mount; Pitfall 4 dropdown anchoring)
  </read_first>
  <action>
    In `GamePage.tsx`: add `import { YoutubeAudioPlayer } from '@/components/ui/YoutubeAudioPlayer';` and render exactly ONE `<YoutubeAudioPlayer />` inside the top-level fragment return (lines 351-358), as a sibling to `{renderGamePhase()}` and `<TutorialOverlay />`. It is hidden/absolute-positioned so placement in the tree is fine anywhere in that fragment. This single mount persists across phase changes so `useAudio.youtubePlayer` is never null.

    In `BattleScreen.tsx`: change the import on line 12 from `BossMusicControls` to `import { MusicControls } from '@/components/ui/MusicControls';`. Replace `<BossMusicControls />` at line 336 and at line 403 with `<MusicControls />` (keep the surrounding `absolute top-6 right-6 z-[60]` wrappers). REMOVE the `<YoutubeAudioPlayer />` mount at line 822 AND remove its now-unused import on line 13 (it is hoisted to GamePage; leaving it here would create a second player instance whose onReady overwrites the store ref and whose unmount calls .destroy() — RESEARCH Pitfall 1).

    In `Lobby.tsx`: add `import { MusicControls } from '@/components/ui/MusicControls';`. Render `<MusicControls />` in the top-right region near the existing mute button block (around lines 1718-1727) — place it inside a sibling wrapper so its history dropdown is not clipped by `overflow-x-hidden` on the root container; if needed, give the wrapper its own stacking/positioning context rather than relying on the mute button's `absolute top-4 right-4 z-50` (RESEARCH Pitfall 4). Do NOT add a second YoutubeAudioPlayer here — the GamePage mount covers the Lobby.

    Delete `client/src/components/ui/BossMusicControls.tsx` entirely (its only importer was BattleScreen, now migrated — RESEARCH State of the Art line 271). Do not touch the stale `.bak` files.
  </action>
  <verify>
    <automated>npm run check && grep -rn "BossMusicControls" client/src --include=*.tsx --include=*.ts | grep -v '\.bak'</automated>
  </verify>
  <acceptance_criteria>
    - `GamePage.tsx` contains exactly one `<YoutubeAudioPlayer` occurrence and imports it
    - `BattleScreen.tsx` imports `MusicControls` and contains zero `BossMusicControls` references
    - `BattleScreen.tsx` contains zero `YoutubeAudioPlayer` references (mount + import both removed)
    - `Lobby.tsx` imports and renders `MusicControls`
    - `client/src/components/ui/BossMusicControls.tsx` does not exist
    - The grep for `BossMusicControls` across client/src (excluding .bak) returns no matches
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Single persistent player at GamePage; MusicControls in Lobby + both BattleScreen phases; BossMusicControls deleted; tsc green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Two-browser live sync + playlist + history verification</name>
  <action>Manual human verification — follow the numbered <how-to-verify> steps in two browser windows. This proves live YouTube playback, cross-client socket sync, lobby↔battle transition survival of the GamePage player mount, playlist playback, and per-host history persistence — none of which unit tests can assert.</action>
  <what-built>
    YoutubeAudioPlayer hoisted to GamePage (single persistent mount). MusicControls replaces
    BossMusicControls in both BattleScreen phases and is newly added to the Lobby. The
    end-to-end play/stop/sync + playlist + history paths are all wired (Plans 01-03).
  </what-built>
  <how-to-verify>
    Two-browser test (host + one peer), since live YouTube playback, cross-client socket sync,
    and the lobby↔battle transition cannot be asserted by unit tests:
    1. `npm run dev`, open http://localhost:5000 in two browser windows.
    2. Window A creates a lobby and picks an avatar (becomes host); Window B joins with the lobby code and picks an avatar.
    3. In Window A's LOBBY, open Music Settings, paste a normal YouTube watch URL, click Play.
       Expect: audio plays in BOTH windows; the history dropdown now lists the video by its resolved title.
    4. In Window B (non-host), confirm there is NO URL input — only a read-only "Music playing" status.
    5. Host clicks Stop. Expect: audio stops in both windows.
    6. Start the battle (host begins the game). Confirm MusicControls still appears (top-right) in the battle screen and play/stop still syncs to both windows.
    7. Navigate lobby→battle→back is not directly testable, but verify play STILL works after entering battle (proves the GamePage player mount survived the transition — Pitfall 1).
    8a. Host pastes a watch URL that includes a playlist (youtube.com/watch?v=ID&list=...). Expect: the first video plays in both windows (extractVideoId still matches the v= param); no warning note shown.
    8b. Host pastes a bare playlist URL (youtube.com/playlist?list=...). Expect: the accept-only note "This playlist URL may not play inline — try a direct video URL instead" shows; the entry is still saved to history with its resolved title; clicking Play submits but produces no inline audio (server rejects the empty videoId — acceptable accept-only outcome per the team-lead Option B override).
    9. Reload Window A's host browser, rejoin the same lobby; open Music Settings. Expect: the history dropdown still lists previously-used entries (localStorage persistence, per-host key).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any step that failed (no audio, desync, missing controls, clipped dropdown, watch+list first-video not playing).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host client → server (youtube_play/stop) | Same boundary as Plan 01/02; server host-gate is authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-06 | Denial of Service (self-inflicted desync) | YoutubeAudioPlayer mount count | mitigate | Mount exactly once at GamePage; remove the BattleScreen mount so two player instances cannot race the store ref / destroy() — verified by grep acceptance criterion |
| T-46-07 | Elevation of Privilege | non-host music control | accept | Server host-gate unchanged; non-host UI is read-only and any spoofed emit is server-rejected |
| T-46-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `npm run check` exits 0
- grep confirms zero `BossMusicControls` references in client/src (excluding .bak) and the file is deleted
- Full suite regression: `npm test`
- Two-browser human-verify checkpoint passes (live playback + cross-client sync + transition survival + accept-only playlist behavior + history persistence)
</verification>

<success_criteria>
- MusicControls renders in Lobby + both BattleScreen phases (MUSIC-01)
- Single persistent YoutubeAudioPlayer at GamePage; lobby-initiated play works and syncs to peers with no null-player no-op (MUSIC-01 / MUSIC-02 / Pitfall 1 closed)
- Accept-only playlist behavior (D-07, Option B): watch?v=...&list=... plays first video; bare playlist?list=... shows the note and is saved to history (no payload/schema widening)
- History persists per-host across reload (MUSIC-03)
- BossMusicControls.tsx deleted, no dangling imports
- `npm run check` + `npm test` green
</success_criteria>

<output>
Create `.planning/phases/46-music-controls-history/46-03-SUMMARY.md` when done.
</output>
