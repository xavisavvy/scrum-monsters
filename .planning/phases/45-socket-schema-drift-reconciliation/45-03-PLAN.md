---
phase: 45-socket-schema-drift-reconciliation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - server/websocket.ts
  - server/gameState.ts
  - server/events/ClientEventEmitter.ts
  - shared/gameEvents.ts
  - shared/clientEvents.ts
  - shared/socket-schemas.ts
autonomous: false
requirements: []

must_haves:
  truths:
    - "The 6 as-any legacy emits in server/websocket.ts (estimation_started, vote_state_updated x3, timer_paused, timer_resumed, timer_extended, estimate_forced) are deleted; no remaining `as any` casts on emit/io.to(...).emit calls in server/websocket.ts"
    - "consensus_countdown_update emit at server/gameState.ts:~1718 is deleted; the unused Lobby.consensusCountdown field is also removed if no consumer remains"
    - "estimation:discussion_started bridge in ClientEventEmitter.ts is deleted"
    - "The 11 H5 SAFE_TO_DELETE legacy emits are removed from server/websocket.ts and server/gameState.ts: player_left, player_disconnected, player_reconnected, boss_defeated, game_over, score_submitted, scores_revealed, voting_timeout, modifier_updated, player_attacked, player_state_updated (last has no emit; remove the type declaration only)"
    - "Corresponding ServerToClientEvents entries for the deleted events are removed from shared/gameEvents.ts (keep them only if a fine-grained replacement reuses the legacy type — none should)"
    - "session:player_reconnected dead type is removed from shared/clientEvents.ts"
    - "After this plan, every event in ServerToClientEvents has at least one server emit AND at least one client handler; verified via grep"
  artifacts:
    - path: "server/websocket.ts"
      provides: "Dead legacy emits removed; no `'foo' as any` event-name casts"
    - path: "shared/gameEvents.ts"
      provides: "ServerToClientEvents trimmed of legacy events with no handler"
    - path: "shared/clientEvents.ts"
      provides: "Dead session:player_reconnected type removed"
    - path: ".planning/phases/45-socket-schema-drift-reconciliation/45-03-DELETED.md"
      provides: "Audit trail listing every removed event with file:line citations and verification that fine-grained replacement is wired"
      min_lines: 50
  key_links:
    - from: "Each deleted legacy emit"
      to: "Fine-grained replacement listed in 45-H5-TRIAGE.md"
      via: "Per-event mapping in 45-03-DELETED.md"
---

<objective>
Delete confirmed-dead wire traffic. The H5 triage (45-H5-TRIAGE.md) verified each of these 17 events has a working fine-grained replacement; the H1 as-any family + H2 + H3 + H9 have no consumer at all. This is pure subtraction — no new behavior, no UX change.

Must land before 45-04 because 45-04 touches the same files (websocket.ts, ClientEventEmitter.ts) and deleting first keeps the diff readable.
</objective>

<context>
@.planning/phases/45-socket-schema-drift-reconciliation/45-RESEARCH.md
@.planning/phases/45-socket-schema-drift-reconciliation/45-H5-TRIAGE.md
@.planning/phases/45-socket-schema-drift-reconciliation/45-CONTEXT.md
@server/websocket.ts
@server/gameState.ts
@server/events/ClientEventEmitter.ts
@shared/gameEvents.ts
@shared/clientEvents.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete H1 as-any legacy emit family in server/websocket.ts</name>
  <behavior>
    - Remove emits at lines: ~1775 (estimation_started), ~1815/1860/2037 (vote_state_updated x3), ~1898 (timer_paused), ~1929 (timer_resumed), ~1961 (timer_extended), ~1992 (estimate_forced). Line numbers per 45-RESEARCH.md H1 — re-verify with grep before editing.
    - Confirm via grep: `socket.on(` for each event name returns zero client matches before deleting.
    - For each, identify the fine-grained equivalent that already covers the same state change (per H1 note: "covered by sibling fine-grained estimation:* events that ARE handled").
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete H2 consensus_countdown_update and clean up Lobby.consensusCountdown</name>
  <behavior>
    - Remove the emit at server/gameState.ts:~1718
    - Search for consumers of `currentLobby.consensusCountdown` / `Lobby.consensusCountdown` field. If none exist, remove the field from `interface Lobby` in shared/gameEvents.ts (lines ~76-80)
    - If consumers exist, leave the field but note the dead-update in 45-03-DELETED.md as a future-cleanup item
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Delete H3 estimation:discussion_started bridge in ClientEventEmitter.ts</name>
  <behavior>
    - Remove the `this.eventBus.on('estimation:discussion_started', ...)` block at ~lines 167-173
    - Search for any internal eventBus listeners on `estimation:discussion_started` — if the bridge was the only one, the internal event itself can be removed from `server/events/types.ts` too
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Delete H5 SAFE_TO_DELETE legacy emits + schema entries</name>
  <behavior>
    Per the 45-H5-TRIAGE.md verdicts, delete each of these 11 + their schema entries:

    | Event | Server emit site(s) | Fine-grained replacement |
    |---|---|---|
    | player_left | websocket.ts:692 | session:player_left |
    | player_disconnected | websocket.ts:2268, 2303 | session:player_disconnected (verify) |
    | player_reconnected | websocket.ts:1686 | session:player_joined / session:player_reconnected fine-grained equivalent |
    | boss_defeated | websocket.ts:1016, 1052, 1317 | combat:boss_defeated |
    | game_over | websocket.ts:1115, 1507 | session:phase_changed → 'game_over' |
    | score_submitted | websocket.ts:953 | estimation:vote_cast |
    | scores_revealed | websocket.ts:973, 1292, 1430 | estimation:votes_revealed |
    | voting_timeout | gameState.ts:1396 | estimation:timer_expired |
    | modifier_updated | websocket.ts:1086, 1502 | combat:modifier_updated |
    | player_attacked | websocket.ts:1106, 1493 | combat:player_damaged |
    | player_state_updated | (no emit) | type declaration only — remove from shared/gameEvents.ts |

    For each: delete the emit, delete the schema entry, verify no test file references it.
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Delete H9 dead type in clientEvents.ts</name>
  <behavior>
    - Remove the `session:player_reconnected` declaration at clientEvents.ts:37-40 (dead — never emitted, never bridged)
    - Note: the broader clientEvents.ts ⇄ gameEvents.ts dedupe is in 45-05 (L1); this plan only removes the dead entry to keep diffs small
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Write 45-03-DELETED.md audit trail</name>
  <behavior>
    - For each deleted event, capture: name, where deleted (file:line), fine-grained replacement (event + handler file:line), one-line note. This is the artifact future debugging can grep against.
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Verification</name>
  <behavior>
    - `npm run check` clean
    - `npm run lint` — expect ~7 fewer warnings from the 7 deleted `as any` casts in websocket.ts
    - `npm test` — all green
    - Manual grep: `socket.on('player_left'\|socket.on('boss_defeated'\|...` returns zero matches across client/src/
    - Manual smoke: lobby → battle → vote → reveal → discussion → next ticket → game over flow works end-to-end (validates that fine-grained replacements actually cover the deleted events)
  </behavior>
</task>

</tasks>

## Verification

After this plan: ~17 events of wire bandwidth gone, schema trimmed of legacy entries, ~7 fewer lint warnings. The Phase 42 fine-grained event migration finally has its cleanup pass.

## Risk

Medium. The H5 triage agent verified each replacement exists, but didn't manually trace every UI path. The smoke test in Task 7 is the safety net. If a deletion regresses a UX, the audit trail in 45-03-DELETED.md lets us revert exactly one event.

**Cannot delete `battle_emote`** — H5 inventory flagged it for verification; the triage agent confirmed it IS handled at `client/src/components/game/BattleScreen.tsx:208`. Leave it alone.

**Cannot delete `youtube_play_synced` / `youtube_stop_synced`** — those are BROKEN_FEATURE per the triage. 45-04 wires them up; this plan must NOT remove them.

**Cannot delete `revive_complete` / `revive_progress` / `revive_cancelled`** — same. 45-04 either wires them up or replaces with fine-grained equivalents. This plan leaves them.

**Cannot delete `party_healed`** — same. 45-04 replaces it with a new `combat:player_healed` event + popup.
