---
phase: 45-socket-schema-drift-reconciliation
plan: 05
type: execute
wave: 3
depends_on: [45-01, 45-02, 45-03, 45-04]
files_modified:
  - client/src/lib/socket/eventHandlers.ts
  - client/src/lib/stores/useEventSync.ts
  - client/src/lib/stores/useWebSocket.tsx
  - client/src/pages/GamePage.tsx
  - server/websocket.ts
  - eslint.config.mjs
  - shared/socket-schemas.ts
  - shared/clientEvents.ts
  - shared/gameEvents.ts
autonomous: false
requirements: []

must_haves:
  truths:
    - "Client setupEventHandlers takes Socket<ServerToClientEvents, ClientToServerEvents>; every `(data: any)` annotation in eventHandlers.ts is removed and payload types are inferred from the schema"
    - "Server io and per-connection socket are typed `Server<ClientToServerEvents, ServerToClientEvents>` / `Socket<ClientToServerEvents, ServerToClientEvents>`; the 33 `any` warnings in server/websocket.ts are gone"
    - "useEventSync.ts and useWebSocket.tsx are typed; the 25 `any` warnings between them are gone"
    - "The per-file `@typescript-eslint/no-explicit-any: off` override in eslint.config.mjs for the 4 socket-boundary files is removed"
    - "clientEvents.ts is deleted (or pared down to types not duplicated in gameEvents.ts); GameEvents.ts is the single source of truth for socket event signatures. All imports redirected."
    - "Low-priority sweep items L1, L2, L3, L4, L5, L6, L7, L9, L10, L11, L12 from 45-RESEARCH.md are addressed: duplicate emits removed, Zod registry gaps filled, game_error Zod aligned, lobby_player_charge optional alignment, request_missed_events as-any cast dropped, session:* handlers verified, cosmetic cleanups landed"
    - "C6 (combat:player_damaged source field) — choose one: (a) add a brief inline comment in CombatManager noting the hardcode is correct until PvP lands, OR (b) thread source through anyway as future-proofing. Either way, document the decision"
    - "npm run lint reports 0 problems; npm run check is clean; all tests pass"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "No socket-boundary override; the @typescript-eslint/no-explicit-any: off block from commit e2a5c33 is gone"
    - path: "client/src/lib/socket/eventHandlers.ts"
      provides: "Typed socket; zero `(data: any)` annotations"
    - path: "server/websocket.ts"
      provides: "Typed socket; the 33 `any` warnings cleared"
    - path: "shared/socket-schemas.ts"
      provides: "Zod coverage for all C→S events including update_lobby_name, leave_lobby, start_battle, restart_game, abandon_quest, force_reveal, youtube_stop, heal_party, player_charge, battle_emote, return_to_lobby, update_estimation_settings, use_ability, use_item"
  key_links:
    - from: "Client socket type annotation"
      to: "shared/gameEvents.ts ServerToClientEvents + ClientToServerEvents"
      via: "Socket<...> generic params"
---

<objective>
The payoff plan. With 45-01..04 done (handlers fixed, schema reality-matched, dead traffic removed, broken features restored), every event in `ServerToClientEvents` should have a matching server emit and matching client handler, AND the shapes should agree. Typing the socket should now compile.

If 45-01..04 missed anything, it surfaces here as a TypeScript error — budget time for follow-up handler/emit fixes triggered by the typed compile.

After this plan, `npm run lint` reports 0 problems and the codebase is back to a typed contract end-to-end at the socket boundary.
</objective>

<context>
@.planning/phases/45-socket-schema-drift-reconciliation/45-CONTEXT.md
@.planning/phases/45-socket-schema-drift-reconciliation/45-RESEARCH.md
@eslint.config.mjs
@client/src/lib/socket/eventHandlers.ts
@client/src/lib/stores/useEventSync.ts
@client/src/lib/stores/useWebSocket.tsx
@server/websocket.ts
@shared/gameEvents.ts
@shared/clientEvents.ts
@shared/socket-schemas.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Type the client socket through the schema</name>
  <files>client/src/lib/socket/eventHandlers.ts, client/src/lib/stores/useEventSync.ts, client/src/lib/stores/useWebSocket.tsx, client/src/pages/GamePage.tsx</files>
  <behavior>
    - Define `type TypedSocket = Socket<ServerToClientEvents, ClientToServerEvents>` (or `import type`) in one shared location (likely `client/src/lib/gameTypes.ts`)
    - Change `setupEventHandlers(socket: Socket)` → `setupEventHandlers(socket: TypedSocket)`
    - Delete every `(data: any) =>` annotation in eventHandlers.ts — TypeScript infers payload types from the event map. Expect ZERO remaining type errors if 45-01..04 succeeded
    - useEventSync.ts: type the socket parameter passed in, type the buffered-event store with the inferred event union
    - useWebSocket.tsx: same treatment
    - GamePage.tsx: type any local `socket.on('foo', ...)` registrations
    - Any remaining errors after typing surface real drift that 45-01..04 missed → fix them inline (likely 1-3 surprises)
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Type the server socket</name>
  <files>server/websocket.ts</files>
  <behavior>
    - Change `io: SocketIOServer` (currently untyped) → `Server<ClientToServerEvents, ServerToClientEvents>` (note: server-side, the generic order is inbound-first)
    - Change per-connection `socket` to `Socket<ClientToServerEvents, ServerToClientEvents>`
    - Delete every `(data: any) =>` annotation — should clear the 33 `any` warnings in this file
    - Expect a handful of surprises (the inventory found the boss_attacked / boss_healed legacy vs fine-grained split here too)
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove the per-file ESLint override</name>
  <files>eslint.config.mjs</files>
  <behavior>
    - Delete the override block (added in commit e2a5c33) that turns `@typescript-eslint/no-explicit-any: off` for eventHandlers.ts, useEventSync.ts, useWebSocket.tsx, server/websocket.ts
    - Run `npm run lint` — expect 0 problems
    - If any warnings reappear, they're real `any` usages introduced by 45-04 or unfixed drift — address inline
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Dedupe clientEvents.ts → gameEvents.ts (L1, H7, H9)</name>
  <files>shared/clientEvents.ts, shared/gameEvents.ts, plus all importers</files>
  <behavior>
    - For each type in clientEvents.ts: check if gameEvents.ts has an equivalent. If yes, redirect imports and delete from clientEvents.ts. If no, move the type to gameEvents.ts then redirect imports
    - Resolves H7 (combat:boss_telegraph attackType strict-vs-string disagreement — keep the strict union from clientEvents.ts since CombatManager only emits 'light' | 'heavy' | 'special')
    - Resolves L1
    - When clientEvents.ts is empty, delete the file
    - Grep all imports of `@shared/clientEvents` and redirect to `@shared/gameEvents`
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Duplicate-emit cleanup (L4, L5, L6)</name>
  <files>server/websocket.ts, server/events/ClientEventEmitter.ts, client/src/pages/GamePage.tsx, client/src/lib/socket/eventHandlers.ts</files>
  <behavior>
    - L4: boss_attacked (handled by GamePage:227) + combat:boss_damaged (handled by eventHandlers) both fire. Remove the legacy boss_attacked emit + GamePage handler. Schema entry stays only if some other consumer exists (likely not after this)
    - L5: boss_healed (GamePage) + combat:boss_healed (eventHandlers, fixed in 45-01). Same cleanup
    - L6: avatar_selected (websocket.ts:538) + session:avatar_selected (websocket.ts:547). Keep fine-grained, remove legacy
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Zod coverage gaps (L2, L3, L8)</name>
  <files>shared/socket-schemas.ts</files>
  <behavior>
    - L2: add Zod entries for update_lobby_name, leave_lobby, start_battle, restart_game, abandon_quest, force_reveal, youtube_stop, heal_party, player_charge, battle_emote, return_to_lobby, update_estimation_settings, use_ability, use_item
    - L3: extend game_error Zod schema to include `code?` and `tiedValues?`
    - L8: confirm parameterless events (start_battle, restart_game, etc.) have z.void() or z.undefined() entries
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Cosmetic cleanups (L7, L9, L11, L12)</name>
  <behavior>
    - L7: lobby_player_charge schema — change `chargePower: number` (required) to `chargePower?: number` on the broadcast signature, matching the C→S optional
    - L9: useEventSync.ts:96 — drop the `'request_missed_events' as any` cast, the schema does exist
    - L11: verify system:full_state / system:missed_events handlers pass seq correctly (visual inspection)
    - L12: cosmetic — `data.timestamp || Date.now()` in progression:xp_awarded handler is unnecessary; schema guarantees timestamp. Remove the fallback
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 8: Verify session:* handlers have matching emits (L10)</name>
  <behavior>
    - For each `socket.on('session:foo', ...)` in eventHandlers.ts, grep server for matching `emit('session:foo'` or `emitToLobby('session:foo'`
    - The typed-socket compile in Task 1 should catch any handler reading a field the emit doesn't send. This task is the inverse: catch handlers with NO matching emit at all
    - Any gaps → either add the emit (if the handler is meant to fire) or delete the handler (if the feature is dead)
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 9: C6 source field decision</name>
  <files>server/events/ClientEventEmitter.ts, server/domains/CombatManager.ts</files>
  <behavior>
    - Per 45-CONTEXT.md: today all combat:player_damaged comes from boss-attack code paths, so the hardcoded `source: 'boss'` in the bridge is functionally correct
    - Choose one:
      - (a) Add a comment in ClientEventEmitter.ts noting "source hardcoded — extend when PvP damage flow lands" and leave the hardcode
      - (b) Thread `source` through the eventBus payload anyway and have CombatManager pass `source: 'boss'` explicitly (future-proofing, no behavior change)
    - Recommend (b) — cleaner, makes the schema field meaningful — but (a) is acceptable
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 10: Final verification</name>
  <behavior>
    - `npm run check` — clean
    - `npm run lint` — exactly 0 problems (was 199 at start of phase, 101 at start of 45-05)
    - `npm test` — 736+ all green
    - Full smoke test: lobby → battle → vote → reveal → discussion → next ticket → victory → game over, including revive + heal-party + youtube sync
    - Verify the typed socket catches a forced drift: temporarily add `socket.emit('session:player_joined', { wrong: 'shape' })` on the server, confirm tsc errors, then remove
  </behavior>
</task>

</tasks>

## Verification

After this plan: `npm run lint` is 0 problems, the socket boundary is typed end-to-end, and any future drift surfaces at compile time. The phase exit.

## Risk

Medium-high — this is the integration step. The typed-socket compile in Tasks 1-2 is where any 45-01..04 misses surface. Budget 1-2h for follow-up fixes triggered by those errors. If they cascade (a handler change requires an emit change which requires a CombatManager change), pause and consider splitting into a 45-05 + 45-06.

If `clientEvents.ts` dedupe (Task 4) reveals more types than expected (the L1 item is "many fine-grained events typed in BOTH files with subtle differences"), that subtask alone could grow to a half-day. Split out as 45-06 if so.
