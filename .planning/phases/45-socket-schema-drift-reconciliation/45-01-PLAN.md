---
phase: 45-socket-schema-drift-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/lib/socket/eventHandlers.ts
  - server/events/ClientEventEmitter.ts
  - server/domains/CombatManager.ts
  - client/src/lib/socket/eventHandlers.test.ts
  - server/events/ClientEventEmitter.test.ts
autonomous: false
requirements: []

must_haves:
  truths:
    - "combat:boss_healed handler reads data.newHp (not data.newHealth) and the schema field name is preserved"
    - "estimation:timer_started handler builds TimerState.startedAt from data.endsAt - data.durationMs (handler-side recompute, no schema change)"
    - "estimation:timer_resumed handler recomputes both startedAt and durationMs from the wire payload (resume schema only carries endsAt; handler derives durationMs as endsAt - Date.now() or carries prior durationMs via local state)"
    - "combat:player_revived bridge in ClientEventEmitter.ts threads payload.newHp from CombatManager (no hardcoded 50); CombatManager.completeRevival emits newHp = floor(maxHp * 0.5)"
    - "Regression tests assert (a) boss HP is a finite number after combat:boss_healed for a 100/50/200 maxHp boss, (b) currentTimer.startedAt is defined and finite after pause→resume, (c) post-revive client HP matches server's maxHp/2 for at least two different maxHp values"
  artifacts:
    - path: "client/src/lib/socket/eventHandlers.ts"
      provides: "Fixed handlers for combat:boss_healed, estimation:timer_started, estimation:timer_resumed"
      contains: "data.newHp"
    - path: "server/events/ClientEventEmitter.ts"
      provides: "combat:player_revived emit threads real newHp from payload; no '50' literal"
      contains: "newHp: payload.newHp"
    - path: "server/domains/CombatManager.ts"
      provides: "completeRevival emits combat:player_revived with newHp populated"
      contains: "newHp:"
    - path: "client/src/lib/socket/eventHandlers.test.ts"
      provides: "Regression coverage for C1/C2/C3 handler bugs"
      min_lines: 60
    - path: "server/events/ClientEventEmitter.test.ts"
      provides: "Regression coverage that the bridge no longer drops newHp"
      min_lines: 40
  key_links:
    - from: "CombatManager.completeRevival (~line 1611)"
      to: "ClientEventEmitter combat:player_revived bridge (~line 254)"
      via: "eventBus.emit payload now includes newHp"
      pattern: "newHp:.*maxHp"
---

<objective>
Three handler bugs and one bridge hardcode that the audit identified as silently corrupting client state. No schema changes — purely handler-side reads of correctly-emitted fields, plus one eventBus payload extension to thread an existing computed value through the bridge.

This is the safest plan to land first because it cannot regress anything: each fix replaces an `undefined` with a real value.
</objective>

<context>
@.planning/phases/45-socket-schema-drift-reconciliation/45-RESEARCH.md
@.planning/phases/45-socket-schema-drift-reconciliation/45-CONTEXT.md
@client/src/lib/socket/eventHandlers.ts
@server/events/ClientEventEmitter.ts
@server/domains/CombatManager.ts

<interfaces>
<!-- C1: ClientEventEmitter already sends newHp; only the handler is wrong -->
<!-- server/events/ClientEventEmitter.ts:201-206 -->
this.eventBus.on('combat:boss_healed', (payload) => {
  this.emitToLobby(payload.lobbyId, 'combat:boss_healed', {
    healAmount: payload.healAmount,
    newHp: payload.bossHealth,
  });
});

<!-- BUGGY: client/src/lib/socket/eventHandlers.ts:505,508 -->
setBoss({ ...currentBoss, currentHealth: data.newHealth });
<!-- FIX: data.newHp -->

<!-- C2: bridge emits endsAt+durationMs but handler reads non-existent startedAt -->
<!-- server/events/ClientEventEmitter.ts:130-136 -->
this.eventBus.on('estimation:timer_started', (payload) => {
  this.emitToLobby(payload.lobbyId, 'estimation:timer_started', {
    team: payload.team,
    endsAt: payload.startedAt + payload.durationMs,
    durationMs: payload.durationMs,
  });
});

<!-- BUGGY: client/src/lib/socket/eventHandlers.ts:364-368 -->
const timerState: TimerState = {
  startedAt: data.startedAt,  // undefined!
  durationMs: data.durationMs,
  isActive: true
};
<!-- FIX: startedAt: data.endsAt - data.durationMs -->

<!-- C3: resume only sends endsAt; handler reads startedAt AND durationMs (both undefined) -->
<!-- server/events/ClientEventEmitter.ts:145-150 -->
<!-- FIX: handler must reconstruct durationMs from the previous timer state in store -->

<!-- C5: bridge hardcodes newHp:50, ignoring server reality (maxHp * 0.5) -->
<!-- server/events/ClientEventEmitter.ts:254-260 -->
this.eventBus.on('combat:player_revived', (payload) => {
  this.emitToLobby(payload.lobbyId, 'combat:player_revived', {
    playerId: payload.playerId,
    reviverId: payload.reviverId,
    newHp: 50, // BUG: ignores actual revived HP
  });
});
<!-- FIX: payload must carry newHp from CombatManager.completeRevival (line 1611) which computes targetState.maxHp * 0.5 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix C1 — combat:boss_healed handler reads data.newHp</name>
  <files>client/src/lib/socket/eventHandlers.ts</files>
  <behavior>
    - Lines 505 and 508 read `data.newHp` instead of `data.newHealth`
    - Defensive `?? data.newHealth` is OK as a soft-landing for an in-flight server but not required (the bridge has only ever sent newHp)
    - Vitest spec asserts: after dispatching a `combat:boss_healed` event with `{ healAmount: 10, newHp: 75, ... }`, `useGameState.getState().currentBoss.currentHealth === 75` and `useGameState.getState().currentLobby.boss.currentHealth === 75`
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix C2 — estimation:timer_started handler reconstructs startedAt</name>
  <files>client/src/lib/socket/eventHandlers.ts</files>
  <behavior>
    - Line 365: replace `startedAt: data.startedAt` with `startedAt: data.endsAt - data.durationMs`
    - Vitest spec asserts: after dispatching `estimation:timer_started` with `{ endsAt: 1000000, durationMs: 30000, ... }`, `currentLobby.currentTimer.startedAt === 970000` and `durationMs === 30000`
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix C3 — estimation:timer_resumed handler reconstructs both fields</name>
  <files>client/src/lib/socket/eventHandlers.ts</files>
  <behavior>
    - Resume payload only carries `endsAt`. Reconstruct: `durationMs = data.endsAt - Date.now()`, `startedAt = Date.now()`. This is correct because resume restarts the countdown clock at "now" for the remaining duration
    - Vitest spec asserts: dispatching paused → resumed produces `currentTimer.isActive === true`, `Number.isFinite(currentTimer.startedAt) === true`, and `currentTimer.durationMs` matches the resumed remainingMs ± 100ms tolerance
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Fix C5 — thread real newHp through combat:player_revived</name>
  <files>
    - server/domains/CombatManager.ts (~line 1611)
    - server/events/ClientEventEmitter.ts (~line 254)
    - client/src/lib/socket/eventHandlers.ts (no change — handler already reads data.newHp)
  </files>
  <behavior>
    - CombatManager.completeRevival: extend `eventBus.emit('combat:player_revived', ...)` payload to include `newHp: targetState.hp` (which is `Math.floor(targetState.maxHp * 0.5)` from line 1599)
    - ClientEventEmitter bridge: replace literal `newHp: 50` with `newHp: payload.newHp`
    - Internal `combat:player_revived` event type (wherever it lives — likely `server/events/types.ts` or in-file) must include the new field
    - Vitest spec asserts: emit `combat:player_revived` with payload `{ ..., newHp: 25 }` (for maxHp=50) and `{ ..., newHp: 100 }` (for maxHp=200); bridge forwards both faithfully; client handler writes the right HP into `playerCombatStates`
    - Smoke check: grep `'combat:player_revived'` server-side — any other emitters (test mocks aside) must also carry newHp
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Run full check + lint + tests</name>
  <behavior>
    - `npm run check` clean
    - `npm run lint` reports same count as before this plan (no new warnings introduced; no warnings cleared because socket boundary remains untyped — that's 45-05)
    - `npm test` passes including the 4 new specs above
  </behavior>
</task>

</tasks>

## Verification

After this plan: no new lint warnings, no new type errors, +4 new tests. The three runtime bugs are fixed in isolation — neither schema nor wire format has changed, so no peer client running an older build will see anything different. Safe to land alone.

## Risk

Low. The only non-trivial change is threading `newHp` through the internal eventBus type — confirm no other emitter of `combat:player_revived` (search test mocks in `server/domains/*.test.ts`) breaks compile when the payload type gains a required field. If they do, those mocks need updating too.
