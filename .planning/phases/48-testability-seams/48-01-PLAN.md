---
phase: 48-testability-seams
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/gameState.ts
  - server/gameState.test.ts
autonomous: true
requirements: [MAINT-01]
must_haves:
  truths:
    - "GameStateManager class is importable by name from server/gameState.ts (no `as any` needed)"
    - "A test can construct GameStateManager with { startWatchdogs: false } and no setInterval is started"
    - "handleVotingTimeout is callable as a public method without `as any`"
    - "The production singleton `gameState` is behaviorally unchanged (watchdogs still start by default)"
    - "Full suite stays green (890+ tests), tsc clean, lint clean"
  artifacts:
    - path: "server/gameState.ts"
      provides: "Exported GameStateManager class with startWatchdogs opt + public handleVotingTimeout"
      contains: "export class GameStateManager"
    - path: "server/gameState.test.ts"
      provides: "New MAINT-01 describe block constructing the class directly"
      contains: "new GameStateManager(undefined, { startWatchdogs: false })"
  key_links:
    - from: "server/gameState.ts"
      to: "export const gameState"
      via: "singleton construction with default startWatchdogs=true"
      pattern: "export const gameState = new GameStateManager\\(\\)"
    - from: "server/gameState.test.ts"
      to: "GameStateManager"
      via: "named import"
      pattern: "import \\{ GameStateManager"
---

<objective>
Open the MAINT-01 testability seam: export the `GameStateManager` class, add an optional
`opts?: { startWatchdogs?: boolean }` constructor parameter (default `true`) that gates the two
watchdog `setInterval` calls, and promote `handleVotingTimeout` from `private` to `public`. Add a
new `describe` block to `server/gameState.test.ts` that constructs the class directly with no leaked
timers and no `as any`.

Purpose: This is the safety-net seam every later v6.0 refactor (Phases 49-52) depends on — tests can
instantiate GameState without leaking timers into the test process or reaching through `as any`.

Output: Exported `GameStateManager`, behaviorally-identical singleton, new characterization/seam tests.

INVARIANT (non-negotiable): byte-identical production behavior. The singleton at the module bottom
must keep starting both watchdogs (default `startWatchdogs=true`). This is a pure-refactor — three
keyword/signature changes plus new test code, no logic changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-testability-seams/48-RESEARCH.md
@.planning/phases/48-testability-seams/48-PATTERNS.md

<interfaces>
<!-- DI constructor analog the executor must replicate. From server/domains/SessionManager.test.ts:1-19 -->
SessionManager.test.ts fixture pattern (the model for the new describe block):
  beforeEach(() => {
    eventBus = new ScopedEventBus();
    sessionManager = new SessionManager({ eventBus });
  });

<!-- Current shape in server/gameState.ts (verified by RESEARCH.md + PATTERNS.md): -->
- L39:  `class GameStateManager {`  (NOT exported)
- L62:  `this.revivalWatchdog = setInterval(() => { this.processRevivalSessions(); }, 100);`
- L67:  `this.disconnectWatchdog = setInterval(() => { this.processDisconnectedPlayers(); }, 30000);`
- L1435: `private handleVotingTimeout(lobbyId: string): void { ... }`
- L2111: `export const gameState = new GameStateManager();`
- L2114: `export function setGameStateIO(io: SocketIOServer) { ... }`

<!-- Import site that must NOT change. From server/websocket.ts:11 -->
`import { gameState, setGameStateIO } from './gameState';`  (singleton + helper export unchanged)

<!-- Existing test fixture pattern. From server/gameState.test.ts:16 -->
`const gs = gameState as any;`  // acceptable in EXISTING describes ONLY; new MAINT-01 describe must not use it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export GameStateManager, gate watchdogs, make handleVotingTimeout public</name>
  <files>server/gameState.ts</files>
  <read_first>
    - server/gameState.ts (the file being modified — read the constructor L39-70 and the handleVotingTimeout signature at L1435, and the singleton export at L2111)
    - server/domains/CombatManager.ts L37-48 and L196-211 (DI constructor analog — the `deps` + `?? default` shape to mirror for the opts guard)
    - .planning/phases/48-testability-seams/48-PATTERNS.md (the "server/gameState.ts (MAINT-01)" section — exact target code)
  </read_first>
  <behavior>
    - Test 1: `new GameStateManager(undefined, { startWatchdogs: false })` constructs and is `instanceof GameStateManager`; no `setInterval` fires (assert via `vi.useFakeTimers()` + `vi.getTimerCount() === 0` after construction, or `vi.spyOn(global, 'setInterval')` not called).
    - Test 2: `new GameStateManager()` (default) starts exactly two intervals (assert `setInterval` spy called twice), proving the production default is unchanged.
    - Test 3: `handleVotingTimeout` is callable as a public method on a constructed instance with no `as any` — assert it advances `gamePhase` to `'reveal'` on a fixture lobby that has votes (mirrors the existing 42-02a safety-net assertion, but via the public method + `createLobby` public API).
  </behavior>
  <action>
    Apply exactly three changes to server/gameState.ts, no logic edits:
    (1) L39: change `class GameStateManager {` to `export class GameStateManager {`.
    (2) Constructor (currently `constructor(io?: SocketIOServer)`): add a second parameter
    `opts?: { startWatchdogs?: boolean }`. Compute `const startWatchdogs = opts?.startWatchdogs ?? true;`
    and wrap BOTH `setInterval` assignments (revivalWatchdog at L62, disconnectWatchdog at L67) in a
    single `if (startWatchdogs) { ... }` block. Mirror the `?? default` DI idiom from CombatManager.ts L196-211.
    (3) L1435: change `private handleVotingTimeout` to `public handleVotingTimeout` (keyword only; body unchanged).
    Leave the singleton at L2111 (`export const gameState = new GameStateManager();`) and
    `setGameStateIO` at L2114 byte-identical — the new opts param is optional so the no-arg call still
    starts both watchdogs. Do NOT touch the `revivalWatchdog`/`disconnectWatchdog` field declarations
    (they remain `NodeJS.Timeout`; they are simply only assigned when watchdogs start — confirm tsc is
    satisfied, using the definite-assignment `!` already present or adding it if tsc complains, without
    changing runtime behavior).
  </action>
  <verify>
    <automated>cd "C:/Users/Preston/git/ScrumMonsters" && npx vitest run server/gameState.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `server/gameState.ts` contains `export class GameStateManager`.
    - SOURCE: constructor signature includes `opts?: { startWatchdogs?: boolean }` and both `setInterval` calls are inside `if (startWatchdogs)`.
    - SOURCE: `public handleVotingTimeout` present; no remaining `private handleVotingTimeout`.
    - SOURCE: `export const gameState = new GameStateManager();` at module bottom is unchanged (no arguments passed → watchdogs default on).
    - BEHAVIOR: `npx vitest run server/gameState.test.ts` exits 0.
    - INVARIANT: `npm run check` (tsc) exits 0.
  </acceptance_criteria>
  <done>Class exported, watchdogs gated behind default-true opt, handleVotingTimeout public, singleton unchanged, tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add MAINT-01 seam describe block to server/gameState.test.ts</name>
  <files>server/gameState.test.ts</files>
  <read_first>
    - server/gameState.test.ts (the file being modified — read the full file to see existing describe structure and the `const gs = gameState as any` pattern at L16 that the new block must NOT use)
    - server/domains/SessionManager.test.ts L1-19 (exact analog for constructor-injectable fixture setup)
    - .planning/phases/48-testability-seams/48-PATTERNS.md (the "server/gameState.test.ts (MAINT-01)" section — target describe block)
    - .planning/phases/48-testability-seams/48-RESEARCH.md (Pitfall 3 and Pitfall 6 — `as any` must not appear in new tests; `handleVotingTimeout` no-ops the io cascade when io is undefined, which is expected)
  </read_first>
  <behavior>
    - Test 1 ("constructs without starting timers"): construct `new GameStateManager(undefined, { startWatchdogs: false })`; assert no timer leak (use `vi.useFakeTimers()` in this describe; assert `vi.getTimerCount()` is 0 after construction, then `vi.useRealTimers()` in afterEach).
    - Test 2 ("default constructor starts watchdogs"): spy on `setInterval`; `new GameStateManager()`; assert spy called exactly twice — proves production default unchanged.
    - Test 3 ("handleVotingTimeout callable without as any"): construct with `{ startWatchdogs: false }`, build a fixture lobby via the public `createLobby(...)` API and advance it to a voting state, call `gs.handleVotingTimeout(lobbyId)` directly (no `as any`), assert the resulting `gamePhase` mutation. Add a code comment noting the io-cascade no-ops when io is undefined (RESEARCH Pitfall 6) — this is expected and the phase mutation still fires.
  </behavior>
  <action>
    Add a NEW `describe('GameStateManager — MAINT-01 testability seam', ...)` block at the END of
    server/gameState.test.ts. Add `import { GameStateManager } from './gameState';` as a named import
    (alongside the existing `gameState` singleton import). Use only the public API for fixture setup:
    `new GameStateManager(undefined, { startWatchdogs: false })` then `createLobby(...)` — do NOT use
    `const gs = ... as any` anywhere in this new block (existing blocks keep their `as any` — out of scope).
    For the timer-leak assertion, scope `vi.useFakeTimers()` to this describe's `beforeEach` and restore
    with `vi.useRealTimers()` in `afterEach` so the rest of the suite is unaffected. Mirror the
    SessionManager.test.ts fixture-construction style. Cover all three behaviors above.
  </action>
  <verify>
    <automated>cd "C:/Users/Preston/git/ScrumMonsters" && npx vitest run server/gameState.test.ts && npm test && npm run check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `server/gameState.test.ts` contains `import { GameStateManager }` and a `describe('GameStateManager — MAINT-01 testability seam'` block.
    - SOURCE: the new describe block contains zero occurrences of `as any`.
    - SOURCE: the new block uses `new GameStateManager(undefined, { startWatchdogs: false })` for fixtures.
    - BEHAVIOR: the three new tests pass (`npx vitest run server/gameState.test.ts` exits 0).
    - INVARIANT: `npm test` exits 0 with 890+ tests passing (no regressions); `npm run check` exits 0; `npm run lint` exits 0.
    - INVARIANT: no `MaxListenersExceededWarning` newly introduced by this plan (this plan does not touch eventBus wiring).
  </acceptance_criteria>
  <done>New seam tests prove the class is constructable without timer leaks and handleVotingTimeout is public-callable; full suite green; tsc + lint clean.</done>
</task>

</tasks>

<verification>
- `npm test` → 890+ tests pass, 0 failures.
- `npm run check` → 0 errors.
- `npm run lint` → 0 problems.
- `server/websocket.ts:11` still imports `{ gameState, setGameStateIO }` and the server boots (type-checks) unchanged.
- Production singleton starts both watchdogs (Test 2 proves the default path).
</verification>

<success_criteria>
GameStateManager is exported and constructable with `{ startWatchdogs?: boolean }` (default true);
`handleVotingTimeout` is public; new tests instantiate it with no `as any` and no leaked timers; the
production singleton and all import sites are byte-identical; full suite green, tsc clean, lint clean.
</success_criteria>

<output>
Create `.planning/phases/48-testability-seams/48-01-SUMMARY.md` when done.
</output>
