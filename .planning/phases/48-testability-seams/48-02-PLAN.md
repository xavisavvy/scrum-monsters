---
phase: 48-testability-seams
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/domains/CombatManager.ts
  - server/domains/index.ts
  - server/domains/CombatManager.test.ts
autonomous: true
requirements: [MAINT-02]
must_haves:
  truths:
    - "CombatManagerDeps has an optional damageInterceptor dependency defaulting to pass-through"
    - "All 7 internal applyDamageToPlayer call sites route through the interceptor"
    - "The module-scope monkey-patch of combatManager.applyDamageToPlayer in domains/index.ts is removed"
    - "Production combatManager is constructed WITH the shield-absorption damageInterceptor — shield behavior is byte-identical"
    - "A new CombatManager test exercises shield absorption via an injected interceptor (previously untested path)"
    - "Full suite stays green (890+ tests), tsc clean, lint clean — including the existing AbilityEffectHandler shield tests"
  artifacts:
    - path: "server/domains/CombatManager.ts"
      provides: "damageInterceptor dep + applyDamageToPlayerRaw private method + public delegating applyDamageToPlayer"
      contains: "damageInterceptor"
    - path: "server/domains/index.ts"
      provides: "combatManager constructed with damageInterceptor; monkey-patch block deleted"
      contains: "damageInterceptor:"
    - path: "server/domains/CombatManager.test.ts"
      provides: "Injected-interceptor shield-absorption test"
      contains: "damageInterceptor"
  key_links:
    - from: "server/domains/CombatManager.ts applyDamageToPlayer"
      to: "this.damageInterceptor"
      via: "public method delegates to interceptor, interceptor calls applyFn → applyDamageToPlayerRaw"
      pattern: "this\\.damageInterceptor\\("
    - from: "server/domains/index.ts"
      to: "reduceShield / getShieldAbsorption / combat:shield_absorbed"
      via: "damageInterceptor closure passed at construction"
      pattern: "reduceShield\\("
---

<objective>
Open the MAINT-02 testability seam: replace the module-scope monkey-patch of
`combatManager.applyDamageToPlayer` (server/domains/index.ts:462-486) with a first-class, optional
`damageInterceptor` dependency on `CombatManagerDeps`. Rename the existing method body to a private
`applyDamageToPlayerRaw`, make the public `applyDamageToPlayer` delegate through the interceptor, and
wire the existing shield-absorption logic into the production construction at the `domains/index.ts`
construction site. Add a CombatManager test that exercises shield absorption via an injected
interceptor — the path that was previously untestable because the monkey-patch only existed on the
singleton.

Purpose: Shield absorption currently ships untested (the monkey-patch is applied at module scope to
the singleton, so `new CombatManager(...)` in unit tests gets the unpatched method). A first-class
dep makes the damage-routing path reachable and the production behavior identical.

Output: `damageInterceptor` dep, deleted monkey-patch, production wiring, new interceptor test.

INVARIANT (non-negotiable): byte-identical production behavior. The production `combatManager` MUST be
constructed with the shield-absorption interceptor (the exact logic moved out of the monkey-patch), so
shield absorption behaves identically. The default interceptor (when none supplied) is pure
pass-through. All 7 internal call sites stay as `this.applyDamageToPlayer(...)` — none may be rewritten
to `applyDamageToPlayerRaw`. Full suite stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-testability-seams/48-RESEARCH.md
@.planning/phases/48-testability-seams/48-PATTERNS.md

<interfaces>
<!-- Existing CombatManagerDeps interface. From server/domains/CombatManager.ts:37-48 -->
export interface CombatManagerDeps {
  eventBus: ScopedEventBus;
  getPlayerTeam?: (lobbyId: string, playerId: string) => TeamType | null;
  getPlayerClass?: (lobbyId: string, playerId: string) => AvatarClass | null;
  classMasteryManager?: { ... };
  progressionManager?: { getPlayerLevel: (lobbyId, playerId) => number };
}

<!-- NEW field to add (exact signature). damageInterceptor receives an applyFn that wraps applyDamageToPlayerRaw -->
damageInterceptor?: (lobbyId: string, playerId: string, damage: number, applyFn: (lobbyId: string, playerId: string, damage: number) => void) => void;
private readonly damageInterceptor: NonNullable<CombatManagerDeps['damageInterceptor']>;

<!-- The 7 internal call sites (all `this.applyDamageToPlayer(...)`) — MUST stay unchanged: -->
- CombatManager.ts L805  (minion attacks random fighting player)
- CombatManager.ts L1029 (pattern AoE telegraphed)
- CombatManager.ts L1035 (pattern AoE instant)
- CombatManager.ts L1148 (legacy AoE telegraphed)
- CombatManager.ts L1156 (legacy AoE instant)
- CombatManager.ts L1186 (legacy single-target telegraphed)
- CombatManager.ts L1190 (legacy single-target instant)

<!-- The monkey-patch to DELETE. From server/domains/index.ts:462-486 (verbatim in 48-PATTERNS.md) -->
const originalApplyDamage = combatManager.applyDamageToPlayer.bind(combatManager);
combatManager.applyDamageToPlayer = (lobbyId, playerId, damage) => { ...reduceShield + combat:shield_absorbed emits... };

<!-- Existing shield tests that will exercise the real interceptor after this change. -->
<!-- From server/domains/AbilityEffectHandler.test.ts:258-306 — import singleton from ./index, assert combat:shield_absorbed. -->

<!-- CombatManager.test.ts fresh-bus fixture. From server/domains/CombatManager.test.ts:18-39 -->
beforeEach(() => { eventBus = new ScopedEventBus(); combatManager = new CombatManager({ eventBus, getPlayerTeam, getPlayerClass }); });
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add damageInterceptor dep to CombatManager; rename body to applyDamageToPlayerRaw</name>
  <files>server/domains/CombatManager.ts</files>
  <read_first>
    - server/domains/CombatManager.ts (the file being modified — read L37-48 deps interface, L190-211 constructor + private readonly fields, the existing applyDamageToPlayer body ~L1257-1294, and the 7 call sites at L805/L1029/L1035/L1148/L1156/L1186/L1190 to confirm they read `this.applyDamageToPlayer`)
    - server/domains/SessionManager.ts (DI constructor analog — the `deps.x` assignment + `?? default` idiom)
    - .planning/phases/48-testability-seams/48-PATTERNS.md (the "CombatManager.ts (MAINT-02)" section — exact target additions)
    - .planning/phases/48-testability-seams/48-RESEARCH.md (Pattern 2 + Pitfall 2: optional dep silently falls back to pass-through if production forgets to wire it)
  </read_first>
  <behavior>
    - Test 1 (default pass-through): a `CombatManager` constructed with NO `damageInterceptor` applies full damage to a fighting player's hp (existing behavior preserved — assert `combat:player_damaged` with full damage, no `combat:shield_absorbed`).
    - Test 2 (injected interceptor halves/absorbs): a `CombatManager` constructed WITH a custom `damageInterceptor` that calls `applyFn(lobbyId, playerId, damage - 5)` results in the player taking `damage - 5` — proving the public method routes through the injected interceptor and `applyFn` reaches the raw method.
    - Test 3 (interceptor can fully absorb): an interceptor that does NOT call `applyFn` results in zero hp change — proving full absorption short-circuits the raw apply.
  </behavior>
  <action>
    Edit server/domains/CombatManager.ts only. Three additions, one rename, zero call-site edits:
    (1) Add to `CombatManagerDeps` (after `progressionManager`): `damageInterceptor?: (lobbyId: string,
    playerId: string, damage: number, applyFn: (lobbyId: string, playerId: string, damage: number) =>
    void) => void;`.
    (2) Add a `private readonly damageInterceptor: NonNullable<CombatManagerDeps['damageInterceptor']>;`
    field; in the constructor assign `this.damageInterceptor = deps.damageInterceptor ?? ((lobbyId,
    playerId, damage, applyFn) => { applyFn(lobbyId, playerId, damage); });`.
    (3) Rename the EXISTING `applyDamageToPlayer(...)` method (body ~L1257-1294) to a `private
    applyDamageToPlayerRaw(lobbyId, playerId, damage)` — body byte-identical.
    (4) Add a NEW public `applyDamageToPlayer(lobbyId, playerId, damage)` that calls
    `this.damageInterceptor(lobbyId, playerId, damage, (l, p, d) => this.applyDamageToPlayerRaw(l, p,
    d));`.
    DO NOT touch any of the 7 internal call sites at L805/L1029/L1035/L1148/L1156/L1186/L1190 — they
    already call `this.applyDamageToPlayer(...)` and now route through the interceptor automatically.
    Verify with a grep that no call site reads `applyDamageToPlayerRaw` (only the new public wrapper may).
  </action>
  <verify>
    <automated>cd "C:/Users/Preston/git/ScrumMonsters" && npx vitest run server/domains/CombatManager.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `CombatManagerDeps` contains the `damageInterceptor?:` field with the exact 4-arg signature.
    - SOURCE: a `private applyDamageToPlayerRaw` method exists; the public `applyDamageToPlayer` body is exactly the single `this.damageInterceptor(...)` delegation.
    - SOURCE: `grep -n "applyDamageToPlayerRaw" server/domains/CombatManager.ts` shows it referenced ONLY by the new public wrapper (and its own declaration) — the 7 attack call sites still read `this.applyDamageToPlayer`.
    - BEHAVIOR: `npx vitest run server/domains/CombatManager.test.ts` exits 0 (including the 3 new interceptor tests once added in Task 2's file edit — for this task the existing suite must still pass).
    - INVARIANT: `npm run check` exits 0.
  </acceptance_criteria>
  <done>Interceptor dep added with pass-through default; raw method renamed; public method delegates; all 7 call sites untouched; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire shield interceptor at production construction, delete monkey-patch, add interceptor test</name>
  <files>server/domains/index.ts, server/domains/CombatManager.test.ts</files>
  <read_first>
    - server/domains/index.ts (the file being modified — read the combatManager construction site L97-130 AND the monkey-patch block L462-486 to be deleted; confirm `reduceShield`, `getShieldAbsorption` are module-scope helpers in this file)
    - server/domains/CombatManager.test.ts (the test file being modified — read the fresh-bus fixture at L18-39 to mirror for the new interceptor test)
    - server/domains/AbilityEffectHandler.test.ts L258-306 (the existing shield tests that import the singleton and assert combat:shield_absorbed — these are the regression guard and must stay green)
    - .planning/phases/48-testability-seams/48-PATTERNS.md (the "domains/index.ts (MAINT-02 partial)" section — exact target construction with damageInterceptor)
    - .planning/phases/48-testability-seams/48-RESEARCH.md (Pitfall 2 — production must pass the interceptor or shield breaks silently)
  </read_first>
  <action>
    Two edits + one new test:
    (1) server/domains/index.ts — at the `combatManager` construction site (L97-130), add a
    `damageInterceptor:` property whose body is the EXACT shield logic currently in the monkey-patch:
    compute `const remainingDamage = reduceShield(lobbyId, playerId, damage);`; if `<= 0` emit
    `combat:shield_absorbed` with `absorbed: damage` + `shieldRemaining: getShieldAbsorption(...)` and
    return (do NOT call applyFn); else if `remainingDamage < damage` emit `combat:shield_absorbed` with
    `absorbed: damage - remainingDamage`; finally call `applyFn(lobbyId, playerId, remainingDamage)`.
    This is the verbatim logic from the monkey-patch, now passed as a constructor dep.
    (2) server/domains/index.ts — DELETE the entire monkey-patch block at L462-486
    (`const originalApplyDamage = combatManager.applyDamageToPlayer.bind(...)` through the reassignment).
    (3) server/domains/CombatManager.test.ts — add a `describe('CombatManager — damageInterceptor seam (MAINT-02)')`
    block implementing Task-1's three behaviors (default pass-through, injected partial absorb via
    `applyFn(..., damage - N)`, full absorb when interceptor skips `applyFn`). Use a fresh
    `new ScopedEventBus()` per test (mirror L18-39). This is the test that closes the previously-untested
    shield-routing path.
  </action>
  <verify>
    <automated>cd "C:/Users/Preston/git/ScrumMonsters" && npx vitest run server/domains/CombatManager.test.ts server/domains/AbilityEffectHandler.test.ts && npm test && npm run check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `server/domains/index.ts` combatManager construction includes `damageInterceptor:` with the `reduceShield` + `combat:shield_absorbed` + `applyFn` logic.
    - SOURCE: `grep -n "originalApplyDamage" server/domains/index.ts` returns no matches (monkey-patch fully removed); `grep -n "combatManager.applyDamageToPlayer =" server/domains/index.ts` returns no matches.
    - SOURCE: `server/domains/CombatManager.test.ts` contains a `damageInterceptor` describe block with ≥3 tests using a fresh `new ScopedEventBus()` per test.
    - BEHAVIOR: the existing AbilityEffectHandler shield tests (L258-306) still pass — they now exercise the real production interceptor.
    - INVARIANT: `npm test` exits 0 with 890+ tests passing; `npm run check` exits 0; `npm run lint` exits 0.
    - INVARIANT: shield absorption is byte-identical — a basic boss/minion attack against a shielded player still emits `combat:shield_absorbed` with the same absorbed/remaining values as before.
  </acceptance_criteria>
  <done>Production wiring carries the shield interceptor, monkey-patch deleted, new interceptor test green, regression shield tests green, full suite green, tsc + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal (server-only) | No external/untrusted input crosses any seam in this plan — damageInterceptor is a server-internal dependency over server-computed damage values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering | damageInterceptor wiring (silent pass-through if production forgets the dep) | mitigate | Production construction explicitly passes the shield interceptor; existing AbilityEffectHandler shield tests (L258-306) are the regression guard — they fail loudly if the interceptor is dropped |
| T-48-02 | Tampering | one of 7 call sites accidentally rewritten to applyDamageToPlayerRaw (bypasses shields) | mitigate | Task action forbids editing call sites; acceptance criterion greps that only the public wrapper references applyDamageToPlayerRaw |

No npm/pip/cargo installs in this plan — supply-chain threat (T-48-SC) not applicable.
</threat_model>

<verification>
- `npm test` → 890+ tests pass, 0 failures.
- `npm run check` → 0 errors.
- `npm run lint` → 0 problems.
- `grep originalApplyDamage server/domains/index.ts` → no matches (monkey-patch gone).
- AbilityEffectHandler shield tests pass against the real production interceptor.
</verification>

<success_criteria>
The module-scope monkey-patch is replaced by a first-class `damageInterceptor` dependency; all 7
internal `applyDamageToPlayer` call sites route through it; production construction carries the exact
shield-absorption logic so behavior is byte-identical; a new test exercises shield absorption via an
injected interceptor; full suite green, tsc clean, lint clean.
</success_criteria>

<output>
Create `.planning/phases/48-testability-seams/48-02-SUMMARY.md` when done.
</output>
