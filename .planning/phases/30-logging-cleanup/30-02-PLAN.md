---
phase: 30-logging-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - client/src/lib/stores/useAudio.tsx
  - client/src/lib/stores/useWebSocket.tsx
  - client/src/lib/stores/useAuth.tsx
  - client/src/lib/stores/useEventSync.ts
  - client/src/lib/stores/useAbilities.tsx
  - client/src/lib/socket/eventHandlers.ts
  - client/src/lib/utils/favoriteRoomsStorage.ts
  - client/src/lib/utils/lastLobbyStorage.ts
  - client/src/lib/utils/lobbySettingsStorage.ts
  - client/src/lib/utils/playerNameStorage.ts
  - client/src/lib/utils/teamPreferenceStorage.ts
  - client/src/pages/GamePage.tsx
  - client/src/pages/MenuPage.tsx
  - client/src/hooks/useImageDimensions.ts
  - client/src/hooks/useSpriteAnimation.ts
  - client/src/components/game/BattleScreen.tsx
  - client/src/components/game/BossDisplay.tsx
  - client/src/components/game/Lobby.tsx
  - client/src/components/game/PlayerController.tsx
  - client/src/components/game/PlayerHUD.tsx
  - client/src/components/game/ProjectileSystem.tsx
  - client/src/components/game/SpriteRenderer.tsx
  - client/src/components/game/phases/NextLevelPhase.tsx
  - client/src/components/game/phases/PhaseRenderer.tsx
  - client/src/components/game/phases/VictoryPhase.tsx
  - client/src/components/ui/CheatMenu.tsx
  - client/src/components/ui/ErrorBoundary.tsx
  - client/src/components/ui/ReconnectionDialog.tsx
  - client/src/components/ui/ReconnectionStatus.tsx
  - client/src/components/utils/BossTools.tsx
  - eslint.config.mjs
autonomous: true

must_haves:
  truths:
    - "ESLint no-console rule set to error level (was warn)"
    - "npm run lint passes with zero no-console violations"
    - "npm run build succeeds with no console-related errors"
    - "Test files retain console.log capability (excluded from no-console rule)"
    - "Client debug logging removed or converted to appropriate console.warn/error"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "ESLint no-console at error severity"
      contains: "\"no-console\": [\"error\""
  key_links:
    - from: "eslint.config.mjs"
      to: "all source files"
      via: "ESLint no-console error rule"
      pattern: "no-console.*error"
---

<objective>
Remove or convert all client-side console.log statements and upgrade ESLint no-console rule from warn to error.

Purpose: Fulfills LOG-02 requirement. After Plan 01 migrated server console.log to Pino, this plan handles the client side and locks the rule at error level so no future console.log can be introduced without ESLint failure.

Output: Zero console.log statements across entire codebase (except test files). ESLint no-console at error level with clean lint pass.
</objective>

<execution_context>
@C:/Users/Preston/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Preston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-logging-cleanup/30-01-SUMMARY.md
@eslint.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove or convert all client-side console.log statements</name>
  <files>
    client/src/lib/stores/useAudio.tsx
    client/src/lib/stores/useWebSocket.tsx
    client/src/lib/stores/useAuth.tsx
    client/src/lib/stores/useEventSync.ts
    client/src/lib/stores/useAbilities.tsx
    client/src/lib/socket/eventHandlers.ts
    client/src/lib/utils/favoriteRoomsStorage.ts
    client/src/lib/utils/lastLobbyStorage.ts
    client/src/lib/utils/lobbySettingsStorage.ts
    client/src/lib/utils/playerNameStorage.ts
    client/src/lib/utils/teamPreferenceStorage.ts
    client/src/pages/GamePage.tsx
    client/src/pages/MenuPage.tsx
    client/src/hooks/useImageDimensions.ts
    client/src/hooks/useSpriteAnimation.ts
    client/src/components/game/BattleScreen.tsx
    client/src/components/game/BossDisplay.tsx
    client/src/components/game/Lobby.tsx
    client/src/components/game/PlayerController.tsx
    client/src/components/game/PlayerHUD.tsx
    client/src/components/game/ProjectileSystem.tsx
    client/src/components/game/SpriteRenderer.tsx
    client/src/components/game/phases/NextLevelPhase.tsx
    client/src/components/game/phases/PhaseRenderer.tsx
    client/src/components/game/phases/VictoryPhase.tsx
    client/src/components/ui/CheatMenu.tsx
    client/src/components/ui/ErrorBoundary.tsx
    client/src/components/ui/ReconnectionDialog.tsx
    client/src/components/ui/ReconnectionStatus.tsx
    client/src/components/utils/BossTools.tsx
  </files>
  <action>
Remove or convert all 166 client-side console.log statements across 30 files. Pino is server-only, so client code uses a different strategy:

**Strategy by category:**

### Category 1: DELETE — Pure debug logging (remove entirely)
These are development-time debug logs that provide no value in production. Delete the entire console.log line.

Files and specific statements to DELETE:
- **useAudio.tsx** (44 statements): Remove ALL console.log calls. These are audio state debug logs ("Walking sound started", "Boss music muted", "Sound muted/unmuted", play prevented errors, etc.). Audio errors are non-critical and swallowed intentionally. Convert the few `console.warn` calls for localStorage failures to keep (already allowed by ESLint).
- **useWebSocket.tsx**: Remove all `console.log(...)` calls (~20 of 27). These are connection debug logs ("Connecting to WebSocket", "Connected to server", "Heartbeat started", "Reconnection attempt", "Lobby sync received"). Keep `console.warn(...)` and `console.error(...)` calls (already allowed by ESLint).
- **useEventSync.ts**: Remove all `console.log(...)` calls (~9 of 11). Event sync debug logs ("Processing event", "Replaying events", "Full state refresh"). Keep `console.warn(...)` calls.
- **eventHandlers.ts**: Remove all 3 `console.log(...)` calls ("Received full state refresh", "missed events", "handlers removed").
- **GamePage.tsx**: Remove all `console.log(...)` calls (~6 of 7). Reconnection and phase debug logs. Keep `console.warn(...)`.
- **MenuPage.tsx**: Remove both `console.log(...)` calls.
- **BossDisplay.tsx**: Remove all `console.log(...)` calls (5 of 8: "Boss defeated", "damage effect", "explosion completed"). Keep `console.warn(...)` and `console.error(...)`.
- **Lobby.tsx**: Remove all 7 `console.log(...)` calls ("saveLobbyName called", "trimmedName", etc.). These are debugging artifacts.
- **BattleScreen.tsx**: Remove `console.log(...)` calls for music effects and clipboard success (4 of 6). Keep `console.error(...)` for clipboard failures.
- **lobbySettingsStorage.ts**: Remove all `console.log(...)` calls (3 of 8: "settings saved", "loaded", "cleared"). Keep `console.warn(...)` for error handling.
- **useImageDimensions.ts**: Remove the `console.log(...)` for image dimensions. Keep `console.error(...)`.
- **BossTools.tsx**: Remove all 3 `console.log(...)` calls (debug tool logging).
- **CheatMenu.tsx**: Remove the `console.log(...)` call (cheat activation debug).
- **PlayerController.tsx**: Remove the `console.log(...)` call ("Cleric heals" debug).
- **PlayerHUD.tsx**: Remove the `console.log(...)` call ("Return Home button clicked" debug).
- **ProjectileSystem.tsx**: Remove the `console.log(...)` call ("received projectiles" debug).
- **NextLevelPhase.tsx**: Remove `console.log(...)` calls (2 of 3: "Starting staged transition", "Emitting proceed_next_level"). Keep `console.error(...)`.
- **ReconnectionDialog.tsx**: Remove `console.log(...)` ("Manual retry from dialog").
- **ReconnectionStatus.tsx**: Remove `console.log(...)` ("Manual retry triggered").
- **ErrorBoundary.tsx**: Remove `console.log(...)` calls (2 of 3: "DOM reconciliation", "recovery completed"). Keep `console.error(...)`.

### Category 2: KEEP — Already-allowed console.warn and console.error
These are already permitted by the ESLint `allow: ["warn", "error"]` config. No changes needed:
- All `console.warn(...)` calls in localStorage utilities (favoriteRoomsStorage, lastLobbyStorage, playerNameStorage, teamPreferenceStorage, lobbySettingsStorage)
- All `console.error(...)` calls in useAuth, BattleScreen, BossDisplay, VictoryPhase, NextLevelPhase, PhaseRenderer, ErrorBoundary, SpriteRenderer
- `console.warn(...)` in useSpriteAnimation, useAbilities, PhaseRenderer, BossDisplay, useEventSync

### Category 3: CONVERT to console.warn — Legitimate operational warnings
These provide useful runtime information that warrants a warning:
- **PhaseRenderer.tsx** line ~63: `console.error(...)` for missing phase component — already error, keep as-is
- Any remaining `console.log` that genuinely reports a degraded state — convert to `console.warn`

**Important rules:**
- Do NOT add any new logging library or abstraction for client code. Simply remove debug logs and keep existing console.warn/error (which ESLint allows).
- Do NOT convert debug console.log to console.warn — that would be semantic abuse. If it's a debug log, DELETE it.
- Verify each file still compiles after changes (no dangling references to removed console lines).
  </action>
  <verify>
Run `grep -rc "console\.log" client/src/ --include="*.ts" --include="*.tsx" | grep -v ":0$" | grep -v test` — should return empty (no console.log in client source).

Run `npm run check` — TypeScript compilation succeeds.
  </verify>
  <done>
Zero console.log statements in all client source files (excluding test files). Only console.warn and console.error remain (permitted by ESLint allow list). All files compile cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade ESLint no-console to error and verify clean build</name>
  <files>
    eslint.config.mjs
  </files>
  <action>
Upgrade the ESLint no-console rule from warn to error and add test file exclusion.

**Step 1: Update ESLint config**

In `eslint.config.mjs` line 186, change:
```
"no-console": ["warn", { "allow": ["warn", "error"] }],
```
to:
```
"no-console": ["error", { "allow": ["warn", "error"] }],
```

**Step 2: Add no-console override for test files**

In the test files config block (lines ~280-287), add `"no-console": "off"` to the rules:
```javascript
// Test files - more relaxed rules
{
  files: ["**/*.test.ts", "**/*.test.tsx", "**/*.spec.ts", "**/*.spec.tsx"],
  rules: {
    "@typescript-eslint/no-explicit-any": "off",
    "@typescript-eslint/no-non-null-assertion": "off",
    "no-console": "off",
  },
},
```

**Step 3: Add no-console override for k6 load test files**

In the k6 load test config block (lines ~289-309), add `"no-console": "off"` to the rules if not already present.

**Step 4: Verify clean lint**

Run `npm run lint` and confirm:
- Zero `no-console` errors
- Build is not broken by other pre-existing warnings
- The exit code is 0 (or only warnings from other rules)

**Step 5: Verify CI-compatible build**

Run `npm run build` to confirm the full build pipeline succeeds. ESLint at error level should not break the build since all console.log statements were removed in Plan 01 (server) and Task 1 of this plan (client).

**If any console.log statements were missed**: Fix them before committing. Run `grep -rn "console\.log" server/ client/src/ --include="*.ts" --include="*.tsx" | grep -v test | grep -v node_modules` to find stragglers.
  </action>
  <verify>
Run `npm run lint 2>&1 | grep "no-console"` — should return empty (zero violations).

Run `npm run lint` — exits without no-console errors.

Run `npm run build` — succeeds.

Run `grep "no-console" eslint.config.mjs` — shows `"no-console": ["error", { "allow": ["warn", "error"] }]`.
  </verify>
  <done>
ESLint no-console rule at error level. Zero no-console violations in lint output. Build succeeds. Test files excluded from no-console rule. LOG-02 requirement satisfied.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "console\.log" server/ client/src/ --include="*.ts" --include="*.tsx" | grep -v test | grep -v node_modules` — empty (zero console.log in source)
2. `npm run lint 2>&1 | grep "no-console"` — empty (zero violations)
3. `npm run lint` — passes (exit code 0 or warnings-only from other rules)
4. `npm run build` — succeeds
5. `npm run check` — TypeScript passes
6. `grep "no-console" eslint.config.mjs` — shows error level
7. Test files can still use console.log (confirmed by: `grep "no-console.*off" eslint.config.mjs`)
</verification>

<success_criteria>
All 166 client-side console.log statements removed. ESLint no-console upgraded from warn to error with clean lint pass. Build succeeds. Test files retain console.log capability. LOG-02 requirement satisfied. Combined with Plan 01, entire Phase 30 success criteria met.
</success_criteria>

<output>
After completion, create `.planning/phases/30-logging-cleanup/30-02-SUMMARY.md`
</output>
