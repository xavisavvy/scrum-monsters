---
phase: 16-class-mastery-system
plan: 05
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - client/src/components/game/TierUpToast.tsx
  - client/src/components/game/phases/BattlePhase.tsx
  - client/src/components/game/AvatarSelection.tsx
autonomous: true

must_haves:
  truths:
    - "Player sees tier-up notification when crossing mastery tier threshold"
    - "Player sees their mastery tier for each class in avatar selection screen"
    - "Tier-up notification does not overlap with level-up celebration"
  artifacts:
    - path: "client/src/components/game/TierUpToast.tsx"
      provides: "Tier-up notification toast component"
      exports: ["TierUpToast"]
    - path: "client/src/components/game/phases/BattlePhase.tsx"
      provides: "TierUpToast integrated into battle phase"
      contains: "TierUpToast"
    - path: "client/src/components/game/AvatarSelection.tsx"
      provides: "Mastery tier badges in avatar selection"
      contains: "MasteryBadge"
  key_links:
    - from: "client/src/components/game/TierUpToast.tsx"
      to: "client/src/lib/stores/useClassMastery.tsx"
      via: "useClassMastery().pendingTierUp subscription"
      pattern: "pendingTierUp"
    - from: "client/src/components/game/AvatarSelection.tsx"
      to: "client/src/lib/stores/useClassMastery.tsx"
      via: "useClassMastery().getMasteryForClass"
      pattern: "getMasteryForClass"
---

<objective>
Create the tier-up celebration notification and integrate mastery display into avatar selection screen.

Purpose: Give players visual feedback when they advance mastery tiers, and show mastery progress when choosing classes. Satisfies success criteria 2 (see mastery tier) and addresses the tier-up celebration interaction with level-up (Pitfall 5 from research).
Output: TierUpToast component, BattlePhase integration, avatar selection mastery display.
</objective>

<execution_context>
@C:/Users/Preston/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Preston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-class-mastery-system/16-RESEARCH.md
@.planning/phases/16-class-mastery-system/16-01-SUMMARY.md
@.planning/phases/16-class-mastery-system/16-04-SUMMARY.md

# Key references
@client/src/components/game/phases/BattlePhase.tsx
@client/src/components/game/AvatarSelection.tsx
@client/src/lib/stores/useClassMastery.tsx
@client/src/lib/stores/useProgression.tsx
@shared/classMasteryTypes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TierUpToast and integrate into BattlePhase</name>
  <files>
    client/src/components/game/TierUpToast.tsx
    client/src/components/game/phases/BattlePhase.tsx
  </files>
  <action>
**Create `client/src/components/game/TierUpToast.tsx`:**

A toast notification that appears when a player reaches a new mastery tier. Positioned to NOT overlap with the level-up celebration (which is fullscreen). Use a corner-positioned toast instead.

Props: none (reads from useClassMastery store directly)

Behavior:
- Subscribe to `useClassMastery().pendingTierUp`
- When pendingTierUp is set, show toast notification
- Auto-dismiss after 3 seconds (slightly longer than level-up's 2.5s)
- Call `clearTierUp()` on dismiss
- If useProgression's levelUp.active is true, DELAY showing tier-up toast until level-up clears (queue behavior from research Pitfall 5)

Styling:
- Position: fixed, bottom-right corner (bottom-4 right-4)
- JRPG gold theme: gradient background from-amber-900/90 to-yellow-900/90
- Border: border-amber-500
- Content: Class icon/name + "Promoted to Expert!" or "Promoted to Master!"
- Include the class name: "Ranger — Expert" format
- Entrance animation: slide up + fade in (CSS transition or framer-motion if available)
- Exit animation: fade out

Implementation detail for priority handling:
```typescript
const { pendingTierUp, clearTierUp } = useClassMastery();
const { levelUp } = useProgression();

// Delay tier-up toast if level-up celebration is active
const showTierUp = pendingTierUp && !levelUp?.active;
```

Use `useEffect` with a 3-second timeout for auto-dismiss when showTierUp becomes true.

**Integrate into BattlePhase.tsx:**

Add `<TierUpToast />` to the BattlePhase render tree. Place it after the existing LevelUpCelebration component (since tier-up is a toast, it won't conflict spatially — celebration is fullscreen, toast is corner).

Import TierUpToast and add it:
```tsx
<LevelUpCelebration />
<TierUpToast />
```
  </action>
  <verify>
- `npm run build` — production build succeeds
- `grep "TierUpToast" client/src/components/game/phases/BattlePhase.tsx` — component included
- TierUpToast reads pendingTierUp from useClassMastery store
- TierUpToast delays display when level-up celebration is active
- Auto-dismiss after 3 seconds with clearTierUp call
  </verify>
  <done>
TierUpToast displays gold-themed notification on tier advancement, auto-dismisses after 3s, defers to level-up celebration when both trigger simultaneously
  </done>
</task>

<task type="auto">
  <name>Task 2: Show mastery tiers in avatar selection screen</name>
  <files>client/src/components/game/AvatarSelection.tsx</files>
  <action>
**Add mastery badges to avatar selection screen:**

In AvatarSelection.tsx, for each avatar class card/option:
1. Import `useClassMastery` store and `MasteryBadge` component
2. For each class option, get mastery data: `useClassMastery.getState().getMasteryForClass(avatarClass)`
3. If mastery exists and tier is NOT 'Novice' (progressive disclosure — per Phase 15-08 pattern: only show above baseline), display a small `<MasteryBadge tier={tier} size="sm" />` on the avatar card

Placement: Below the class name or in the corner of the avatar card, depending on the existing layout. Look at the component structure and place it naturally.

Also add the `MasteryProgressBar` component. When a class is selected/hovered, show the MasteryProgressBar for that class below the class details panel. This gives players insight into their mastery progress for each class.

Import MasteryProgressBar and conditionally render it:
```tsx
{selectedClass && (
  <MasteryProgressBar avatarClass={selectedClass} className="mt-2" />
)}
```

**Progressive disclosure rules:**
- Novice tier: No badge shown (default state, no clutter)
- Expert tier: Amber badge shown on avatar card
- Master tier: Gold badge shown on avatar card
- Progress bar: Only shown for selected/focused class, only if player has any mastery XP for that class

This matches the Phase 15-08 pattern where level badges only appear for players above level 1.
  </action>
  <verify>
- `npm run build` — production build succeeds
- `grep "MasteryBadge" client/src/components/game/AvatarSelection.tsx` — badge component used
- `grep "MasteryProgressBar" client/src/components/game/AvatarSelection.tsx` — progress bar used
- Avatar selection shows mastery tier for Expert/Master classes
- Progress bar appears for selected class
  </verify>
  <done>
Avatar selection screen displays mastery tier badges (Expert/Master only) on class cards and shows class mastery XP progress bar for the selected class
  </done>
</task>

</tasks>

<verification>
- TierUpToast appears on tier advancement (pendingTierUp set)
- TierUpToast auto-dismisses after 3 seconds
- TierUpToast defers to level-up celebration (priority handling)
- Avatar selection shows mastery badges for Expert/Master classes
- Avatar selection shows progress bar for selected class
- Production build succeeds
- No test regressions
</verification>

<success_criteria>
- Player advancing from Novice to Expert sees a gold-themed tier-up toast notification
- Player advancing from Expert to Master sees a gold-themed tier-up toast notification
- Tier-up notification does NOT overlap with level-up celebration
- Player sees Expert/Master badges on class cards in avatar selection
- Player sees mastery XP progress bar when selecting a class
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-class-mastery-system/16-05-SUMMARY.md`
</output>
