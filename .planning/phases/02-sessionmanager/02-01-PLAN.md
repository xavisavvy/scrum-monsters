---
phase: 02-sessionmanager
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/errors/SessionErrors.ts
  - server/domains/SessionManager.ts
autonomous: true

must_haves:
  truths:
    - "SessionManager class can be instantiated with EventBus dependency"
    - "Typed exceptions exist for validation failures"
    - "SessionManager owns lobbies and playerToLobby Maps"
  artifacts:
    - path: "server/errors/SessionErrors.ts"
      provides: "Typed exception hierarchy for session validation"
      exports: ["SessionError", "LobbyNotFoundError", "LobbyFullError", "PlayerNotFoundError", "PlayerNotHostError", "ReconnectionFailedError"]
    - path: "server/domains/SessionManager.ts"
      provides: "SessionManager class shell with state ownership"
      exports: ["SessionManager", "SessionManagerDeps"]
  key_links:
    - from: "server/domains/SessionManager.ts"
      to: "server/events/ScopedEventBus.ts"
      via: "dependency injection"
      pattern: "eventBus.*ScopedEventBus"
---

<objective>
Create the SessionManager class shell and typed exception hierarchy for session validation errors.

Purpose: Establish the foundational infrastructure for the SessionManager domain manager, including typed exceptions for clear error handling and the class structure with dependency injection.

Output: SessionErrors.ts with exception hierarchy, SessionManager.ts with class structure and state Maps
</objective>

<execution_context>
@C:\Users\Preston\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Preston\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sessionmanager/02-CONTEXT.md
@.planning/phases/02-sessionmanager/02-RESEARCH.md
@server/events/ScopedEventBus.ts
@server/events/eventTypes.ts
@shared/types/SessionState.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create typed exception hierarchy</name>
  <files>server/errors/SessionErrors.ts</files>
  <action>
Create `server/errors/SessionErrors.ts` with a typed exception hierarchy for session validation errors. This follows the pattern from RESEARCH.md:

1. Create base `SessionError` class extending `Error` with `code` property
2. Create specific error classes:
   - `LobbyNotFoundError(lobbyId: string)` - code: 'LOBBY_NOT_FOUND'
   - `LobbyFullError(lobbyId: string, maxPlayers: number)` - code: 'LOBBY_FULL'
   - `PlayerNotFoundError(playerId: string)` - code: 'PLAYER_NOT_FOUND'
   - `PlayerNotHostError(playerId: string)` - code: 'NOT_HOST'
   - `ReconnectionFailedError(reason: 'invalid_token' | 'lobby_closed' | 'grace_expired')` - code: 'RECONNECTION_FAILED'

Each error class:
- Extends SessionError
- Takes relevant parameters in constructor
- Stores parameters as public readonly properties
- Sets appropriate `this.name` for stack traces

Export all error classes for use by SessionManager and socket handlers.
  </action>
  <verify>
Run `npx tsc --noEmit server/errors/SessionErrors.ts` - should compile without errors
  </verify>
  <done>
Six error classes exported: SessionError, LobbyNotFoundError, LobbyFullError, PlayerNotFoundError, PlayerNotHostError, ReconnectionFailedError
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SessionManager class shell</name>
  <files>server/domains/SessionManager.ts</files>
  <action>
Create `server/domains/SessionManager.ts` with the SessionManager class structure:

1. Import dependencies:
   - `ScopedEventBus` from '../events'
   - `Lobby, Player, TeamType, GamePhase, DisconnectedPlayer, ReconnectToken` from '../../shared/gameEvents'
   - Error classes from '../errors/SessionErrors'
   - `createHmac` from 'crypto' (for token generation later)

2. Define `SessionManagerDeps` interface:
   ```typescript
   export interface SessionManagerDeps {
     eventBus: ScopedEventBus;
   }
   ```

3. Create `SessionManager` class with:
   - Private state Maps:
     - `lobbies: Map<string, Lobby>`
     - `playerToLobby: Map<string, string>`
     - `disconnectedPlayers: Map<string, DisconnectedPlayer>`
     - `reconnectTokens: Map<string, ReconnectToken>`
     - `playerActivity: Map<string, number>` (for host transfer)

   - Private constants (per CONTEXT.md):
     - `DISCONNECT_GRACE_PERIOD = 10 * 60 * 1000` (10 minutes, but use 5 min for token validity)
     - `TOKEN_EXPIRY_TIME = 5 * 60 * 1000` (5 minutes per CONTEXT.md)
     - `TOKEN_SECRET` from env or random fallback

   - Constructor taking `deps: SessionManagerDeps`

   - Stub methods (to be implemented in later plans):
     - `createLobby(hostName: string, lobbyName: string, options?: CreateLobbyOptions): Lobby`
     - `joinLobby(lobbyId: string, playerName: string): { lobby: Lobby; player: Player }`
     - `getLobby(lobbyId: string): Lobby | null`
     - `getPlayerLobby(playerId: string): Lobby | null`
     - `removePlayer(playerId: string): Lobby | null`
     - `recordPlayerActivity(playerId: string): void`

4. Define `CreateLobbyOptions` interface for optional parameters:
   ```typescript
   export interface CreateLobbyOptions {
     customLobbyId?: string;
     timerSettings?: TimerSettings;
     jiraSettings?: JiraSettings;
     estimationSettings?: EstimationSettings;
   }
   ```

Stub methods should throw `new Error('Not implemented')` for now.

Export SessionManager, SessionManagerDeps, and CreateLobbyOptions.
  </action>
  <verify>
Run `npx tsc --noEmit server/domains/SessionManager.ts` - should compile without errors (stubs throw, which is valid)
  </verify>
  <done>
SessionManager class exists with state Maps, dependency injection, constants, and stub methods. Class can be instantiated with an EventBus.
  </done>
</task>

</tasks>

<verification>
1. `server/errors/SessionErrors.ts` exists and exports all error classes
2. `server/domains/SessionManager.ts` exists and exports SessionManager class
3. Both files compile without TypeScript errors
4. SessionManager can be instantiated: `new SessionManager({ eventBus: new ScopedEventBus() })`
</verification>

<success_criteria>
- Typed exception hierarchy provides clear error codes for each failure mode
- SessionManager class has state ownership (lobbies, playerToLobby, disconnectedPlayers, reconnectTokens)
- Dependency injection pattern established with ScopedEventBus
- Constants reflect CONTEXT.md decisions (5-minute token validity)
</success_criteria>

<output>
After completion, create `.planning/phases/02-sessionmanager/02-01-SUMMARY.md`
</output>
