---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/types/SessionState.ts
  - shared/types/EstimationState.ts
  - shared/types/CombatState.ts
  - shared/types/index.ts
autonomous: true

must_haves:
  truths:
    - "SessionState interface compiles with lobbyId, hostId, playerIds, currentPhase, timestamps"
    - "EstimationState interface compiles with votes Map, consensusReached flag, voting timestamps"
    - "CombatState interface compiles with boss health, player health Map, battle timing"
    - "All domain types are exported and importable from shared/types"
  artifacts:
    - path: "shared/types/SessionState.ts"
      provides: "Session domain state type definition"
      exports: ["SessionState"]
    - path: "shared/types/EstimationState.ts"
      provides: "Estimation domain state type definition"
      exports: ["EstimationState"]
    - path: "shared/types/CombatState.ts"
      provides: "Combat domain state type definition"
      exports: ["CombatState"]
    - path: "shared/types/index.ts"
      provides: "Barrel export for all domain types"
      exports: ["SessionState", "EstimationState", "CombatState"]
  key_links:
    - from: "shared/types/index.ts"
      to: "shared/types/*.ts"
      via: "barrel exports"
      pattern: "export.*from"
---

<objective>
Create domain state type definitions (SessionState, EstimationState, CombatState) as TypeScript interfaces for the domain separation refactor.

Purpose: Establish the vocabulary and contracts for the three domains (Session, Estimation, Combat) that will replace the monolithic Lobby type in later phases. These types use ID-based references to maintain domain isolation.

Output: Four TypeScript files in shared/types/ that compile cleanly and can be imported throughout the codebase.
</objective>

<execution_context>
@C:\Users\Preston\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Preston\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@shared/gameEvents.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SessionState type definition</name>
  <files>shared/types/SessionState.ts</files>
  <action>
Create a new file `shared/types/SessionState.ts` with the SessionState interface.

The interface must include:
- `lobbyId: string` - Unique identifier for the session
- `name: string` - Display name for the lobby
- `hostId: string` - ID of the current host player
- `playerIds: string[]` - Array of player IDs (not embedded Player objects)
- `currentPhase: GamePhase` - Current game phase (import from shared/gameEvents)
- `createdAt: number` - Unix timestamp in milliseconds when created
- `updatedAt: number` - Unix timestamp in milliseconds when last modified

Import GamePhase from `../gameEvents` using relative path.

Add JSDoc comment explaining this is the session domain state, separate from Estimation and Combat domains.
  </action>
  <verify>Run `npm run check` to verify TypeScript compiles without errors</verify>
  <done>SessionState interface exists with all required fields and compiles</done>
</task>

<task type="auto">
  <name>Task 2: Create EstimationState and CombatState type definitions</name>
  <files>shared/types/EstimationState.ts, shared/types/CombatState.ts</files>
  <action>
Create two new files for the remaining domain states:

**shared/types/EstimationState.ts:**
- `lobbyId: string` - Reference to session by ID
- `currentTicketId?: string` - ID of ticket being estimated (optional)
- `votes: Map<string, number | '?'>` - playerId to vote value mapping
- `votingStartedAt?: number` - Unix timestamp when voting started (optional)
- `consensusReached: boolean` - Whether consensus has been achieved
- `consensusValue?: number` - The agreed-upon value (optional)

**shared/types/CombatState.ts:**
- `lobbyId: string` - Reference to session by ID
- `bossId?: string` - ID of current boss (optional)
- `bossHealth: number` - Current boss HP
- `bossMaxHealth: number` - Maximum boss HP
- `playerHealth: Map<string, number>` - playerId to health mapping
- `playerPositions: Map<string, { x: number; y: number }>` - playerId to position mapping
- `battleStartTime?: number` - Unix timestamp when battle started (optional)
- `battleModifier: number` - Damage modifier (increases over time)

Add JSDoc comments to each interface explaining their domain purpose.

Note: Use Map types for runtime state. These will be converted to Record types for serialization in later phases.
  </action>
  <verify>Run `npm run check` to verify both files compile without errors</verify>
  <done>EstimationState and CombatState interfaces exist with all required fields and compile</done>
</task>

<task type="auto">
  <name>Task 3: Create barrel export and verify imports</name>
  <files>shared/types/index.ts</files>
  <action>
Create `shared/types/index.ts` as a barrel export file:

```typescript
/**
 * Domain State Types
 *
 * These types represent the three separated domains that replace
 * the monolithic Lobby type:
 * - SessionState: Lobby lifecycle, players, host management
 * - EstimationState: Voting, consensus, discussion
 * - CombatState: Boss battles, player health, positions
 *
 * Each domain stores IDs to reference other domains (not embedded objects).
 * This enables domain isolation and future domain manager separation.
 */

export { SessionState } from './SessionState';
export { EstimationState } from './EstimationState';
export { CombatState } from './CombatState';
```

After creating, verify the barrel export works by temporarily adding an import statement to shared/gameEvents.ts:
```typescript
// Verify domain types are importable (remove after verification)
import type { SessionState, EstimationState, CombatState } from './types';
```

Run `npm run check` to verify, then remove the test import.
  </action>
  <verify>
1. Run `npm run check` - TypeScript compiles with no errors
2. Run `npm test` - Existing tests pass (no regressions)
  </verify>
  <done>All domain types are exported from shared/types/index.ts and importable throughout codebase</done>
</task>

</tasks>

<verification>
1. Run `npm run check` - All TypeScript files compile without errors
2. Run `npm test` - Existing test suite passes (no regressions from adding new types)
3. Verify file existence:
   - shared/types/SessionState.ts exists
   - shared/types/EstimationState.ts exists
   - shared/types/CombatState.ts exists
   - shared/types/index.ts exists
4. Verify imports work from both server and client (path aliases resolve)
</verification>

<success_criteria>
- SessionState, EstimationState, CombatState interfaces compile cleanly
- All three types use ID-based references (strings) not embedded objects
- Barrel export allows importing all types from `shared/types`
- No regressions in existing test suite
- Types match the design documented in 01-RESEARCH.md Pattern 3
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
