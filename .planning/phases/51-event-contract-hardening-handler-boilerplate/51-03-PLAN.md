---
phase: 51-event-contract-hardening-handler-boilerplate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/lib/hooks/useViewport.ts
  - client/src/lib/hooks/useViewport.test.ts
  - client/src/components/game/PlayerController.tsx
autonomous: true
requirements: [MAINT-10]
nyquist_compliant: true

must_haves:
  truths:
    - "worldToPercent and percentToWorld are exported pure functions in useViewport.ts"
    - "worldToPercent ALWAYS clamps result to [0,100]; percentToWorld NEVER clamps (trusts server values)"
    - "All 5 open-coded coordinate conversion sites use the helpers (no remaining inline (x/worldWidth)*100 math)"
    - "Sites 2 (PlayerController:180-185) and 4 (:553-558) now clamp — documented as the ONE intentional runtime change in the phase"
    - "Projectile coords emitted from sites 2+4, now clamped, still render in-range (clamp only affects out-of-bounds edge cases)"
    - "npm test passes, npm run check is clean, npm run lint is clean"
  artifacts:
    - path: "client/src/lib/hooks/useViewport.ts"
      provides: "worldToPercent (clamps), percentToWorld (no clamp) pure exports; Site 5 camera-follow uses percentToWorld"
      contains: "export function worldToPercent"
    - path: "client/src/lib/hooks/useViewport.test.ts"
      provides: "round-trip + clamp-at-bounds tests; percentToWorld no-clamp test"
    - path: "client/src/components/game/PlayerController.tsx"
      provides: "5 coordinate sites refactored to helpers"
      contains: "worldToPercent"
  key_links:
    - from: "client/src/components/game/PlayerController.tsx"
      to: "useViewport worldToPercent/percentToWorld"
      via: "named import + call at each of the 4 in-component sites"
      pattern: "worldToPercent|percentToWorld"
---

<objective>
Extract `worldToPercent` (always clamps to [0,100]) and `percentToWorld` (never clamps) as pure named exports in `useViewport.ts`, then replace the 5 open-coded screen↔percent conversion sites (4 in PlayerController.tsx, 1 inside useViewport.ts) with calls to them.

Purpose: The 5 sites currently clamp inconsistently — sites 1 and 3 clamp, sites 2, 4, and 5 do not. "Consistent clamping" is the MAINT-10 success criterion, so canonicalizing all wire-write conversions to clamp IS the intent. This canonicalizes sites 2 and 4 (keyboard + click-to-shoot projectile emits) to clamp — the ONE permitted runtime change in this phase. Document it explicitly (not a silent change) and prove projectiles still render in-range.

Output: 2 pure helpers + tests; 5 sites refactored; the sites-2/4 clamping canonicalization documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-event-contract-hardening-handler-boilerplate/51-RESEARCH.md
@.planning/phases/51-event-contract-hardening-handler-boilerplate/51-PATTERNS.md

<interfaces>
<!-- Helper signatures + exact site map. From RESEARCH "The 5 Coordinate Sites" + PATTERNS. -->

useViewport.ts NEW exports (pure, no hook state — add after the hook, same file):
  export function worldToPercent(worldX: number, worldY: number, worldWidth: number, worldHeight: number): { x: number; y: number };
    // returns { x: clamp01_100((worldX/worldWidth)*100), y: clamp01_100((worldY/worldHeight)*100) } — ALWAYS clamps to [0,100]
  export function percentToWorld(percentX: number, percentY: number, worldWidth: number, worldHeight: number): { x: number; y: number };
    // returns { x: (percentX/100)*worldWidth, y: (percentY/100)*worldHeight } — NEVER clamps
  Existing exports: WORLD_WIDTH=1920, WORLD_HEIGHT=1080 (use these for the camera-follow site).

The 5 sites (exact behavior per RESEARCH):
  Site 1 PlayerController:71-76 — percent→world, pre-clamps input [0,100]. KEEP pre-clamp at call site, then percentToWorld. NO behavior change.
  Site 2 PlayerController:180-185 — world→percent, keyboard projectile emit, currently NO clamp. → worldToPercent. BEHAVIOR CHANGE (now clamps).
  Site 3 PlayerController:477-479 — world→percent, movement loop, currently clamps after convert. → worldToPercent. NO change (equivalent).
  Site 4 PlayerController:553-558 — world→percent, click-to-shoot emit, currently NO clamp. → worldToPercent. BEHAVIOR CHANGE (now clamps).
  Site 5 useViewport.ts:146-147 — percent→world, camera follow, no clamp. → percentToWorld(playerPos.x, playerPos.y, WORLD_WIDTH, WORLD_HEIGHT). NO change.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add worldToPercent/percentToWorld helpers + tests; convert Site 5</name>
  <files>client/src/lib/hooks/useViewport.ts, client/src/lib/hooks/useViewport.test.ts</files>
  <behavior>
    - worldToPercent(960, 540, 1920, 1080) === { x: 50, y: 50 } (center → 50%).
    - worldToPercent(2000, 540, 1920, 1080).x === 100 (clamps above 100%).
    - worldToPercent(-100, 540, 1920, 1080).x === 0 (clamps below 0%).
    - percentToWorld(50, 50, 1920, 1080) === { x: 960, y: 540 } (50% → center).
    - percentToWorld(110, 50, 1920, 1080).x ≈ 2112 (does NOT clamp; 1920 * 1.1).
    - Round-trip: percentToWorld(worldToPercent(in-range world)) returns the original world coords (within float tolerance).
  </behavior>
  <read_first>
    - client/src/lib/hooks/useViewport.ts (the file being modified — read L1-7 for WORLD_WIDTH/HEIGHT exports and the export pattern; L146-147 for Site 5 camera-follow `(playerPos.x / 100) * WORLD_WIDTH`)
    - ANALOG (helper bodies + JSDoc with behavior-change note): 51-PATTERNS.md "useViewport.ts" section (exact worldToPercent/percentToWorld bodies, the Site 5 BEFORE/AFTER)
    - ANALOG (pure-function test, no DOM): 51-PATTERNS.md "useViewport.test.ts" section + client/src/lib/utils/lastLobbyStorage.test.ts
  </read_first>
  <action>
    In useViewport.ts, add the two pure named exports AFTER the existing useViewport hook (same file), copying the bodies + JSDoc from PATTERNS. worldToPercent body: `{ x: Math.max(0, Math.min(100, (worldX/worldWidth)*100)), y: Math.max(0, Math.min(100, (worldY/worldHeight)*100)) }`. percentToWorld body: `{ x: (percentX/100)*worldWidth, y: (percentY/100)*worldHeight }`. Include the JSDoc on worldToPercent noting the sites-2/4 clamping behavior change is intentional.
    Convert Site 5 (L146-147): replace the inline `const worldX = (playerPos.x / 100) * WORLD_WIDTH; const worldY = (playerPos.y / 100) * WORLD_HEIGHT;` with `const { x: worldX, y: worldY } = percentToWorld(playerPos.x, playerPos.y, WORLD_WIDTH, WORLD_HEIGHT);` (no behavior change — percentToWorld does not clamp, matching the old code).
    Create useViewport.test.ts: Vitest pure-function tests, no DOM, no mocking. Cover every <behavior> bullet including the round-trip.
  </action>
  <verify>
    <automated>npm run check && npx vitest run client/src/lib/hooks/useViewport.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: useViewport.ts exports `worldToPercent` and `percentToWorld`; worldToPercent clamps with Math.max(0, Math.min(100, ...)); percentToWorld has NO clamp.
    - SOURCE: Site 5 (L146-147) uses percentToWorld (no remaining inline `/ 100) * WORLD_WIDTH` math there).
    - BEHAVIOR: `npm run check` exits 0; all useViewport.test.ts cases pass (center, clamp-high, clamp-low, percentToWorld no-clamp, round-trip).
  </acceptance_criteria>
  <done>Helpers exported with correct clamping asymmetry, Site 5 converted (no behavior change), pure-function tests green, npm run check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Convert PlayerController sites 1-4 + projectile-render proof</name>
  <files>client/src/components/game/PlayerController.tsx</files>
  <read_first>
    - client/src/components/game/PlayerController.tsx (the file being modified — read all 4 sites in full: L71-76, L180-185, L477-479, L553-558, plus the surrounding emit/sync context so the variable names mapped into the destructure are correct)
    - ANALOG (exact BEFORE/AFTER for each of the 4 sites + the clamping behavior-change summary table): 51-PATTERNS.md "PlayerController.tsx" section
    - Pitfall 3 in 51-RESEARCH.md (sites 2+4 clamping is intentional; out-of-bounds projectile coords are physically impossible in normal gameplay; accept the change)
  </read_first>
  <action>
    Add `import { worldToPercent, percentToWorld } from '@/lib/hooks/useViewport';` to PlayerController.tsx (match the file's existing import style for useViewport).
    Site 1 (L71-76): KEEP the defensive pre-clamp of serverPos.x/y to [0,100], then replace the manual world conversion with `const { x: worldX, y: worldY } = percentToWorld(clampedX, clampedY, viewport.worldWidth, viewport.worldHeight);`. NO behavior change.
    Site 2 (L180-185): replace the 4 manual percent computations with two worldToPercent calls (start + target) destructured into percentStartX/Y and percentTargetX/Y. This NOW clamps — intentional canonicalization.
    Site 3 (L477-479): replace the manual clamp-after-convert with `const { x: percentX, y: percentY } = worldToPercent(worldPos.x, worldPos.y, viewport.worldWidth, viewport.worldHeight);`. Equivalent — no behavior change.
    Site 4 (L553-558): same shape as Site 2 — two worldToPercent calls. NOW clamps — intentional canonicalization.
    Do NOT change any other coordinate math in this file beyond these 4 sites. Keep the emitted payload field names identical (percentStartX/Y, percentTargetX/Y, percentX/Y, worldX/Y) so no emit-shape change occurs.
    Projectile-render proof: add or extend a test (PlayerController test if one exists, else assert via the worldToPercent helper test added in Task 1) that confirms a projectile world coordinate that is IN-BOUNDS round-trips to the same in-range percent it did before, AND an out-of-bounds world coord now clamps to [0,100] (the only changed case). Document in the SUMMARY that sites 2+4 now clamp and that in-bounds gameplay coordinates are unaffected.
  </action>
  <verify>
    <automated>npm run check && npm test</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: PlayerController.tsx imports worldToPercent + percentToWorld; all 4 in-component sites use the helpers (grep for the old pattern `/ viewport.worldWidth) * 100` returns 0 matches in this file).
    - SOURCE: Site 1 retains the defensive pre-clamp of serverPos before percentToWorld; emit payload field names unchanged.
    - BEHAVIOR: in-bounds projectile world coords produce the same percent as before (proven by helper round-trip test); out-of-bounds coords now clamp to [0,100] (the documented intentional change).
    - DOC: SUMMARY explicitly records sites 2+4 as the one intentional runtime change in the phase, with the rationale (consistent clamping; out-of-bounds coords physically impossible in normal play).
    - SUITE INVARIANT: `npm test` exits 0 (938+ tests), `npm run check` exits 0, `npm run lint` exits 0.
  </acceptance_criteria>
  <done>All 5 sites use the helpers, Site 1 pre-clamp retained, sites 2+4 clamping canonicalization done + documented, projectile-render proof in place, full suite + check + lint clean.</done>
</task>

</tasks>

<verification>
- worldToPercent clamps / percentToWorld does not — unit tests pass.
- No remaining open-coded `(x / worldWidth) * 100` conversion math in PlayerController.tsx.
- Projectile in-bounds coords unchanged; out-of-bounds clamp documented + tested.
- `npm test` 938+ exits 0; `npm run check` exits 0; `npm run lint` exits 0.
</verification>

<success_criteria>
MAINT-10 satisfied: worldToPercent/percentToWorld replace the 5 open-coded sites with consistent clamping; sites 2 and 4 clamping canonicalization is documented as the one intentional runtime change and proven not to affect in-range projectile rendering.
</success_criteria>

<output>
Create `.planning/phases/51-event-contract-hardening-handler-boilerplate/51-03-SUMMARY.md` when done. Record the sites-2/4 clamping canonicalization decision.
</output>
