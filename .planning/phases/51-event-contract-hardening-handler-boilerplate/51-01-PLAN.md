---
phase: 51-event-contract-hardening-handler-boilerplate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/gameEvents.ts
  - shared/socket-schemas.ts
  - shared/socket-schemas.test.ts
  - server/events/ClientEventEmitter.ts
  - server/websocket.ts
autonomous: true
requirements: [EXT-04]
nyquist_compliant: true

must_haves:
  truths:
    - "Passing an event name not in ServerToClientEvents to emitFineGrained/emitToLobby is a tsc ERROR"
    - "Adding a bridge emit to a wire name not in ServerToClientEvents is a tsc ERROR (via _BRIDGE_COVERAGE satisfies guard)"
    - "Adding/removing a ClientEventSchemas entry that diverges from ClientToServerEvents keys is a tsc ERROR (via satisfies) and a vitest failure (key-set parity)"
    - "itemType and avatar/avatarClass wire payloads are typed by their domain unions, not bare string"
    - "All ~40 sequenced fine-grained events declare their seq/timestamp via Sequenced<T> (4 control messages excluded)"
    - "npm run check is clean, npm test passes, npm run lint is clean after the change"
  artifacts:
    - path: "shared/gameEvents.ts"
      provides: "Sequenced<T> export; ItemType import + 4 substitutions; AvatarClass 3 substitutions"
      contains: "export type Sequenced"
    - path: "shared/socket-schemas.ts"
      provides: "ClientEventSchemas with satisfies Record<keyof ClientToServerEvents, z.ZodTypeAny> (as const removed)"
      contains: "satisfies Record<keyof ClientToServerEvents"
    - path: "shared/socket-schemas.test.ts"
      provides: "key-set parity test (48 entries)"
    - path: "server/events/ClientEventEmitter.ts"
      provides: "emitFineGrained/emitToLobby constrained to keyof ServerToClientEvents; _BRIDGE_COVERAGE satisfies guard; emit(event as string) cast"
      contains: "_BRIDGE_COVERAGE"
    - path: "server/websocket.ts"
      provides: "emitFineGrained closure constrained to keyof ServerToClientEvents"
  key_links:
    - from: "server/events/ClientEventEmitter.ts"
      to: "shared/gameEvents.ts ServerToClientEvents"
      via: "keyof constraint on event param"
      pattern: "event: keyof ServerToClientEvents"
    - from: "shared/socket-schemas.ts ClientEventSchemas"
      to: "shared/gameEvents.ts ClientToServerEvents"
      via: "satisfies Record<keyof ClientToServerEvents, z.ZodTypeAny>"
      pattern: "satisfies Record<keyof ClientToServerEvents"
---

<objective>
Make the fine-grained socket event contract compile-checked end-to-end. Constrain `emitFineGrained`/`emitToLobby` to `keyof ServerToClientEvents`, add a `satisfies` bridge-coverage guard, convert `ClientEventSchemas` from `as const` to `satisfies Record<keyof ClientToServerEvents, z.ZodTypeAny>` (+ key-set parity test), add the `Sequenced<T>` envelope wrapper, and substitute domain wire unions (`ItemType`, `AvatarClass`).

Purpose: Kill the C1/C2/C3/C5 silent-drift bug class — adding/changing an event with a mismatched name or schema becomes a `tsc` error, not a production drift. This is forward-prevention: research verified all 22 current emit call sites are valid (no existing drift). The proof that the contract WORKS is a tsc-fails-on-injected-drift assertion, not just current green.

Output: Constrained emitter signatures, two `satisfies` guards, `Sequenced<T>`, 7 union substitutions, 1 new parity test. No wire or runtime behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-event-contract-hardening-handler-boilerplate/51-RESEARCH.md
@.planning/phases/51-event-contract-hardening-handler-boilerplate/51-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from RESEARCH + PATTERNS — no codebase exploration required for these. -->

shared/gameEvents.ts (source of truth — already exports ServerToClientEvents, ClientToServerEvents, AvatarClass):
  - Sequenced<T> NEW export: `export type Sequenced<T> = T & { seq: number; timestamp: number };`
  - ItemType lives in shared/itemTypes.ts:11 → `export type ItemType = 'heal_potion' | 'damage_boost' | 'shield';`
  - AvatarClass already exported FROM gameEvents.ts (no new import needed for it).

server/events/ClientEventEmitter.ts current (verified):
  - L583: `public emitFineGrained(lobbyId: string, event: string, data: Record<string, unknown>): void`
  - L594: `private emitToLobby(lobbyId: string, event: string, data: Record<string, unknown>): void`
  - L608: `this.io.to(lobbyId).emit(event, payload);`  ← needs `event as string` cast after narrowing
  - setupInternalEventListeners() closes after L575 — place _BRIDGE_COVERAGE immediately after.
  - Bridge remaps to capture as KEYS in _BRIDGE_COVERAGE: 'host_transferred' (NOT 'session:host_transferred', L119 emits the legacy wire name; it IS in ServerToClientEvents L388), 'estimation:consensus_reached' (from estimation:team_consensus_reached domain), 'stats:session_summary' (from stats:session_complete domain).

server/websocket.ts current (verified):
  - L129 closure: `const emitFineGrained = (lobbyId: string, event: string, data: Record<string, unknown>): void =>`
  - 20 call sites — all pass valid literal event names; NO call-site edits needed.

server/gameState.ts (NOT in files_modified — verify only): L1177, L1183 call emitter.emitFineGrained(..., 'estimation:votes_revealed', ...) — valid, compiles unchanged. Do NOT edit this file.

shared/socket-schemas.ts current (verified):
  - ClientEventSchemas declared L685-737, ends with `} as const;` — 48 entries.
  - ClientEventName = keyof typeof ClientEventSchemas (L742) and getClientEventSchema (L744+) must still resolve after removing as const.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Constrain emitter signatures + add _BRIDGE_COVERAGE satisfies guard</name>
  <files>server/events/ClientEventEmitter.ts, server/websocket.ts</files>
  <read_first>
    - server/events/ClientEventEmitter.ts (the file being modified — read L1-130 for imports + bridge registrations, L575-615 for emitFineGrained/emitToLobby/emit-call-site)
    - server/websocket.ts (the file being modified — read L1-135 for imports + the emitFineGrained closure at L129)
    - ANALOG: 51-PATTERNS.md "server/events/ClientEventEmitter.ts" section (target signatures, the _BRIDGE_COVERAGE shape, the `} satisfies Config` precedent at tailwind.config.ts:129) and the "Notable bridge remaps" list
  </read_first>
  <action>
    In ClientEventEmitter.ts: add `import { ServerToClientEvents } from '../../shared/gameEvents';` (match the file's existing relative-import style — verify against current imports). Narrow `emitFineGrained` (L583) and `emitToLobby` (L594) from `event: string` to `event: keyof ServerToClientEvents`. At the Socket.IO emit call (L608) add the cast: `this.io.to(lobbyId).emit(event as string, payload);` — required because Socket.IO's Server.emit types event as string (Pitfall 5).
    Immediately after `setupInternalEventListeners()` closes (after L575), add a module/class-level const `_BRIDGE_COVERAGE = { ... } satisfies Partial<Record<keyof ServerToClientEvents, true>>;` enumerating EVERY wire name the bridge emits via this.emitToLobby. Read the ~54 this.eventBus.on(...) registrations (L89-575) and include each emitted WIRE name as a `'name': true` key. CRITICAL remap keys: use `'host_transferred'` (NOT 'session:host_transferred'), `'estimation:consensus_reached'`, `'stats:session_summary'` per the remap list. Add a one-line JSDoc above it: every new emitToLobby wire name must be added here or tsc errors.
    In websocket.ts: add a `ServerToClientEvents` type import (use the @shared alias or the file's existing import style). Narrow the L129 closure param from `event: string` to `event: keyof ServerToClientEvents`. Make NO call-site edits (all 20 are valid). Do NOT touch server/gameState.ts.
    Confirm the host_transferred bridge at L119 still emits the legacy wire name `'host_transferred'` — do NOT rename it to 'session:host_transferred' (Pitfall 6 — GamePage host_transferred toast depends on it).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: ClientEventEmitter.ts emitFineGrained AND emitToLobby both declare `event: keyof ServerToClientEvents` (grep -c "event: keyof ServerToClientEvents" returns >= 2 in this file).
    - SOURCE: ClientEventEmitter.ts L608 emit call uses `event as string`.
    - SOURCE: `_BRIDGE_COVERAGE` const exists with `satisfies Partial<Record<keyof ServerToClientEvents, true>>` and includes the keys `'host_transferred'`, `'estimation:consensus_reached'`, `'stats:session_summary'`.
    - SOURCE: websocket.ts L129 closure declares `event: keyof ServerToClientEvents`.
    - SOURCE: server/gameState.ts is unchanged (not in this commit's diff).
    - BEHAVIOR: `npm run check` exits 0 (all 22 emit call sites compile because every literal is a valid keyof ServerToClientEvents).
    - DRIFT PROOF (do this, observe, then REVERT — do not commit the injected drift): temporarily change one emitFineGrained call site literal in websocket.ts to a bogus name (e.g. `'combat:NONEXISTENT'`) and add a bogus key to _BRIDGE_COVERAGE (e.g. `'combat:NONEXISTENT': true`); run `npm run check` and confirm it now ERRORS at both sites; then revert both edits and confirm `npm run check` is clean again. Record this proof in the SUMMARY.
  </acceptance_criteria>
  <done>emitFineGrained/emitToLobby/closure constrained, emit cast in place, _BRIDGE_COVERAGE satisfies guard added with the 3 remap keys, host_transferred unchanged, npm run check clean, drift-injection proof recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sequenced<T> + ItemType/AvatarClass substitutions + ClientEventSchemas satisfies + parity test</name>
  <files>shared/gameEvents.ts, shared/socket-schemas.ts, shared/socket-schemas.test.ts</files>
  <behavior>
    - Test (shared/socket-schemas.test.ts): `Object.keys(ClientEventSchemas).length === 48` — fails if a schema entry is added/removed without matching ClientToServerEvents.
    - Compile-time: `ClientEventSchemas satisfies Record<keyof ClientToServerEvents, z.ZodTypeAny>` errors if a key is missing or extra vs ClientToServerEvents.
    - Compile-time: events typed `Sequenced<...>` still carry seq+timestamp on the wire (no wire-format change — Sequenced<T> = T & { seq; timestamp }).
  </behavior>
  <read_first>
    - shared/gameEvents.ts (the file being modified — read the type-utility export region near the top, the ClientToServerEvents interface ~L245-336, and ServerToClientEvents ~L348-634; the specific substitution lines 335/471/506/516/587/595/603 and the ~40 events spelling out `seq: number; timestamp: number`)
    - shared/socket-schemas.ts (the file being modified — read L685-745: ClientEventSchemas declaration ending in `} as const;`, ClientEventName at L742, getClientEventSchema at L744+)
    - shared/itemTypes.ts (read L11 for the ItemType union)
    - ANALOG (Sequenced + substitutions): 51-PATTERNS.md "shared/gameEvents.ts" section (Change types A/B/C, exact lines, the BEFORE/AFTER for session:player_left, the NOT-CHANGED list)
    - ANALOG (satisfies): 51-PATTERNS.md "shared/socket-schemas.ts" section + tailwind.config.ts:129 `} satisfies Config;` precedent; Pitfall 1 (as const + satisfies are mutually exclusive)
    - ANALOG (test): 51-PATTERNS.md "shared/socket-schemas.test.ts" section and client/src/lib/utils/lastLobbyStorage.test.ts (Vitest describe/it/expect, no DOM)
  </read_first>
  <action>
    In shared/gameEvents.ts:
    (A) Add the `Sequenced<T>` export near the top type-utility region: `export type Sequenced<T> = T & { seq: number; timestamp: number };` with the JSDoc from PATTERNS (lists the included event families + the 4 excluded control messages). Then rewrite every fine-grained event in ServerToClientEvents that currently inlines `seq: number; timestamp: number` to wrap its remaining payload in `Sequenced<...>` (e.g. `'session:player_left': (data: Sequenced<{ playerId: string }>) => void;`). Apply to all session:*, estimation:*, combat:*, progression:*, class_mastery:*, ability:*, combo:*, item:*, stats:*, and system:full_state events. DO NOT wrap the 4 control messages: `system:missed_events`, `server_shutdown`, `connection_lost`, `reconnect_attempt` (they have no seq/timestamp).
    (B) Add `import type { ItemType } from './itemTypes';` and substitute `string` → `ItemType` at lines 335 (use_item itemType), 587 (item:awarded), 595 (item:used), 603 (item:effect_applied).
    (C) Substitute `string` → `AvatarClass` (already exported from this file) at lines 471 (combat:minion_spawned avatar), 506 (class_mastery:xp_awarded avatarClass), 516 (class_mastery:tier_up avatarClass).
    DO NOT change `bossType: string` (L455/456) or `attackType: string` (L472) — explicit non-goals (server-private BossType; mis-cited attackType union out of scope).
    In shared/socket-schemas.ts: add `import type { ClientToServerEvents } from './gameEvents';`. Replace the trailing `} as const;` on ClientEventSchemas with `} satisfies Record<keyof ClientToServerEvents, z.ZodTypeAny>;`. REMOVE `as const` entirely (mutually exclusive — Pitfall 1; do NOT write `} as const satisfies ...`). Verify ClientEventName (keyof typeof) and getClientEventSchema still typecheck.
    Create shared/socket-schemas.test.ts: Vitest describe/it/expect, assert `Object.keys(ClientEventSchemas).length === 48`.
  </action>
  <verify>
    <automated>npm run check && npx vitest run shared/socket-schemas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `export type Sequenced` exists in gameEvents.ts; the 4 control messages (system:missed_events, server_shutdown, connection_lost, reconnect_attempt) are NOT wrapped in Sequenced.
    - SOURCE: itemType uses `ItemType` at L335/587/595/603; avatar/avatarClass uses `AvatarClass` at L471/506/516; ItemType import present.
    - SOURCE: bossType (L455/456) and attackType (L472) are still `string` (unchanged).
    - SOURCE: socket-schemas.ts ends ClientEventSchemas with `satisfies Record<keyof ClientToServerEvents, z.ZodTypeAny>` and contains NO `as const` on that declaration.
    - BEHAVIOR: `npm run check` exits 0; `npx vitest run shared/socket-schemas.test.ts` passes (48 entries).
    - SUITE INVARIANT: `npm test` exits 0 (938+ tests), `npm run check` exits 0, `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Sequenced<T> added + applied to ~40 events (4 control excluded), 7 union substitutions done, bossType/attackType untouched, ClientEventSchemas uses satisfies (as const removed), parity test green, full suite + check + lint clean.</done>
</task>

</tasks>

<verification>
- `npm run check` exits 0 (the EXT-04 contract test).
- Drift-injection proof from Task 1 confirms the contract actually catches a bad event name (recorded in SUMMARY).
- `npx vitest run shared/socket-schemas.test.ts` passes.
- `npm test` 938+ exits 0; `npm run lint` exits 0.
- server/gameState.ts unchanged.
</verification>

<success_criteria>
EXT-04 satisfied: emit constrained to keyof ServerToClientEvents; _BRIDGE_COVERAGE + ClientEventSchemas satisfies guards in place; key-set parity test green; Sequenced<T> wraps the ~40 sequenced events; ItemType/AvatarClass substituted; bossType + minion attackType explicitly unchanged; contract proven by tsc-fails-on-injected-drift. No wire/runtime change.
</success_criteria>

<output>
Create `.planning/phases/51-event-contract-hardening-handler-boilerplate/51-01-SUMMARY.md` when done. Include the drift-injection proof result.
</output>
