---
phase: 16-class-mastery
plan: 06
type: execute
wave: 5
depends_on: ["16-03", "16-04", "16-05"]
files_modified:
  - server/domains/index.ts
  - client/src/components/game/Lobby.tsx
  - server/socketHandlers.ts
autonomous: false

must_haves:
  truths:
    - "Player earns class-specific XP when playing as that class during a game"
    - "Player sees their mastery tier in the lobby via badge, border, and title"
    - "Player can view detailed mastery progress for all 10 classes in profile panel"
    - "All players see tier-up celebration when someone tiers up"
    - "Player's class stats (HP, damage) improve based on mastery tier"
  artifacts:
    - path: "client/src/components/game/Lobby.tsx"
      provides: "MasteryBadge integration on player avatars + MasteryPanel access"
      contains: "MasteryBadge"
    - path: "server/socketHandlers.ts"
      provides: "Mastery data sync on player join, mastery event listener wiring"
      contains: "mastery:sync"
  key_links:
    - from: "client/src/components/game/Lobby.tsx"
      to: "client/src/components/game/MasteryBadge.tsx"
      via: "renders MasteryBadge for each player in lobby"
      pattern: "MasteryBadge"
    - from: "server/socketHandlers.ts"
      to: "server/domains/ClassMasteryManager.ts"
      via: "sends mastery:sync event on player join"
      pattern: "mastery:sync"
    - from: "server/domains/index.ts"
      to: "server/domains/CombatManager.ts"
      via: "getPlayerMasteryBonuses callback wired to ClassMasteryManager"
      pattern: "getPlayerMasteryBonuses"
---

<objective>
Wire all mastery components together: integrate badge into lobby, connect mastery panel, sync mastery data on join, and verify the full end-to-end flow.

Purpose: This plan connects all the isolated pieces (store, badge, panel, celebration, combat bonuses) into a working end-to-end system. Without this integration, the mastery system exists as disconnected components.
Output: Fully functional class mastery system visible to players.
</objective>

<execution_context>
@/Users/preston.farr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/preston.farr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-class-mastery/16-CONTEXT.md
@.planning/phases/16-class-mastery/16-03-SUMMARY.md
@.planning/phases/16-class-mastery/16-04-SUMMARY.md
@.planning/phases/16-class-mastery/16-05-SUMMARY.md

@server/domains/index.ts
@server/socketHandlers.ts
@client/src/components/game/Lobby.tsx
@shared/gameEvents.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire server-side mastery data sync and combat bonus callback</name>
  <files>server/domains/index.ts, server/socketHandlers.ts</files>
  <action>
1. In `server/domains/index.ts`:
   - Wire `getPlayerMasteryBonuses` callback from ClassMasteryManager to CombatManager:
     ```typescript
     // After classMasteryManager is created, update combatManager deps
     // OR if CombatManager accepts it in constructor, pass it there
     combatManager.setMasteryBonusCallback?.((playerId, avatarClass) => {
       const tier = classMasteryManager.getPlayerClassTier(playerId, avatarClass);
       return classMasteryManager.getStatBonuses(tier);
     });
     ```
   - Alternative: If CombatManager was built with getPlayerMasteryBonuses in deps (Plan 05), pass it during construction. Adjust creation order: create ClassMasteryManager before CombatManager, or use a setter method.

2. In `server/socketHandlers.ts`:
   - Add mastery data sync on player join/reconnect:
     - When a player joins a lobby, load their mastery data from storage/ClassMasteryManager
     - Emit `mastery:sync` event to the joining player with their full mastery data
     - Also emit each player's mastery data to new joiners so they see existing players' badges
   - Wire Socket.IO event listeners for mastery-related client requests:
     - On `mastery:request_sync`: Player requests their mastery data (for reconnect/refresh)
       - Respond with `mastery:sync` containing their ClassMasteryData

3. Ensure ClassMasteryManager tracks player class when avatar is selected:
   - In socketHandlers.ts where `select_avatar` is handled, call `classMasteryManager.setPlayerClass(lobbyId, playerId, avatarClass)`
   - This ensures the mastery manager knows which class each player is using for XP tracking

4. On lobby join, emit other players' mastery tiers to the joining player:
   - For each existing player in lobby, send their mastery data for badge display
   - OR include mastery tier data in the lobby state sync (lobby_updated event)
  </action>
  <verify>Run `npm run check` — TypeScript compiles. Run `npm test` — no regressions.</verify>
  <done>Mastery data syncs on player join. Player class tracking is wired on avatar selection. Combat mastery bonus callback is connected. Reconnecting players receive mastery state.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate MasteryBadge into Lobby and wire MasteryPanel access</name>
  <files>client/src/components/game/Lobby.tsx</files>
  <action>
1. In Lobby.tsx (or wherever player avatars are rendered in lobby phase):
   - Import MasteryBadge, MasteryPanel, MasteryTierCelebration, useClassMastery
   - For each player rendered in the lobby:
     - Get their mastery data from `useClassMastery().lobbyMasteryData[playerId]`
     - Get their current avatar class
     - Look up their tier: `masteryData?.[avatarClass]?.currentTier ?? 0`
     - Wrap or augment the player avatar display with `<MasteryBadge avatarClass={avatarClass} tier={tier} playerName={name} />`
   - The badge adds the colored border, badge icon, and title text to the player display

2. Add MasteryTierCelebration component:
   - Render `<MasteryTierCelebration />` at the lobby level (not per-player)
   - This component self-manages — it reads from the pendingTierUps queue in useClassMastery
   - It displays celebrations visible to ALL players when any player tiers up

3. Add MasteryPanel access:
   - Add a button/link in lobby UI to open the MasteryPanel (e.g., a small shield/book icon)
   - Use React state: `const [showMastery, setShowMastery] = useState(false)`
   - Render `<MasteryPanel isOpen={showMastery} onClose={() => setShowMastery(false)} />`
   - Position the trigger button near other profile/settings controls

4. Wire Socket.IO event handlers in the appropriate component (e.g., where other socket events are handled):
   - Listen for `mastery:xp_awarded` → call `useClassMastery().handleMasteryXPAwarded(data)`
   - Listen for `mastery:tier_up` → call `useClassMastery().handleTierUp(data)`
   - Listen for `mastery:sync` → call `useClassMastery().handleMasterySync(data)`
   - Follow the same pattern used for progression events in Phase 15

5. Ensure cleanup:
   - On lobby leave/disconnect, call `useClassMastery().reset()`
  </action>
  <verify>Run `npm run check` — TypeScript compiles. Run `npm test` — no regressions.</verify>
  <done>MasteryBadge renders on all player avatars in lobby. MasteryPanel is accessible via button. MasteryTierCelebration displays on tier-ups. Socket.IO events wired to store.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete class mastery system with 5-tier progression, lobby badges, profile panel, broadcast celebrations, and combat stat bonuses.</what-built>
  <how-to-verify>
  1. Start dev server: `npm run dev`
  2. Open browser to http://localhost:5000
  3. Create a lobby and join with at least 2 players (two browser tabs)

  **Mastery Badge (Lobby):**
  4. In lobby, verify player avatars show mastery indicators
  5. New players should show "Unranked" (gray, no badge icon)

  **Mastery XP Tracking:**
  6. Start a game and submit votes
  7. Check if mastery XP is tracking for the selected class (may need debug overlay or console)

  **Mastery Panel (Profile):**
  8. Look for mastery panel button in lobby UI
  9. Open it — verify all 10 classes are listed
  10. Unplayed classes should appear grayed out/locked
  11. Played class should show progress bar with JRPG gold styling

  **Tier-Up Celebration (if XP is sufficient):**
  12. If testing with low thresholds or debug XP: trigger a tier-up
  13. Verify celebration is visible to ALL players in lobby
  14. Verify celebration shows player name + tier name + class name

  **Combat Stat Bonuses:**
  15. If a player has mastery tier > 0, their combat stats should show slight improvement

  **Known limitation:** Mastery progress may be slow to observe in a single session due to steep XP thresholds. The system is designed for long-term engagement.
  </how-to-verify>
  <resume-signal>Type "approved" to mark phase complete, or describe issues to address.</resume-signal>
</task>

</tasks>

<verification>
1. `npm run check` passes — no TypeScript errors
2. `npm test` passes — no regressions
3. Player avatars in lobby show MasteryBadge with correct tier visuals
4. MasteryPanel opens and shows all 10 classes
5. Socket.IO mastery events flow from server to client
6. MasteryTierCelebration component is rendered in lobby
7. CombatManager applies mastery bonuses via callback
</verification>

<success_criteria>
- Player earns class-specific XP when playing as that class (MSTR-01)
- Player sees their mastery tier for each class via badge and panel (MSTR-02)
- Player's class stats improve based on mastery tier during combat (MSTR-03)
- Mastery system hints at ability unlocks at higher tiers (placeholder for MSTR-04, actual abilities in Phase 18)
- All players see broadcast tier-up celebrations
- Human verification confirms end-to-end system works
</success_criteria>

<output>
After completion, create `.planning/phases/16-class-mastery/16-06-SUMMARY.md`
</output>
