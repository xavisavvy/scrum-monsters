---
phase: 16-class-mastery
plan: 04
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - client/src/components/game/MasteryPanel.tsx
  - client/src/components/game/MasteryProgressBar.tsx
  - client/src/components/game/MasteryProgressBar.css
autonomous: true

must_haves:
  truths:
    - "Player sees mastery progress for ALL avatar classes in profile panel"
    - "Unplayed classes appear grayed out/locked to encourage exploration"
    - "Progress bars use JRPG gold styling matching Phase 15 XP bar"
    - "Each class shows tier name, progress bar, and XP numbers"
    - "Expert+ tiers show grayed-out ability unlock teasers (foundation for Phase 18)"
  artifacts:
    - path: "client/src/components/game/MasteryProgressBar.tsx"
      provides: "Reusable progress bar with tier-specific colors and JRPG styling"
      exports: ["MasteryProgressBar"]
    - path: "client/src/components/game/MasteryPanel.tsx"
      provides: "Profile panel showing all 10 classes with mastery progress and ability teasers"
      exports: ["MasteryPanel"]
  key_links:
    - from: "client/src/components/game/MasteryPanel.tsx"
      to: "client/src/lib/stores/useClassMastery.tsx"
      via: "useClassMastery hook for mastery data"
      pattern: "useClassMastery"
    - from: "client/src/components/game/MasteryProgressBar.tsx"
      to: "shared/progressionTypes.ts"
      via: "imports MASTERY_THRESHOLDS, MASTERY_TIER_NAMES for progress calculation"
      pattern: "MASTERY_THRESHOLDS"
---

<objective>
Create the detailed mastery profile panel showing all classes with progress bars.

Purpose: Players need to see their mastery progress across all 10 avatar classes, including unplayed classes (grayed out) to encourage exploration. This is the "detailed view" display location from CONTEXT.md.
Output: MasteryPanel component with per-class progress bars using JRPG gold styling.
</objective>

<execution_context>
@/Users/preston.farr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/preston.farr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-class-mastery/16-CONTEXT.md
@.planning/phases/16-class-mastery/16-02-SUMMARY.md

@shared/progressionTypes.ts
@client/src/components/game/XPBar.tsx
@client/src/components/game/XPBar.css
@shared/socket-schemas.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MasteryProgressBar with JRPG gold styling</name>
  <files>client/src/components/game/MasteryProgressBar.tsx, client/src/components/game/MasteryProgressBar.css</files>
  <action>
Create `client/src/components/game/MasteryProgressBar.tsx`:

1. Props interface:
   ```typescript
   interface MasteryProgressBarProps {
     avatarClass: string;
     totalXP: number;
     currentTier: number;
     gamesPlayed: number;
     isPlayed: boolean;  // false = grayed out
   }
   ```

2. Calculate progress to next tier:
   - Import MASTERY_THRESHOLDS and MASTERY_TIER_NAMES from shared/progressionTypes
   - If currentTier === 5 (Master): show "MAX" with full bar
   - Otherwise: calculate percentage between current tier threshold and next tier threshold
   - Display: "{currentXP} / {nextThreshold} XP"

3. Tier-specific bar colors (CSS custom properties):
   - Unranked (0): #666 gray
   - Novice (1): bronze gradient (#CD7F32 -> #D4A038 -> #DEB887)
   - Apprentice (2): silver gradient (#C0C0C0 -> #D8D8D8 -> #E8E8E8)
   - Adept (3): gold gradient (#b8860b -> #ffd700 -> #ffec8b) — matches Phase 15 XP bar
   - Expert (4): platinum gradient (#E5E4E2 -> #F0F0F0 -> #FFFFFF)
   - Master (5): diamond gradient (#87CEEB -> #B9F2FF -> #E0FFFF) with shimmer animation

4. Component structure:
   - Tier icon (emoji) on left
   - Class name + tier name label
   - Progress bar track (dark background, tier-colored fill)
   - XP text on right ("{current}/{needed}")
   - JRPG beveled edge effect on fill (same as XP bar — `::after` pseudo with white gradient)
   - When `isPlayed: false`, render entire row at 40% opacity with lock icon

Create `client/src/components/game/MasteryProgressBar.css`:
   - Reuse the JRPG styling patterns from XPBar.css:
     - Dark track background: `linear-gradient(180deg, #2a2a2a, #1a1a1a)`
     - Track border: `1px solid #4a4a4a`
     - Beveled fill: `::after` with white gradient overlay on top half
     - Border-radius: 6px for track, 5px for fill
   - Transition on fill width: `width 0.5s ease-out`
   - Grayed out state: `opacity: 0.4; filter: grayscale(100%)`
   - Master shimmer animation: `@keyframes shimmer` that moves a highlight across the bar
  </action>
  <verify>Run `npm run check` — TypeScript compiles. Visual output matches JRPG gold aesthetic.</verify>
  <done>MasteryProgressBar renders tier-colored progress with JRPG beveled styling. Unplayed classes are grayed out. Master tier has shimmer animation.</done>
</task>

<task type="auto">
  <name>Task 2: Create MasteryPanel showing all classes</name>
  <files>client/src/components/game/MasteryPanel.tsx</files>
  <action>
Create `client/src/components/game/MasteryPanel.tsx`:

1. Props interface:
   ```typescript
   interface MasteryPanelProps {
     isOpen: boolean;
     onClose: () => void;
   }
   ```

2. Import all 10 avatar classes from shared (AvatarClassSchema values or hardcoded list):
   `['ranger', 'rogue', 'bard', 'sorcerer', 'wizard', 'warrior', 'paladin', 'cleric', 'oathbreaker', 'monk']`

3. Get mastery data from `useClassMastery()` store

4. Component structure:
   - Modal/panel overlay with dark semi-transparent background
   - Panel header: "Class Mastery" title with JRPG styling (gold text, retro font)
   - Close button (X) in top-right
   - Grid/list of all 10 classes, each showing:
     - Class avatar icon/sprite (use class name text if no sprite available yet)
     - MasteryProgressBar for that class
     - Stats summary: "Games Played: {N}"
   - Sort order: Played classes first (sorted by tier desc, then XP desc), unplayed classes last (sorted alphabetically)
   - Classes where `gamesPlayed === 0` or class not in masteryData render as grayed/locked
   - Per CONTEXT.md: "unplayed ones grayed out/locked to encourage exploration"

5. Panel styling:
   - Dark background matching existing game UI (rgba(0, 0, 0, 0.85))
   - Gold border: `2px solid #4a3f2f`
   - Scrollable if more than visible area
   - Max-width: 600px, centered
   - Use existing retro/JRPG styling patterns

6. Summary stats at top:
   - "Classes Mastered: {count}/10"
   - "Highest Tier: {tierName} {className}" or "None" if all unranked

7. Ability unlock teasers (foundation for Phase 18 — MSTR-04):
   - Below each class's progress bar row, show ability unlock preview lines for Expert (tier 4) and Master (tier 5) tiers
   - Define a static map of class-to-ability-name teasers (placeholder names — Phase 18 will define real abilities):
     ```typescript
     const CLASS_ABILITY_TEASERS: Record<string, { expert: string; master: string }> = {
       warrior:    { expert: "Shield Wall",      master: "Berserker Rage" },
       paladin:    { expert: "Holy Shield",      master: "Divine Judgment" },
       cleric:     { expert: "Group Heal",       master: "Resurrection" },
       ranger:     { expert: "Rain of Arrows",   master: "Eagle Eye" },
       rogue:      { expert: "Shadow Step",      master: "Assassinate" },
       bard:       { expert: "War Song",         master: "Inspire" },
       sorcerer:   { expert: "Chain Lightning",  master: "Meteor" },
       wizard:     { expert: "Arcane Barrier",   master: "Time Stop" },
       oathbreaker:{ expert: "Dread Aura",       master: "Soul Harvest" },
       monk:       { expert: "Ki Strike",        master: "Inner Peace" },
     };
     ```
   - For each class entry, render 1-2 ability teaser lines:
     - If player tier < 4 (Expert): show grayed out with lock icon: "🔒 Ability: Shield Wall — unlocks at Expert tier"
     - If player tier >= 4 but < 5: show Expert ability unlocked (bright), Master ability still locked
     - If player tier === 5: show both abilities unlocked (bright)
   - Styling: grayed out teasers at `opacity: 0.45`, smaller font (`0.8em`), italic text
   - Unlocked abilities: normal opacity, gold text color (`#ffd700`)
   - This is purely UI — no ability logic. Phase 18 will wire real abilities to these slots.

Do NOT integrate into game UI yet — that will be done in Plan 06 (integration).
  </action>
  <verify>Run `npm run check` — TypeScript compiles. Component is importable and renders without errors.</verify>
  <done>MasteryPanel shows all 10 classes with progress bars. Played classes are sorted by tier/XP. Unplayed classes are grayed out. Each class shows ability unlock teasers (locked/unlocked based on tier). Panel uses JRPG dark theme styling.</done>
</task>

</tasks>

<verification>
1. `npm run check` passes — no TypeScript errors
2. `npm test` — no regressions
3. MasteryProgressBar renders correctly for all tier levels (0-5)
4. MasteryPanel shows all 10 avatar classes
5. Unplayed classes appear grayed out
6. Progress bars use JRPG gold gradient styling
7. Each class entry shows ability unlock teasers (locked for tiers below Expert, unlocked at Expert/Master)
</verification>

<success_criteria>
- MasteryProgressBar shows tier-colored progress with JRPG beveled styling
- MasteryPanel displays all 10 avatar classes with their mastery state
- Played classes show colored progress bars and tier info
- Unplayed classes appear grayed out/locked per CONTEXT.md
- Each class shows ability unlock teasers: locked below Expert tier, unlocked at Expert/Master (MSTR-04 foundation for Phase 18)
- Styling matches Phase 15 JRPG aesthetic (gold gradients, dark backgrounds, beveled edges)
- Components ready for integration in Plan 06
</success_criteria>

<output>
After completion, create `.planning/phases/16-class-mastery/16-04-SUMMARY.md`
</output>
