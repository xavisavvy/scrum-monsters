---
phase: 29-hosting-analysis
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - tests/profiling/cost-analyzer.ts
  - tests/profiling/report-generator.ts
  - tests/profiling/generate-report.ts
autonomous: true
must_haves:
  truths:
    - "Operator reads cost comparison table showing monthly costs for Replit vs Railway vs Render vs Fly.io vs AWS Lightsail at measured resource levels"
    - "Operator receives clear recommendation (platform + tier) that fits $5-20/mo budget with headroom for 2x traffic growth"
    - "Report includes performance bottleneck analysis with actionable mitigation steps"
  artifacts:
    - path: "tests/profiling/cost-analyzer.ts"
      provides: "Hosting platform cost comparison engine with budget filtering and headroom calculation"
      min_lines: 80
    - path: "tests/profiling/report-generator.ts"
      provides: "Markdown report generator with executive summary, cost table, bottleneck analysis, and recommendation"
      min_lines: 100
    - path: "tests/profiling/generate-report.ts"
      provides: "CLI script that reads profile JSON and generates hosting analysis markdown report"
      min_lines: 40
  key_links:
    - from: "tests/profiling/generate-report.ts"
      to: "tests/profiling/cost-analyzer.ts"
      via: "imports compareHosting function"
      pattern: "import.*compareHosting.*cost-analyzer"
    - from: "tests/profiling/generate-report.ts"
      to: "tests/profiling/report-generator.ts"
      via: "imports generateReport function"
      pattern: "import.*generateReport.*report-generator"
    - from: "tests/profiling/generate-report.ts"
      to: "tests/reports/profile-*.json"
      via: "reads profiling results JSON"
      pattern: "profile-.*\\.json"
---

<objective>
Build the cost comparison engine and report generator that transforms profiling data into actionable hosting recommendations.

Purpose: Raw metrics are useless without context. This plan takes the profiling results from Plan 01 and produces a human-readable hosting analysis comparing 5 platforms, identifying the best option within the $5-20/mo budget, and flagging bottlenecks with mitigation steps.

Output: Three files — cost-analyzer.ts (platform comparison logic), report-generator.ts (markdown formatting), and generate-report.ts (CLI entrypoint). Together they read profile JSON and produce a markdown hosting analysis report in tests/reports/.
</objective>

<execution_context>
@C:/Users/Preston/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Preston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-hosting-analysis/29-RESEARCH.md
@.planning/phases/29-hosting-analysis/29-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cost analyzer with 5-platform comparison and budget filtering</name>
  <files>tests/profiling/cost-analyzer.ts</files>
  <action>
Create `tests/profiling/cost-analyzer.ts` with hosting platform definitions and comparison logic.

**Platform definitions (verified from research, February 2026 pricing):**

1. **Render Starter** — $7/mo
   - 512MB RAM, 0.5 vCPU
   - No bandwidth charges (included)
   - Free SSL, auto-deploy from Git
   - WebSocket support: yes (sticky sessions built-in)
   - Persistent disk: $0.25/GB/mo extra
   - Notes: Zero-config deployment, Dockerfile or buildpack

2. **Render Standard** — $25/mo
   - 2GB RAM, 1 vCPU
   - Same perks as Starter at higher resource tier
   - Notes: If Starter is too tight, this is next step

3. **Railway Hobby** — $5/mo base + usage
   - Up to 8GB RAM, 8 vCPU (pay per use)
   - Compute: ~$0.000463/min per vCPU + ~$0.000231/min per 512MB
   - Calculate monthly cost: $5 + (avgCPU * 0.000463 * 43800) + (avgRAM_GB * 0.000231/0.512 * 43800)
   - Bandwidth: $0.10/GB egress
   - WebSocket support: yes
   - Notes: Usage-based can be cheap or expensive depending on utilization

4. **Fly.io** — $0/mo base (Hobby plan, free allowances)
   - Shared CPU VMs: ~$1.94/mo for shared-cpu-1x (256MB), ~$3.57/mo for 512MB, ~$7.12/mo for 1GB
   - 160GB egress free, then $0.02/GB
   - Free SSL, global edge deployment
   - WebSocket support: yes (requires fly.toml config for sticky sessions)
   - Notes: Global deployment good for distributed teams, VM pricing is straightforward

5. **Replit Core** — $25/mo
   - Includes $25 compute credits
   - ~8GB RAM, 4 vCPU available
   - Always-on deployment included
   - WebSocket support: yes (current hosting)
   - Notes: Currently used; expensive relative to alternatives for this workload

6. **AWS Lightsail** — $3.50-10/mo
   - $3.50: 512MB RAM, 1 vCPU, 1TB transfer
   - $5: 1GB RAM, 1 vCPU, 2TB transfer
   - $10: 2GB RAM, 1 vCPU, 3TB transfer
   - Free SSL via Load Balancer ($18/mo extra) or Let's Encrypt manual
   - WebSocket support: yes (requires manual nginx/Node config)
   - Notes: Cheapest raw compute, but more manual setup; no auto-deploy

**Interface:**
```typescript
export interface ResourceRequirements {
  peakRAMMB: number;
  avgCPUPercent: number;  // 0-100
  bandwidthGBPerMonth: number;
  budget: { min: number; max: number };
}

export interface HostingOption {
  platform: string;
  tier: string;
  monthlyCost: number;
  ramMB: number;
  cpuVCPU: number;
  bandwidthIncludedGB: number;
  meetsRequirements: boolean;
  headroom2x: number;  // % capacity remaining if traffic doubles
  websocketSupport: 'native' | 'config-required' | 'limited';
  autoDeployGit: boolean;
  notes: string;
}

export interface CostAnalysis {
  options: HostingOption[];
  withinBudget: HostingOption[];
  recommendation: HostingOption | null;
  recommendationReason: string;
}

export function compareHosting(requirements: ResourceRequirements): CostAnalysis;
```

**Comparison logic:**
- `meetsRequirements`: platform RAM >= peakRAM AND platform can handle avgCPU
- `headroom2x`: calculate what % of resources remain if peakRAM and avgCPU both double. Formula: `min((platformRAM - peakRAM*2) / platformRAM * 100, (platformCPU - avgCPU*2/100) / platformCPU * 100)`. Clamp to 0 minimum.
- `recommendation`: Pick the cheapest option that meetsRequirements AND has headroom2x >= 0% (can handle 2x growth) AND is within budget. If tie, prefer native WebSocket support and Git auto-deploy.
- `recommendationReason`: Explain why (e.g., "Render Starter at $7/mo provides 512MB RAM with 45% headroom for 2x growth, native WebSocket support, and zero-config Git deployment")

Railway cost calculation requires runtime estimates — use avgCPU% converted to fractional vCPU and peakRAM converted to GB. Assume server runs 730 hours/month (always-on).

Sort final options by monthlyCost ascending.
  </action>
  <verify>Run `npx tsc --noEmit tests/profiling/cost-analyzer.ts` — no TypeScript errors. Verify compareHosting returns CostAnalysis with all 6 platform options populated.</verify>
  <done>Cost analyzer compares 6 hosting tiers across 5 platforms, calculates monthly costs with bandwidth, evaluates 2x growth headroom, and produces a ranked recommendation within $5-20/mo budget.</done>
</task>

<task type="auto">
  <name>Task 2: Create report generator and CLI entrypoint for hosting analysis markdown</name>
  <files>tests/profiling/report-generator.ts, tests/profiling/generate-report.ts</files>
  <action>
Create two files:

**File 1: `tests/profiling/report-generator.ts`** — Pure function that generates markdown from profiling + cost data.

Takes ProfileReport (from run-profile.ts) and CostAnalysis (from cost-analyzer.ts) and produces a complete markdown document.

**Report structure:**

```markdown
# ScrumQuest Hosting Analysis Report

**Generated:** {YYYY-MM-DD}
**Test Configuration:** {users} concurrent WebSocket users, {duration}s steady state duration

## Executive Summary

**Measured Resource Usage (Steady State):**
| Metric | Value |
|--------|-------|
| Peak RSS Memory | {X} MB |
| Peak Heap Memory | {X} MB |
| Avg CPU Usage | {X}% |
| Peak CPU Usage | {X}% |
| Event Loop Utilization | {X}% |
| Estimated Bandwidth | {X} GB/month |
| WebSocket Connections | {X} |

**Recommendation:** {platform} ({tier}) at ${cost}/month
{reason}

## Resource Profile by Scenario

### Cold Start (0 to {users} users in 10s)
| Metric | Value |
|--------|-------|
| Peak RSS | {X} MB |
| Peak CPU | {X}% |
| Event Loop Util | {X}% |

### Steady State ({users} users for {duration}s)
| Metric | Value |
|--------|-------|
| Peak RSS | {X} MB |
| Avg CPU | {X}% |
| Event Loop Util | {X}% |
| Bandwidth | {X} MB |

### Teardown (connections draining)
| Metric | Value |
|--------|-------|
| Peak RSS | {X} MB |
| Avg CPU | {X}% |

## Cost Comparison

| Platform | Tier | Monthly Cost | RAM | CPU | Meets Needs | 2x Headroom | WebSocket | Auto-Deploy |
|----------|------|--------------|-----|-----|-------------|-------------|-----------|-------------|
{for each option, sorted by cost}

**Budget range:** ${min}-${max}/month

### Within Budget Options
{filtered list with details}

## Performance Bottlenecks

{for each bottleneck:}
### {type} — Severity: {severity}
{description}

**Mitigation:** {actionable step}

{if no bottlenecks: "No critical performance bottlenecks detected at {users} concurrent users."}

## Recommendation Details

### Why {platform} {tier}?
{detailed reasoning covering cost, headroom, WebSocket support, deployment simplicity}

### Migration Path
{platform-specific deployment steps, e.g., for Render: "1. Connect Git repo, 2. Set build command: npm run build, 3. Set start command: npm run start, 4. Add DATABASE_URL env var"}

### Database Recommendation
Pair with Neon PostgreSQL free tier (100 CU-hours/month, 0.5GB storage). Fallback: Render PostgreSQL at $7/mo if Neon limits are exceeded.

### Cost Projection
| Scenario | Monthly Cost | Annual Cost |
|----------|-------------|-------------|
| Current (Replit Core) | $25 | $300 |
| Recommended | ${X} | ${X*12} |
| Annual Savings | - | ${savings} |
```

**Bottleneck mitigation text map:**
- memory high: "Consider upgrading to the next tier, or investigate memory-heavy lobby state. Run `node --max-old-space-size=384` to cap heap and force GC."
- cpu high: "Profile with `0x` or `clinic flame` to identify hot paths. Common culprits: JSON serialization of large lobby state, combat calculations."
- event-loop: "Investigate synchronous operations in hot paths. Move heavy computation to setImmediate() or worker threads."
- event-loop-blocking: "Critical — blocking operations detected. Use `blocked-at` package to identify exact call sites. Likely candidates: JSON.stringify on large objects, synchronous file I/O."
- cold-start: "Connection storm protection needed. Consider staggered reconnection with exponential backoff in Socket.IO client config."

**File 2: `tests/profiling/generate-report.ts`** — CLI script that ties everything together.

**Flow:**
1. Parse CLI args: `--profile` (path to profile JSON, default: latest in tests/reports/), `--budget-min` (default 5), `--budget-max` (default 20)
2. Read profile JSON file (the output of run-profile.ts)
3. Extract resource requirements from profile data
4. Call `compareHosting()` with requirements
5. Call `generateReport()` with profile + cost analysis
6. Write markdown to `tests/reports/hosting-analysis-YYYY-MM-DD.md`
7. Print path to generated report and key recommendation to stdout

**Finding latest profile:** `fs.readdirSync('tests/reports').filter(f => f.startsWith('profile-')).sort().pop()`

Add npm script to package.json:
```json
"profile": "tsx tests/profiling/run-profile.ts",
"profile:report": "tsx tests/profiling/generate-report.ts"
```

Script should be runnable via: `npx tsx tests/profiling/generate-report.ts` or `npm run profile:report`
  </action>
  <verify>
1. `npx tsc --noEmit tests/profiling/report-generator.ts tests/profiling/generate-report.ts` — no TypeScript errors
2. Verify generate-report.ts imports from both cost-analyzer.ts and report-generator.ts
3. Verify report-generator.ts produces valid markdown with all required sections
  </verify>
  <done>Report generator produces complete markdown hosting analysis with executive summary, 5-platform cost comparison table, bottleneck analysis with mitigation steps, migration path, and database recommendation. CLI script reads profiling JSON and generates the final report. npm scripts added for easy invocation.</done>
</task>

</tasks>

<verification>
1. `npm run check` passes with no new TypeScript errors
2. All three files exist in tests/profiling/ with correct imports
3. Cost analyzer covers all 5 platforms (Render, Railway, Fly.io, Replit, AWS Lightsail)
4. Report includes: executive summary, cost comparison table, bottleneck analysis, recommendation, migration path
5. generate-report.ts is runnable as a standalone CLI script
6. package.json has `profile` and `profile:report` npm scripts
</verification>

<success_criteria>
- Operator can run `npm run profile:report` to generate hosting analysis from profiling data
- Cost comparison covers 5 platforms with 6 tier options, all normalized to monthly cost
- Recommendation identifies cheapest option within $5-20/mo budget with 2x growth headroom
- Bottleneck analysis provides specific mitigation steps (not generic advice)
- Report includes migration path with step-by-step deployment instructions for recommended platform
- Annual cost savings vs current Replit hosting calculated and displayed
</success_criteria>

<output>
After completion, create `.planning/phases/29-hosting-analysis/29-02-SUMMARY.md`
</output>
