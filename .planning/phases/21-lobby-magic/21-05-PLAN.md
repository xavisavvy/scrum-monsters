---
phase: 21-lobby-magic
plan: 05
type: execute
wave: 3
depends_on: ["21-01", "21-02", "21-03", "21-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "npm audit shows no high-severity vulnerabilities"
    - "npm run build succeeds cleanly"
    - "npm test passes all existing tests"
    - "No Math.random() remains in security-sensitive server code"
    - "All Express API routes have rate limiting"
    - "CSRF protection is active on all state-changing endpoints"
    - "All GitHub Actions workflows have top-level permissions"
  artifacts: []
  key_links: []
---

<objective>
Run a comprehensive security verification sweep across the entire codebase to confirm all CodeQL alert categories are resolved: rate limiting, CSRF protection, secure randomness, URL sanitization, and GitHub Actions permissions. Run npm audit and verify no high-severity vulnerabilities remain.

Purpose: Validate that all security hardening from Plans 01-04 is correctly implemented and nothing was missed. This is the final gate before marking the phase complete.

Output: Verification results confirming all 7 success criteria from the phase roadmap are met.
</objective>

<execution_context>
@C:/Users/Preston/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Preston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-lobby-magic/21-01-SUMMARY.md
@.planning/phases/21-lobby-magic/21-02-SUMMARY.md
@.planning/phases/21-lobby-magic/21-03-SUMMARY.md
@.planning/phases/21-lobby-magic/21-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run comprehensive security audit</name>
  <files></files>
  <action>
Run the following verification checks in sequence. Document results for each.

**1. npm audit check:**
```bash
npm audit --audit-level=high
```
Expected: 0 high-severity vulnerabilities. If any remain, document them.

**2. Build verification:**
```bash
npm run build
```
Expected: Clean build, no errors.

**3. Test suite:**
```bash
npm test
```
Expected: All existing tests pass. No regressions from security changes.

**4. TypeScript check:**
```bash
npm run check
```
Expected: No type errors.

**5. Math.random() audit in server security code:**
```bash
grep -rn "Math.random()" server/gameState.ts server/domains/SessionManager.ts server/websocket.ts server/logger.ts server/routes.ts server/index.ts server/auth/
```
Expected: ZERO results in these files (all security-sensitive code). Math.random() should ONLY remain in:
- server/domains/CombatManager.ts (gameplay)
- server/domains/boss-ai/*.ts (gameplay)
- gameState.ts lines for player positions and boss selection (gameplay)
- SessionManager.ts lines for player positions (gameplay)

**6. Rate limiting verification:**
```bash
grep -rn "authLimiter\|profileLimiter\|apiLimiter" server/
```
Expected: Imports in routes.ts, definitions in middleware/rateLimiter.ts, app.use() calls in routes.ts.

**7. CSRF verification:**
```bash
grep -rn "csrfSynchronisedProtection\|csrf-token\|x-csrf-token" server/ client/src/
```
Expected: middleware/csrf.ts has csrfSynchronisedProtection, routes.ts applies it, client has token fetch utility and header injection.

**8. Open redirect verification:**
```bash
grep -n "join/:lobbyId" server/routes.ts
```
Expected: Handler includes regex validation `/^[A-Z0-9]{6}$/i.test(lobbyId)`.

**9. Trust proxy verification:**
```bash
grep -n "trust proxy" server/index.ts
```
Expected: `app.set('trust proxy', 1)` present.

**10. GitHub Actions permissions verification:**
```bash
grep -c "^permissions:" .github/workflows/*.yml
```
Expected: 18 files, each with count >= 1.

If any check fails, document the failure and create a fix. Do NOT mark the plan complete until all 10 checks pass.
  </action>
  <verify>
All 10 checks listed above pass. Document results in the summary.
  </verify>
  <done>All 10 security verification checks pass: npm audit clean, build succeeds, tests pass, types check, no insecure randomness, rate limiting active, CSRF active, open redirect fixed, trust proxy set, all workflows have permissions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual security verification</name>
  <what-built>Complete production security hardening: rate limiting (3 tiers), CSRF protection (Synchronizer Token Pattern), secure randomness (crypto.randomBytes for all IDs), open redirect fix (/join/:lobbyId validation), and GitHub Actions permissions lockdown (all 18 workflows).</what-built>
  <how-to-verify>
1. Start the dev server: `npm run dev`
2. Open http://localhost:5001 (or 5000)
3. **Test rate limiting:**
   - Make 11 rapid POST requests to /api/auth/login (use browser console or curl). The 11th should return 429 status.
   - `curl -X POST http://localhost:5001/api/auth/login -H "Content-Type: application/json" -d '{"email":"test@test.com","password":"wrong"}' -w "\n%{http_code}\n"` (repeat 11 times)
4. **Test CSRF:**
   - Try a POST to /api/auth/login without x-csrf-token header — should get 403
   - `curl -X POST http://localhost:5001/api/auth/login -H "Content-Type: application/json" -d '{"email":"test@test.com","password":"wrong"}' -w "\n%{http_code}\n"` — expect 403
   - Verify the UI still works (login/register forms should function because client sends CSRF token)
5. **Test open redirect fix:**
   - Navigate to http://localhost:5001/join/ABCD12 — should redirect to /?join=ABCD12
   - Navigate to http://localhost:5001/join/../../etc — should redirect to /?error=invalid-invite
   - Navigate to http://localhost:5001/join/javascript:alert(1) — should redirect to /?error=invalid-invite
6. **Verify npm audit:** Run `npm audit --audit-level=high` in terminal — should show 0 high-severity vulnerabilities
  </how-to-verify>
  <resume-signal>Type "approved" if all security checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All 7 phase success criteria from ROADMAP.md verified:
1. All Express routes have rate limiting (auth, profile, API endpoints)
2. CSRF protection middleware is active on all state-changing endpoints
3. Lobby code generation uses cryptographically secure randomness (crypto.randomBytes)
4. WebSocket URL construction is properly sanitized against open redirect
5. GitHub Actions workflows have explicit, least-privilege permissions blocks
6. All CodeQL alerts resolved (verified by category coverage)
7. npm audit shows no high-severity vulnerabilities
</verification>

<success_criteria>
- All 10 automated verification checks pass
- Human verification confirms rate limiting, CSRF, and open redirect work correctly in the running application
- npm audit is clean
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/21-lobby-magic/21-05-SUMMARY.md`
</output>
