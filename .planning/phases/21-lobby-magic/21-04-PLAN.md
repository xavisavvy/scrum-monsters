---
phase: 21-lobby-magic
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/rollback.yml
autonomous: true

must_haves:
  truths:
    - "rollback.yml has a top-level permissions block"
    - "Top-level permissions default to contents: read (least privilege)"
    - "Job-level permissions override for jobs that need write access"
    - "All 18 GitHub Actions workflows have top-level permissions blocks"
  artifacts:
    - path: ".github/workflows/rollback.yml"
      provides: "Top-level permissions block"
      contains: "permissions:"
  key_links:
    - from: ".github/workflows/rollback.yml"
      to: "GitHub Actions security model"
      via: "permissions block restricts GITHUB_TOKEN scope"
      pattern: "^permissions:"
---

<objective>
Add top-level permissions block to rollback.yml — the only GitHub Actions workflow missing one. This satisfies the CodeQL `actions/missing-workflow-permissions` alert.

Purpose: Close CodeQL GitHub Actions permissions alert. Without explicit permissions, the GITHUB_TOKEN has broad default permissions. Adding `permissions: contents: read` at the top level restricts the default scope, with job-level overrides only where needed.

Output: rollback.yml with least-privilege permissions configuration.
</objective>

<execution_context>
@C:/Users/Preston/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Preston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-lobby-magic/21-RESEARCH.md
@.github/workflows/rollback.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add top-level permissions block to rollback.yml</name>
  <files>.github/workflows/rollback.yml</files>
  <action>
Read `.github/workflows/rollback.yml` to understand the full structure.

Add a top-level `permissions:` block at the workflow level. Per the research:
- rollback.yml has job-level permissions at line 307, but NO top-level permissions
- The CodeQL alert `actions/missing-workflow-permissions` requires a TOP-LEVEL `permissions:` key

Add the following AFTER the `concurrency:` block (or after the `on:` trigger block if no concurrency block exists) and BEFORE the `jobs:` key:

```yaml
permissions:
  contents: read   # Default: least privilege
```

Verify the existing job-level permissions block at line 307 is preserved. It should override the top-level default for jobs that need write access (e.g., `contents: write` for git push during rollback).

Also do a quick scan of ALL other workflows to confirm they already have top-level permissions. From the grep results, all 17 other workflows already have `permissions:` at the top level:
- ci.yml, e2e.yml, deploy.yml, docker.yml, release.yml, codeql.yml, security-scan.yml
- label.yml, stale.yml, cleanup.yml, pr-checks.yml, dependabot-auto-merge.yml
- accessibility.yml, api-contracts.yml, auto-rollback-monitor.yml, load-tests.yml, visual-regression.yml

No changes needed to other workflows.
  </action>
  <verify>
- `grep -n "^permissions:" .github/workflows/rollback.yml` shows a top-level match
- `grep -c "^permissions:" .github/workflows/*.yml` shows 18 files (all workflows have it)
- The YAML is valid: `cat .github/workflows/rollback.yml | head -40` shows permissions block in correct position
- Job-level permissions still exist for the rollback job
  </verify>
  <done>rollback.yml has top-level `permissions: contents: read` with job-level overrides preserved. All 18 GitHub Actions workflows now have explicit top-level permissions blocks.</done>
</task>

</tasks>

<verification>
1. `grep -c "^permissions:" .github/workflows/*.yml` returns 18 (every workflow has top-level permissions)
2. rollback.yml has `permissions: contents: read` at top level
3. rollback.yml job-level permissions still override for git push and PR operations
4. YAML syntax is valid (no parsing errors)
</verification>

<success_criteria>
- All 18 GitHub Actions workflows have explicit top-level permissions blocks
- rollback.yml defaults to contents: read with job-level overrides where needed
- CodeQL `actions/missing-workflow-permissions` alert resolved
</success_criteria>

<output>
After completion, create `.planning/phases/21-lobby-magic/21-04-SUMMARY.md`
</output>
