---
phase: 35-observability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/grafana/provisioning/datasources/prometheus.yml
  - docker/grafana/provisioning/dashboards/dashboard.yml
  - docker/grafana/dashboards/scrumquest.json
autonomous: true

must_haves:
  truths:
    - "Grafana auto-loads Prometheus as default datasource on container start"
    - "Grafana auto-loads ScrumQuest dashboard with game metrics panels"
    - "Dashboard shows active lobbies, player count, WebSocket connections, and error rates"
  artifacts:
    - path: "docker/grafana/provisioning/datasources/prometheus.yml"
      provides: "Auto-configured Prometheus datasource"
      contains: "url: http://prometheus:9090"
    - path: "docker/grafana/provisioning/dashboards/dashboard.yml"
      provides: "Dashboard provider pointing to /var/lib/grafana/dashboards"
      contains: "path: /var/lib/grafana/dashboards"
    - path: "docker/grafana/dashboards/scrumquest.json"
      provides: "Pre-built Grafana dashboard with game metric panels"
      min_lines: 50
  key_links:
    - from: "docker/grafana/provisioning/datasources/prometheus.yml"
      to: "prometheus service in docker-compose"
      via: "Docker network DNS"
      pattern: "http://prometheus:9090"
    - from: "docker/grafana/dashboards/scrumquest.json"
      to: "server/metrics.ts metric names"
      via: "PromQL queries referencing metric names"
      pattern: "scrumquest_active_lobbies|scrumquest_active_players|scrumquest_websocket_connections|http_requests_total"
---

<objective>
Create Grafana provisioning files and a pre-built dashboard JSON for ScrumQuest game metrics.

Purpose: Grafana needs provisioning files to auto-configure on first start -- without them, the datasource and dashboard must be manually recreated every time the container is recreated. The dashboard visualizes the four required metrics (OBS-02).
Output: Grafana provisioning YAML files and a dashboard JSON with panels for active lobbies, players, WebSocket connections, error rates, plus Node.js health metrics.
</objective>

<execution_context>
@C:/Users/Preston/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Preston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-observability/35-RESEARCH.md
@server/metrics.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Grafana provisioning configuration files</name>
  <files>docker/grafana/provisioning/datasources/prometheus.yml, docker/grafana/provisioning/dashboards/dashboard.yml</files>
  <action>
1. Create directory structure: `docker/grafana/provisioning/datasources/` and `docker/grafana/provisioning/dashboards/`

2. Create `docker/grafana/provisioning/datasources/prometheus.yml`:
   ```yaml
   apiVersion: 1
   datasources:
     - name: Prometheus
       type: prometheus
       access: proxy
       url: http://prometheus:9090
       isDefault: true
       editable: false
   ```

3. Create `docker/grafana/provisioning/dashboards/dashboard.yml`:
   ```yaml
   apiVersion: 1
   providers:
     - name: 'default'
       orgId: 1
       folder: ''
       type: file
       disableDeletion: false
       updateIntervalSeconds: 30
       options:
         path: /var/lib/grafana/dashboards
         foldersFromFilesStructure: false
   ```
  </action>
  <verify>Confirm both files exist and contain valid YAML. Check `prometheus.yml` references `http://prometheus:9090`. Check `dashboard.yml` points to `/var/lib/grafana/dashboards`.</verify>
  <done>Grafana provisioning files exist: datasource points to Prometheus at Docker internal URL, dashboard provider points to mounted dashboards directory.</done>
</task>

<task type="auto">
  <name>Task 2: Create ScrumQuest Grafana dashboard JSON</name>
  <files>docker/grafana/dashboards/scrumquest.json</files>
  <action>
Create `docker/grafana/dashboards/scrumquest.json` — a Grafana dashboard JSON with the following panels. Use Grafana dashboard JSON schema (schemaVersion 39+, datasource uid "prometheus").

**Row 1 — Game Status (stat panels, single row):**
- Panel 1: "Active Lobbies" — stat panel, query: `scrumquest_active_lobbies`
- Panel 2: "Connected Players" — stat panel, query: `scrumquest_active_players`
- Panel 3: "WebSocket Connections" — stat panel, query: `scrumquest_websocket_connections`
- Panel 4: "Games Completed (1h)" — stat panel, query: `increase(scrumquest_games_completed_total[1h])`

**Row 2 — HTTP Metrics (time series panels):**
- Panel 5: "HTTP Error Rate (5xx)" — time series, query: `rate(http_requests_total{status_code=~"5.."}[5m])`, red line
- Panel 6: "HTTP Request Duration (p95)" — time series, query: `histogram_quantile(0.95, rate(http_request_duration_seconds_bucket[5m]))`, unit: seconds
- Panel 7: "HTTP Requests/sec" — time series, query: `rate(http_requests_total[5m])`, stacked by status_code

**Row 3 — Node.js Health (time series panels):**
- Panel 8: "Heap Memory Used" — time series, query: `scrumquest_nodejs_heap_used_bytes`, unit: bytes
- Panel 9: "Event Loop Lag" — time series, query: `scrumquest_nodejs_eventloop_lag_seconds`, unit: seconds
- Panel 10: "Active Handles" — time series, query: `scrumquest_nodejs_active_handles_total`

**Dashboard settings:**
- Title: "ScrumQuest"
- Refresh interval: 30s
- Time range: Last 6 hours
- Timezone: browser
- Tags: ["scrumquest", "production"]
- Templating: none needed (single app instance)

**Important details for the JSON:**
- Set `"uid": "scrumquest-main"` so the dashboard has a stable ID
- Use `"datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"}` pattern OR use `"datasource": "Prometheus"` (name-based reference matching provisioned datasource name)
- Each panel needs: `gridPos` (x, y, w, h), `type`, `title`, `targets` with `expr`
- Grid is 24 columns wide. Row 1: 4 panels at w=6 each. Rows 2-3: 3 panels at w=8 each.
- Set `id` fields sequentially (1, 2, 3...)
  </action>
  <verify>Validate JSON syntax: `node -e "JSON.parse(require('fs').readFileSync('docker/grafana/dashboards/scrumquest.json','utf8')); console.log('Valid JSON')"`. Verify panel count is 10. Verify all four required metrics appear: active_lobbies, active_players, websocket_connections, and http_requests_total (for error rate).</verify>
  <done>scrumquest.json is valid Grafana dashboard JSON with 10 panels covering all OBS-02 required metrics (active lobbies, player count, WebSocket connections, error rates) plus HTTP performance and Node.js health panels. Dashboard auto-loads via provisioning on Grafana container start.</done>
</task>

</tasks>

<verification>
- All three provisioning files exist in correct directory structure
- JSON is valid (parseable by Node.js)
- Dashboard contains panels for all four OBS-02 metrics
- Datasource references Prometheus at http://prometheus:9090
- Dashboard provider points to /var/lib/grafana/dashboards
</verification>

<success_criteria>
Grafana provisioning auto-configures Prometheus datasource and loads the ScrumQuest dashboard on container start. Dashboard has panels for active lobbies, player count, WebSocket connections, HTTP error rate, request duration, Node.js memory, and event loop lag.
</success_criteria>

<output>
After completion, create `.planning/phases/35-observability/35-02-SUMMARY.md`
</output>
