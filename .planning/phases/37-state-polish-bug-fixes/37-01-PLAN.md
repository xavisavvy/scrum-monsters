---
phase: 37-state-polish-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/ui/EmptyState.tsx
  - client/src/components/ui/LoadingSkeleton.tsx
  - client/src/components/game/Lobby.tsx
  - client/src/components/game/AbilityBar.tsx
  - client/src/components/game/TeamScoreboard.tsx
  - client/src/components/game/phases/BattlePhase.tsx
autonomous: true

must_haves:
  truths:
    - "User sees themed empty state message when lobby has no players"
    - "User sees themed empty state when no abilities are unlocked in battle"
    - "User sees themed empty state when scoreboard has no team competition data"
    - "User sees skeleton loading placeholders for lobby player list while connecting"
    - "User sees themed spinner during battle preparation instead of raw text"
  artifacts:
    - path: "client/src/components/ui/EmptyState.tsx"
      provides: "Reusable JRPG-themed empty state component"
      exports: ["EmptyState"]
    - path: "client/src/components/ui/LoadingSkeleton.tsx"
      provides: "Player list skeleton and battle loading spinner"
      exports: ["PlayerListSkeleton", "BattleLoadingSpinner"]
  key_links:
    - from: "client/src/components/game/Lobby.tsx"
      to: "client/src/components/ui/EmptyState.tsx"
      via: "import and render when players array is empty"
      pattern: "EmptyState"
    - from: "client/src/components/game/phases/BattlePhase.tsx"
      to: "client/src/components/ui/LoadingSkeleton.tsx"
      via: "import BattleLoadingSpinner for preparation state"
      pattern: "BattleLoadingSpinner"
---

<objective>
Add JRPG-themed empty state messages and loading skeletons/spinners to all screens that currently show blank content or raw unstyled text.

Purpose: Users should never see blank screens, raw "Loading..." text, or `null` renders. Every transitional and empty state gets JRPG theming consistent with the rest of the app.
Output: Two reusable UI components (EmptyState, LoadingSkeleton) and integration into Lobby, AbilityBar, TeamScoreboard, and BattlePhase.
</objective>

<execution_context>
@C:/Users/Preston/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Preston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-state-polish-bug-fixes/37-RESEARCH.md
@client/src/components/ui/skeleton.tsx
@client/src/components/ui/retro-card.tsx
@client/src/components/ui/retro-button.tsx
@client/src/components/game/Lobby.tsx
@client/src/components/game/AbilityBar.tsx
@client/src/components/game/TeamScoreboard.tsx
@client/src/components/game/phases/BattlePhase.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable EmptyState and LoadingSkeleton components</name>
  <files>client/src/components/ui/EmptyState.tsx, client/src/components/ui/LoadingSkeleton.tsx</files>
  <action>
Create `EmptyState.tsx` — a reusable JRPG-themed empty state component:
- Props: `icon` (string emoji), `title` (string), `message` (string), optional `action` callback, optional `actionLabel` string
- Uses `RetroCard` as container, centered layout with `text-center py-8 space-y-3`
- Title gets `retro-text-glow-light` class, message gets `text-sm text-gray-400`
- CTA button uses `RetroButton variant="primary"`, only rendered when `action` and `actionLabel` are provided
- Follow the pattern from research (Lobby.tsx line 2095 has an existing empty ticket state for reference)

Create `LoadingSkeleton.tsx` — two loading components:
1. `PlayerListSkeleton({ count = 3 })` — renders `count` skeleton rows, each with a circular avatar Skeleton (h-10 w-10 rounded-full) and two text Skeletons (h-4 w-24 and h-3 w-16), wrapped in `div.space-y-3`. Each row uses `bg-gray-800/50 rounded p-3 flex items-center gap-3`. Uses existing `Skeleton` from `@/components/ui/skeleton`.
2. `BattleLoadingSpinner()` — a full-height centered JRPG-themed loading indicator. Use `RetroCard` with a sword emoji, "Preparing for Battle..." title with `retro-text-glow` class, and a framer-motion `animate={{ rotate: 360 }}` spinning sword or shield icon. Add a `text-sm text-gray-400` subtitle: "Summoning the boss..."

Both components exported as named exports.
  </action>
  <verify>Run `npm run check` to confirm no TypeScript errors in the new files. Visually inspect that imports resolve correctly.</verify>
  <done>EmptyState.tsx exports EmptyState component with icon/title/message/action props. LoadingSkeleton.tsx exports PlayerListSkeleton and BattleLoadingSpinner components. Both use RetroCard/RetroButton/Skeleton from existing UI library.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate empty states and loading skeletons into game components</name>
  <files>client/src/components/game/Lobby.tsx, client/src/components/game/AbilityBar.tsx, client/src/components/game/TeamScoreboard.tsx, client/src/components/game/phases/BattlePhase.tsx</files>
  <action>
**Lobby.tsx** — Add empty state for player list:
- Find where the player list renders (look for the players section in the lobby UI)
- When `currentLobby.players.length === 0`, render `<EmptyState icon="👥" title="No Adventurers Yet" message="Share the lobby code to invite your party!" />` (no action CTA needed — the invite button is already elsewhere on the page)
- When `currentLobby === null` (still connecting/loading), render `<PlayerListSkeleton count={3} />` in the player list area
- Do NOT touch the existing ticket empty state (line ~2095) — it already has one

**AbilityBar.tsx** — Replace silent `return null` with themed empty state:
- When `!currentPlayer?.avatar`, keep returning null (not in battle, nothing to show)
- When `!config` (no ability config for class), render a small inline `EmptyState` with `icon="🔮" title="No Abilities" message="This class has no special abilities yet"` instead of returning null
- Keep the existing `return null` for non-battle phases unchanged

**TeamScoreboard.tsx** — Replace `return null` with themed empty state:
- When `!currentLobby?.teamCompetition`, render `<EmptyState icon="⚔️" title="No Team Battle" message="Team competition activates with enough players" />` instead of returning null

**BattlePhase.tsx** — Replace raw "Preparing Battle..." text:
- Find the fallback UI (lines ~44-50) that shows "Preparing Battle..." and "Loading boss data"
- Replace with `<BattleLoadingSpinner />` from LoadingSkeleton
- Keep the same conditional (when boss data is not yet available)
  </action>
  <verify>Run `npm run check` for TypeScript validation. Run `npm run build` to confirm no build errors. Verify that none of the modified components have bare `return null` for user-visible states (grep for `return null` in modified files — only acceptable for non-battle phase checks in AbilityBar).</verify>
  <done>Lobby shows PlayerListSkeleton when loading and EmptyState when no players. AbilityBar shows themed "No Abilities" instead of null when config missing. TeamScoreboard shows themed empty state instead of null. BattlePhase shows BattleLoadingSpinner instead of raw text. No blank screens or unstyled loading text remain in these components.</done>
</task>

</tasks>

<verification>
- `npm run check` passes with no errors
- `npm run build` succeeds
- Grep `return null` in AbilityBar.tsx — only the `gamePhase !== 'battle'` and `!currentPlayer?.avatar` guards remain (not the missing config case)
- Grep `return null` in TeamScoreboard.tsx — the `!teamCompetition` case no longer returns null
- Grep `"Preparing Battle"` in BattlePhase.tsx — raw text replaced with BattleLoadingSpinner
- Grep `"Loading"` in modified files — no unstyled "Loading..." text
</verification>

<success_criteria>
1. EmptyState and LoadingSkeleton components exist and are reusable
2. Every previously-blank or null-returning state in Lobby, AbilityBar, TeamScoreboard, and BattlePhase now shows JRPG-themed UI
3. All themed states use RetroCard/RetroButton from existing design system
4. TypeScript compiles and build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/37-state-polish-bug-fixes/37-01-SUMMARY.md`
</output>
