---
phase: 15-xp-progression-foundation
plan: 03
type: execute
wave: 2
depends_on: ["15-02"]
files_modified:
  - client/src/components/game/XPBar.tsx
  - client/src/components/game/XPBar.css
  - client/src/components/game/index.ts
autonomous: true

must_haves:
  truths:
    - "Player sees their current level"
    - "Player sees XP bar with progress to next level"
    - "XP bar pulses when XP is gained"
    - "Hovering XP bar shows exact numbers"
  artifacts:
    - path: "client/src/components/game/XPBar.tsx"
      provides: "XP bar UI component"
      exports: ["XPBar"]
    - path: "client/src/components/game/XPBar.css"
      provides: "XP bar styling with JRPG gradient"
  key_links:
    - from: "client/src/components/game/XPBar.tsx"
      to: "client/src/lib/stores/useProgression.tsx"
      via: "useProgression hook"
      pattern: "useProgression"
---

<objective>
Create the XP bar UI component with JRPG-style gradient and progressive disclosure.

Purpose: Give players persistent visual feedback of their progression status.
Output: XPBar component showing level and progress, ready to integrate into game layout.
</objective>

<execution_context>
@C:\Users\Preston\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Preston\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-xp-progression-foundation/15-CONTEXT.md
@.planning/phases/15-xp-progression-foundation/15-RESEARCH.md

@client/src/lib/stores/useProgression.tsx
@client/src/components/game/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create XPBar component</name>
  <files>
    client/src/components/game/XPBar.tsx
    client/src/components/game/XPBar.css
  </files>
  <action>
    Create client/src/components/game/XPBar.tsx:

    ```typescript
    import { useState, useEffect, useRef } from 'react';
    import { useProgression } from '@/lib/stores/useProgression';
    import './XPBar.css';

    export function XPBar() {
      const { currentXP, currentLevel, pendingXPGains, getProgressToNextLevel } = useProgression();
      const [isExpanded, setIsExpanded] = useState(false);
      const [isPulsing, setIsPulsing] = useState(false);
      const prevXPRef = useRef(currentXP);

      const progress = getProgressToNextLevel();

      // Pulse animation when XP changes
      useEffect(() => {
        if (currentXP > prevXPRef.current) {
          setIsPulsing(true);
          const timer = setTimeout(() => setIsPulsing(false), 600);
          prevXPRef.current = currentXP;
          return () => clearTimeout(timer);
        }
        prevXPRef.current = currentXP;
      }, [currentXP]);

      return (
        <div
          className={`xp-bar-container ${isPulsing ? 'pulsing' : ''}`}
          onMouseEnter={() => setIsExpanded(true)}
          onMouseLeave={() => setIsExpanded(false)}
        >
          <div className="xp-level">
            Lv {currentLevel}
          </div>

          <div className="xp-bar-track">
            <div
              className="xp-bar-fill"
              style={{ width: `${progress.percentage}%` }}
            />
          </div>

          {isExpanded && (
            <div className="xp-details">
              <span className="xp-current">{progress.current}</span>
              <span className="xp-separator">/</span>
              <span className="xp-needed">{progress.needed} XP</span>
            </div>
          )}
        </div>
      );
    }
    ```

    Create client/src/components/game/XPBar.css:

    ```css
    .xp-bar-container {
      display: flex;
      align-items: center;
      gap: 8px;
      padding: 8px 12px;
      background: rgba(0, 0, 0, 0.7);
      border-radius: 8px;
      border: 2px solid #4a3f2f;
      transition: all 0.3s ease;
      min-width: 150px;
    }

    .xp-bar-container:hover {
      min-width: 220px;
    }

    .xp-bar-container.pulsing {
      animation: xp-pulse 0.6s ease-out;
    }

    @keyframes xp-pulse {
      0% { box-shadow: 0 0 0 0 rgba(255, 215, 0, 0.7); }
      50% { box-shadow: 0 0 20px 5px rgba(255, 215, 0, 0.5); }
      100% { box-shadow: 0 0 0 0 rgba(255, 215, 0, 0); }
    }

    .xp-level {
      font-family: 'Press Start 2P', monospace;
      font-size: 12px;
      color: #ffd700;
      text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.8);
      white-space: nowrap;
    }

    .xp-bar-track {
      flex: 1;
      height: 12px;
      background: linear-gradient(180deg, #2a2a2a 0%, #1a1a1a 100%);
      border-radius: 6px;
      border: 1px solid #4a4a4a;
      overflow: hidden;
      position: relative;
    }

    .xp-bar-fill {
      height: 100%;
      background: linear-gradient(90deg, #b8860b 0%, #ffd700 50%, #ffec8b 100%);
      border-radius: 5px;
      transition: width 0.5s ease-out;
      position: relative;
    }

    /* JRPG beveled edge effect */
    .xp-bar-fill::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 50%;
      background: linear-gradient(180deg, rgba(255, 255, 255, 0.3) 0%, transparent 100%);
      border-radius: 5px 5px 0 0;
    }

    .xp-details {
      display: flex;
      align-items: center;
      gap: 2px;
      font-family: 'Press Start 2P', monospace;
      font-size: 8px;
      color: #ccc;
      white-space: nowrap;
      animation: fade-in 0.2s ease-out;
    }

    @keyframes fade-in {
      from { opacity: 0; transform: translateX(-10px); }
      to { opacity: 1; transform: translateX(0); }
    }

    .xp-current {
      color: #ffd700;
    }

    .xp-separator {
      color: #666;
    }

    .xp-needed {
      color: #999;
    }
    ```

    Component features per CONTEXT.md decisions:
    - Minimal by default (Lv + bar)
    - Expand on hover to show exact numbers
    - Classic JRPG gradient style (gold/orange gradient, beveled edges)
    - Pulses/glows briefly when receiving XP
  </action>
  <verify>Component renders without errors, styles applied correctly</verify>
  <done>XPBar component created with JRPG styling and hover expansion</done>
</task>

<task type="auto">
  <name>Task 2: Export XPBar from components index</name>
  <files>client/src/components/game/index.ts</files>
  <action>
    Check if client/src/components/game/index.ts exists. If it does, add:
    ```typescript
    export { XPBar } from './XPBar';
    ```

    If index.ts doesn't exist, create it with the XPBar export and any other existing game components.

    The XPBar will be integrated into the game layout in Plan 06 (integration plan).
  </action>
  <verify>Import { XPBar } from '@/components/game' works</verify>
  <done>XPBar exported from game components index</done>
</task>

<task type="auto">
  <name>Task 3: Create XPBar unit test</name>
  <files>client/src/components/game/XPBar.test.tsx</files>
  <action>
    Create basic unit test for XPBar component:

    ```typescript
    import { describe, it, expect, beforeEach, vi } from 'vitest';
    import { render, screen, fireEvent } from '@testing-library/react';
    import { XPBar } from './XPBar';
    import { useProgression } from '@/lib/stores/useProgression';

    // Mock the store
    vi.mock('@/lib/stores/useProgression', () => ({
      useProgression: vi.fn(),
    }));

    describe('XPBar', () => {
      beforeEach(() => {
        vi.mocked(useProgression).mockReturnValue({
          currentXP: 50,
          currentLevel: 1,
          pendingXPGains: [],
          getProgressToNextLevel: () => ({ current: 50, needed: 100, percentage: 50 }),
        } as any);
      });

      it('displays current level', () => {
        render(<XPBar />);
        expect(screen.getByText('Lv 1')).toBeInTheDocument();
      });

      it('shows XP details on hover', () => {
        render(<XPBar />);
        const container = document.querySelector('.xp-bar-container');
        fireEvent.mouseEnter(container!);
        expect(screen.getByText('50')).toBeInTheDocument();
        expect(screen.getByText('100 XP')).toBeInTheDocument();
      });

      it('hides XP details on mouse leave', () => {
        render(<XPBar />);
        const container = document.querySelector('.xp-bar-container');
        fireEvent.mouseEnter(container!);
        fireEvent.mouseLeave(container!);
        expect(screen.queryByText('100 XP')).not.toBeInTheDocument();
      });

      it('sets fill width based on progress percentage', () => {
        render(<XPBar />);
        const fill = document.querySelector('.xp-bar-fill');
        expect(fill).toHaveStyle({ width: '50%' });
      });
    });
    ```
  </action>
  <verify>npm test -- --run XPBar passes</verify>
  <done>XPBar unit tests verify display and interaction behavior</done>
</task>

</tasks>

<verification>
1. `npm run check` - TypeScript compiles without errors
2. `npm test -- --run XPBar` - Component tests pass
3. Visual inspection: XP bar has gold/orange gradient
4. Visual inspection: Hover shows exact XP numbers
5. Visual inspection: Component pulses when XP changes
</verification>

<success_criteria>
- XPBar displays "Lv {N}" label
- XPBar shows gold/orange gradient fill
- Hovering reveals "{current} / {needed} XP"
- Component pulses animation when currentXP increases
- Component exported from game components index
</success_criteria>

<output>
After completion, create `.planning/phases/15-xp-progression-foundation/15-03-SUMMARY.md`
</output>
