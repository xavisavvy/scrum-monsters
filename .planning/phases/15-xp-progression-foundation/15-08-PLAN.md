---
phase: 15-xp-progression-foundation
plan: 08
type: execute
wave: 2
depends_on: [15-07]
files_modified:
  - shared/gameEvents.ts
  - client/src/components/game/Lobby.tsx
  - server/domains/SessionManager.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Player sees their current level next to their name in the lobby player list"
    - "Level displays as 'Lv N' inline with player name per CONTEXT.md requirement"
    - "Level field is part of Player interface for type-safe usage"
  artifacts:
    - path: "shared/gameEvents.ts"
      provides: "Player interface with level field"
      contains: "level.*number"
    - path: "client/src/components/game/Lobby.tsx"
      provides: "Level display in lobby player list"
      contains: "Lv"
  key_links:
    - from: "client/src/components/game/Lobby.tsx"
      to: "shared/gameEvents.ts Player.level"
      via: "player.level property access"
      pattern: "player\\.level"
    - from: "server/domains/SessionManager.ts"
      to: "shared/gameEvents.ts Player.level"
      via: "Player object creation with level field"
      pattern: "level.*1"
---

<objective>
Close gap XP-06 (level display in lobby) from Phase 15 verification.

Purpose: The CONTEXT.md requirement states "Player levels visible in lobby only (next to player name)" with format "PlayerName [Lv 5]". The Player interface lacks a level field and the lobby UI does not display levels.

Output: Player interface has level field, lobby player list shows level next to each player name.
</objective>

<execution_context>
@C:/Users/Preston/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Preston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-xp-progression-foundation/15-VERIFICATION.md
@.planning/phases/15-xp-progression-foundation/15-07-SUMMARY.md
@shared/gameEvents.ts
@client/src/components/game/Lobby.tsx
@server/domains/SessionManager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add level field to Player interface and update server player creation</name>
  <files>
    shared/gameEvents.ts
    server/domains/SessionManager.ts
  </files>
  <action>
    **Gap XP-06 Part 1: Add level to Player type**

    1. In `shared/gameEvents.ts`, add `level` field to the `Player` interface:
       ```typescript
       export interface Player {
         id: string;
         name: string;
         avatar: AvatarClass;
         avatarClass: AvatarClass;
         team: TeamType;
         isHost: boolean;
         currentScore?: number | "?";
         hasSubmittedScore: boolean;
         level: number;  // Player level from XP progression (default 1)
       }
       ```

    2. In `server/domains/SessionManager.ts`, find all places where Player objects are created (look for object literals matching the Player interface, typically in `createLobby` and `joinLobby` methods). Add `level: 1` as the default value for newly created players.

    3. Search for any other files that create Player objects (e.g., test files, game state files). Add `level: 1` to those as well. Key files to check:
       - `server/gameState.ts` — if it creates Player objects
       - Any test files that create mock Player objects
       - `client/src/lib/stores/useGameState.tsx` — if it constructs Player objects

    4. The level field will be updated by the server when:
       - Player joins and their XP is loaded from DB (Plan 15-07 handles this)
       - Player levels up during gameplay (progression:level_up event)
       The lobby_updated event already broadcasts the full lobby state, so updating the Player object's level will automatically propagate.

    **Important:** Do NOT break existing code. The `level` field must have a default so all existing Player creation paths work. Use `level: 1` everywhere a Player is constructed.
  </action>
  <verify>
    Run `npm run check` — no TypeScript errors from the new required field.
    Run `npx vitest run` — all tests pass (any test creating Player objects must include level: 1).
  </verify>
  <done>
    - Player interface in shared/gameEvents.ts includes `level: number`
    - All Player object creation sites include `level: 1` default
    - No TypeScript errors
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Display player level in lobby player list</name>
  <files>
    client/src/components/game/Lobby.tsx
  </files>
  <action>
    **Gap XP-06 Part 2: Level display in lobby UI**

    Per CONTEXT.md requirement: "Player levels visible in lobby only (next to player name), not during combat. Level format: 'PlayerName [Lv 5]' inline with name in lobby."

    1. In `client/src/components/game/Lobby.tsx`, find the player roster rendering section (around line 1991-2006). The current code renders:
       ```tsx
       <span className="truncate max-w-[80px]">{player.name}</span>
       {player.isHost && <span className="text-yellow-400">*</span>}
       ```

    2. Add level display after the player name, before the host indicator. Use JRPG gold color (matching the XP bar aesthetic decision: gold gradient #b8860b to #ffd700):
       ```tsx
       <span className="truncate max-w-[80px]">{player.name}</span>
       {player.level > 1 && (
         <span className="text-amber-400 opacity-75 text-[10px] font-bold ml-0.5">
           Lv{player.level}
         </span>
       )}
       {player.isHost && <span className="text-yellow-400">*</span>}
       ```

    3. Design details:
       - Only show level badge if level > 1 (level 1 is default, no need to show it — reduces clutter per progressive disclosure decision)
       - Use `text-amber-400` for the JRPG gold aesthetic
       - Use `opacity-75` so it doesn't visually compete with the player name
       - Use `text-[10px]` (smaller than the player name text) for subtlety
       - Use `font-bold` for readability at small size
       - Format: "Lv5" (no space, compact) — matches JRPG convention
       - Add `ml-0.5` for slight spacing from player name

    4. Also check if there are other places in Lobby.tsx where player names are rendered (e.g., tooltip, sidebar). If so, add level display there too with the same pattern.

    5. Widen the `max-w-[80px]` on the player name to `max-w-[70px]` to accommodate the level badge without overflow, OR keep it at 80px and ensure the parent flex container handles overflow. Test which looks better — the level badge is small enough it likely fits.
  </action>
  <verify>
    Run `npm run check` — no TypeScript errors.
    Run `npm run build` — production build succeeds.
    Grep for "Lv" in Lobby.tsx — should find the level display.
    Grep for "player.level" in Lobby.tsx — should find usage.
  </verify>
  <done>
    - Lobby player list shows "Lv N" next to player name for players above level 1
    - Level uses JRPG gold color consistent with XP bar aesthetic
    - Level display is subtle (smaller font, reduced opacity) per progressive disclosure
    - No TypeScript errors, production build succeeds
  </done>
</task>

</tasks>

<verification>
1. `npm run check` passes with no errors
2. `npm run build` produces successful production build
3. `npx vitest run` — full test suite passes
4. Grep confirms Player interface has level field in shared/gameEvents.ts
5. Grep confirms "Lv" display in Lobby.tsx
6. Visual verification: player names in lobby show level badge
</verification>

<success_criteria>
- Player interface includes level: number field
- All Player creation sites default to level: 1
- Lobby player list displays "LvN" in gold next to player names (level > 1 only)
- All existing tests pass, no new TypeScript errors
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/15-xp-progression-foundation/15-08-SUMMARY.md`
</output>
