---
phase: 15-xp-progression-foundation
plan: 05
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - client/src/components/game/LevelUpCelebration.tsx
  - client/src/components/game/LevelUpCelebration.css
  - client/src/components/game/index.ts
  - client/public/sounds/level-up.mp3
autonomous: true

must_haves:
  truths:
    - "Player sees dramatic celebration when leveling up"
    - "Celebration includes full-screen flash effect"
    - "Celebration shows new level number prominently"
    - "Celebration has class-specific visual effects"
    - "Celebration plays triumphant fanfare sound"
  artifacts:
    - path: "client/src/components/game/LevelUpCelebration.tsx"
      provides: "Level-up celebration component"
      exports: ["LevelUpCelebration"]
    - path: "client/src/components/game/LevelUpCelebration.css"
      provides: "Celebration styling and animations"
  key_links:
    - from: "client/src/components/game/LevelUpCelebration.tsx"
      to: "client/src/lib/stores/useProgression.tsx"
      via: "useProgression levelUp state"
      pattern: "levelUp"
---

<objective>
Create the level-up celebration component with dramatic effects and class-specific animations.

Purpose: Make leveling up feel rewarding with an impactful visual celebration.
Output: LevelUpCelebration component with full-screen flash, particles, and fanfare sound.
</objective>

<execution_context>
@C:\Users\Preston\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Preston\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-xp-progression-foundation/15-CONTEXT.md
@.planning/phases/15-xp-progression-foundation/15-RESEARCH.md

@client/src/lib/stores/useProgression.tsx
@shared/gameEvents.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LevelUpCelebration component</name>
  <files>
    client/src/components/game/LevelUpCelebration.tsx
    client/src/components/game/LevelUpCelebration.css
  </files>
  <action>
    Create client/src/components/game/LevelUpCelebration.tsx:

    ```typescript
    import { useEffect, useRef, useCallback, useState } from 'react';
    import { useFrame } from '@react-three/fiber';
    import { Html } from '@react-three/drei';
    import * as THREE from 'three';
    import type { AvatarClass } from '@shared/gameEvents';
    import { useProgression } from '@/lib/stores/useProgression';
    import { useAudio } from '@/lib/stores/useAudio';
    import './LevelUpCelebration.css';

    interface LevelUpCelebrationProps {
      playerClass: AvatarClass;
      onComplete?: () => void;
    }

    // Class-specific particle colors
    const CLASS_EFFECTS: Record<AvatarClass, { color: string; secondary: string }> = {
      paladin: { color: '#FFD700', secondary: '#FFFFFF' },  // Golden light
      cleric: { color: '#FFFFFF', secondary: '#87CEEB' },   // Holy white
      rogue: { color: '#F39C12', secondary: '#D4AF37' },    // Gold coins
      warrior: { color: '#FF4500', secondary: '#8B0000' },  // Fiery red
      ranger: { color: '#228B22', secondary: '#98FB98' },   // Forest green
      wizard: { color: '#4169E1', secondary: '#9370DB' },   // Arcane blue
      sorcerer: { color: '#FF4500', secondary: '#FF6347' }, // Flame orange
      bard: { color: '#9370DB', secondary: '#DDA0DD' },     // Musical purple
      oathbreaker: { color: '#8A2BE2', secondary: '#4B0082' }, // Dark purple
      monk: { color: '#8B4513', secondary: '#DEB887' },     // Earth tones
    };

    export function LevelUpCelebration({ playerClass, onComplete }: LevelUpCelebrationProps) {
      const { levelUp, clearLevelUp } = useProgression();
      const { playSound } = useAudio();
      const [particles, setParticles] = useState<Array<{ id: number; x: number; y: number; angle: number }>>([]);
      const containerRef = useRef<HTMLDivElement>(null);
      const startTime = useRef(Date.now());
      const duration = 2500; // 2.5 seconds per CONTEXT.md

      // Generate particles on mount
      useEffect(() => {
        if (!levelUp?.active) return;

        // Play level-up fanfare
        playSound?.('level-up');

        // Generate burst of particles
        const newParticles = Array.from({ length: 30 }, (_, i) => ({
          id: i,
          x: 50 + (Math.random() - 0.5) * 20,
          y: 50 + (Math.random() - 0.5) * 20,
          angle: (i / 30) * 360,
        }));
        setParticles(newParticles);

        // Cleanup after duration
        const timer = setTimeout(() => {
          clearLevelUp();
          onComplete?.();
        }, duration);

        return () => clearTimeout(timer);
      }, [levelUp?.active, clearLevelUp, onComplete, playSound]);

      // Handle skip (click/keypress)
      const handleSkip = useCallback(() => {
        clearLevelUp();
        onComplete?.();
      }, [clearLevelUp, onComplete]);

      useEffect(() => {
        const handleKeyDown = (e: KeyboardEvent) => {
          if (e.key === 'Escape' || e.key === ' ') {
            handleSkip();
          }
        };
        window.addEventListener('keydown', handleKeyDown);
        return () => window.removeEventListener('keydown', handleKeyDown);
      }, [handleSkip]);

      if (!levelUp?.active) return null;

      const effects = CLASS_EFFECTS[playerClass] || CLASS_EFFECTS.warrior;

      return (
        <Html fullscreen>
          <div
            className="level-up-overlay"
            onClick={handleSkip}
            ref={containerRef}
          >
            {/* Full-screen flash */}
            <div className="level-up-flash" />

            {/* Radial burst effect */}
            <div
              className="level-up-burst"
              style={{
                background: `radial-gradient(circle, ${effects.color}40 0%, transparent 70%)`,
              }}
            />

            {/* Class-specific particles */}
            <div className="level-up-particles">
              {particles.map((p) => (
                <div
                  key={p.id}
                  className="level-up-particle"
                  style={{
                    left: `${p.x}%`,
                    top: `${p.y}%`,
                    backgroundColor: Math.random() > 0.5 ? effects.color : effects.secondary,
                    '--angle': `${p.angle}deg`,
                    '--delay': `${Math.random() * 0.3}s`,
                  } as React.CSSProperties}
                />
              ))}
            </div>

            {/* Level text */}
            <div className="level-up-text" style={{ color: effects.color }}>
              <div className="level-up-label">LEVEL UP!</div>
              <div className="level-up-number">{levelUp.newLevel}</div>
            </div>

            {/* Skip hint */}
            <div className="level-up-skip">Click or press ESC to skip</div>
          </div>
        </Html>
      );
    }
    ```

    Create client/src/components/game/LevelUpCelebration.css:

    ```css
    .level-up-overlay {
      position: fixed;
      inset: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      z-index: 1000;
      cursor: pointer;
      animation: overlay-fade-in 0.3s ease-out;
    }

    @keyframes overlay-fade-in {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    .level-up-flash {
      position: absolute;
      inset: 0;
      background: white;
      animation: flash-pulse 0.5s ease-out forwards;
    }

    @keyframes flash-pulse {
      0% { opacity: 0.9; }
      100% { opacity: 0; }
    }

    .level-up-burst {
      position: absolute;
      inset: -50%;
      animation: burst-expand 1.5s ease-out forwards;
    }

    @keyframes burst-expand {
      0% { transform: scale(0); opacity: 1; }
      100% { transform: scale(2); opacity: 0; }
    }

    .level-up-particles {
      position: absolute;
      inset: 0;
      overflow: hidden;
      pointer-events: none;
    }

    .level-up-particle {
      position: absolute;
      width: 10px;
      height: 10px;
      border-radius: 50%;
      animation: particle-burst 1.5s ease-out forwards;
      animation-delay: var(--delay);
    }

    @keyframes particle-burst {
      0% {
        transform: translate(0, 0) scale(1);
        opacity: 1;
      }
      100% {
        transform: translate(
          calc(cos(var(--angle)) * 200px),
          calc(sin(var(--angle)) * 200px)
        ) scale(0);
        opacity: 0;
      }
    }

    .level-up-text {
      position: relative;
      text-align: center;
      font-family: 'Press Start 2P', monospace;
      text-shadow:
        0 0 20px currentColor,
        0 0 40px currentColor,
        2px 2px 0 #000,
        -2px -2px 0 #000;
      animation: text-entrance 0.5s ease-out;
    }

    @keyframes text-entrance {
      0% {
        transform: scale(0.5);
        opacity: 0;
      }
      60% {
        transform: scale(1.2);
      }
      100% {
        transform: scale(1);
        opacity: 1;
      }
    }

    .level-up-label {
      font-size: 24px;
      letter-spacing: 4px;
      margin-bottom: 20px;
      animation: label-glow 1s ease-in-out infinite alternate;
    }

    @keyframes label-glow {
      from { filter: brightness(1); }
      to { filter: brightness(1.3); }
    }

    .level-up-number {
      font-size: 96px;
      animation: number-pulse 0.5s ease-in-out infinite alternate;
    }

    @keyframes number-pulse {
      from { transform: scale(1); }
      to { transform: scale(1.05); }
    }

    .level-up-skip {
      position: absolute;
      bottom: 40px;
      font-family: 'Press Start 2P', monospace;
      font-size: 10px;
      color: rgba(255, 255, 255, 0.5);
      animation: skip-fade-in 1s ease-out 1s forwards;
      opacity: 0;
    }

    @keyframes skip-fade-in {
      to { opacity: 1; }
    }
    ```

    Features per CONTEXT.md decisions:
    - Dramatic celebration (~2.5 seconds)
    - Full-screen flash effect
    - Class-specific colors
    - Triumphant fanfare sound
    - Can be skipped (ESC or click)
    - Shows "LEVEL UP!" with new level number
  </action>
  <verify>Component compiles without errors</verify>
  <done>LevelUpCelebration with full-screen flash, particles, class colors</done>
</task>

<task type="auto">
  <name>Task 2: Add level-up sound asset</name>
  <files>client/public/sounds/level-up.mp3</files>
  <action>
    Check existing sound infrastructure in useAudio store.

    If sound system exists and supports adding new sounds:
    - Add 'level-up' to the sound mapping
    - Use a placeholder or fetch a royalty-free level-up fanfare

    If sound system needs extension:
    - Add 'level-up' key to sound types
    - Create client/public/sounds/level-up.mp3 (can be placeholder initially)
    - User can replace with preferred sound later

    For now, create an empty/placeholder sound file or note that sound is optional until a proper asset is provided. The component already handles missing playSound gracefully.

    Note: The useAudio hook already exists per codebase inspection. Check its interface and add level-up sound support.
  </action>
  <verify>Sound plays on level-up (or gracefully skips if unavailable)</verify>
  <done>Level-up sound integrated with audio system</done>
</task>

<task type="auto">
  <name>Task 3: Export component and add tests</name>
  <files>
    client/src/components/game/index.ts
    client/src/components/game/LevelUpCelebration.test.tsx
  </files>
  <action>
    Update client/src/components/game/index.ts:
    ```typescript
    export { LevelUpCelebration } from './LevelUpCelebration';
    ```

    Create test file client/src/components/game/LevelUpCelebration.test.tsx:

    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest';
    import { render, screen, fireEvent } from '@testing-library/react';
    import { Canvas } from '@react-three/fiber';
    import { LevelUpCelebration } from './LevelUpCelebration';
    import { useProgression } from '@/lib/stores/useProgression';
    import { useAudio } from '@/lib/stores/useAudio';

    // Mock stores
    vi.mock('@/lib/stores/useProgression', () => ({
      useProgression: vi.fn(),
    }));

    vi.mock('@/lib/stores/useAudio', () => ({
      useAudio: vi.fn(() => ({ playSound: vi.fn() })),
    }));

    // R3F wrapper
    const R3FWrapper = ({ children }: { children: React.ReactNode }) => (
      <Canvas>
        {children}
      </Canvas>
    );

    describe('LevelUpCelebration', () => {
      const mockClearLevelUp = vi.fn();

      beforeEach(() => {
        mockClearLevelUp.mockClear();
      });

      it('does not render when levelUp is null', () => {
        vi.mocked(useProgression).mockReturnValue({
          levelUp: null,
          clearLevelUp: mockClearLevelUp,
        } as any);

        render(
          <R3FWrapper>
            <LevelUpCelebration playerClass="warrior" />
          </R3FWrapper>
        );

        expect(screen.queryByText('LEVEL UP!')).not.toBeInTheDocument();
      });

      it('renders when levelUp is active', () => {
        vi.mocked(useProgression).mockReturnValue({
          levelUp: { active: true, oldLevel: 1, newLevel: 2, triggeredAt: Date.now() },
          clearLevelUp: mockClearLevelUp,
        } as any);

        render(
          <R3FWrapper>
            <LevelUpCelebration playerClass="warrior" />
          </R3FWrapper>
        );

        expect(screen.getByText('LEVEL UP!')).toBeInTheDocument();
        expect(screen.getByText('2')).toBeInTheDocument();
      });

      it('calls clearLevelUp on click', () => {
        vi.mocked(useProgression).mockReturnValue({
          levelUp: { active: true, oldLevel: 1, newLevel: 2, triggeredAt: Date.now() },
          clearLevelUp: mockClearLevelUp,
        } as any);

        render(
          <R3FWrapper>
            <LevelUpCelebration playerClass="warrior" />
          </R3FWrapper>
        );

        fireEvent.click(screen.getByText('LEVEL UP!').closest('.level-up-overlay')!);
        expect(mockClearLevelUp).toHaveBeenCalled();
      });

      it('calls clearLevelUp on ESC key', () => {
        vi.mocked(useProgression).mockReturnValue({
          levelUp: { active: true, oldLevel: 1, newLevel: 2, triggeredAt: Date.now() },
          clearLevelUp: mockClearLevelUp,
        } as any);

        render(
          <R3FWrapper>
            <LevelUpCelebration playerClass="warrior" />
          </R3FWrapper>
        );

        fireEvent.keyDown(window, { key: 'Escape' });
        expect(mockClearLevelUp).toHaveBeenCalled();
      });
    });
    ```
  </action>
  <verify>npm test -- --run LevelUpCelebration passes</verify>
  <done>Component exported and tested</done>
</task>

</tasks>

<verification>
1. `npm run check` - TypeScript compiles without errors
2. `npm test -- --run LevelUpCelebration` - Tests pass
3. Visual inspection: Full-screen flash animates
4. Visual inspection: Level number displays prominently
5. Visual inspection: Class-specific colors applied
6. Functional test: Click or ESC dismisses celebration
</verification>

<success_criteria>
- Celebration shows "LEVEL UP!" text with level number
- Full-screen white flash on celebration start
- Class-specific particle colors
- Skippable via click or ESC key
- Clears automatically after ~2.5 seconds
- Level-up fanfare sound plays (or gracefully skips)
</success_criteria>

<output>
After completion, create `.planning/phases/15-xp-progression-foundation/15-05-SUMMARY.md`
</output>
