---
phase: 47-ability-effects-data-driven-registries
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - server/domains/index.ts
  - server/domains/AbilityEffectHandler.test.ts
autonomous: true
requirements: [EXT-01]
must_haves:
  truths:
    - "The ability:effect_applied handler has buff, shield, and debuff branches"
    - "The buff branch reads payload.buffType (not a hardcoded type) and stores it via addBuff"
    - "The shield branch stores buffType 'shield' via addBuff"
    - "The debuff branch stores the debuff (attack_slow) in a separate activeDebuffs map without crashing"
    - "A single applyHealEffect helper is called by both the item and ability heal branches"
    - "warrior_berserker_rage and bard_inspire raise getDamageMultiplier above 1.0; holy_shield raises getShieldAbsorption above 0"
  artifacts:
    - path: "server/domains/index.ts"
      provides: "buff/shield/debuff handler branches + applyHealEffect helper + activeDebuffs map + widened ActiveBuff.buffType"
      contains: "function applyHealEffect"
    - path: "server/domains/AbilityEffectHandler.test.ts"
      provides: "regression coverage for all 8 abilities applying their effect + heal dedup"
  key_links:
    - from: "server/domains/index.ts ability:effect_applied handler buff branch"
      to: "addBuff"
      via: "addBuff(... buffType: payload.buffType ...)"
      pattern: "addBuff"
    - from: "server/domains/index.ts ability heal branch"
      to: "applyHealEffect"
      via: "shared helper call"
      pattern: "applyHealEffect"
---

<objective>
EXT-01b: Add the missing `buff`/`shield`/`debuff` branches to the `ability:effect_applied` handler in `server/domains/index.ts` (~L420), mirroring the existing `item:effect_applied` handler (~L337) but reading the buff type from the payload instead of hardcoding it. Extract the byte-identical heal loop (present in both the item and ability handlers) into a single `applyHealEffect` helper. Add a separate `activeDebuffs` map for boss-targeted debuffs so the narrowly-typed player buffs are not polluted. After this plan, the 8 silently-dropped abilities apply their effect: warrior/bard damage_boost and paladin shields become functionally complete (consumers exist); ranger/rogue/wizard buffs and the oathbreaker debuff are stored-and-emitted with a TODO marker (no consumer yet, per the deferred-ideas decision).

Purpose: This is the rank-2 deferred live bug — eight abilities emit effects that are silently dropped because the handler has no branch for them. Plan 47-01 supplied the forwarded data; this plan consumes it.
Output: Three new handler branches, one `applyHealEffect` helper, one `activeDebuffs` map, widened `ActiveBuff.buffType`, and a new `AbilityEffectHandler.test.ts` regression suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-ability-effects-data-driven-registries/47-CONTEXT.md
@.planning/phases/47-ability-effects-data-driven-registries/47-RESEARCH.md
@.planning/phases/47-ability-effects-data-driven-registries/47-01-SUMMARY.md

<interfaces>
<!-- Extracted from server/domains/index.ts — executor uses these directly. -->

Existing module-private buff helpers (server/domains/index.ts ~L220-296):
```typescript
interface ActiveBuff {
  playerId: string; lobbyId: string;
  buffType: 'damage_boost' | 'shield';   // ← widen to include BuffType members for storage
  value: number; expiresAt: number; timeoutHandle: NodeJS.Timeout;
}
function addBuff(lobbyId, playerId, buff: Omit<ActiveBuff,'timeoutHandle'>): void
function getDamageMultiplier(lobbyId, playerId): number   // reads buffType 'damage_boost'
function getShieldAbsorption(lobbyId, playerId): number   // reads buffType 'shield'
function reduceShield(lobbyId, playerId, damage): number
function cleanupBuffs(lobbyId): void
```

Existing item handler buff/shield branches to MIRROR (server/domains/index.ts ~L359-377):
```typescript
} else if (payload.effectType === 'buff') {
  addBuff(payload.lobbyId, payload.playerId, {
    playerId: payload.playerId, lobbyId: payload.lobbyId,
    buffType: 'damage_boost',                              // ITEM hardcodes — correct for items
    value: payload.value,
    expiresAt: Date.now() + (payload.durationMs ?? 10000),
  });
} else if (payload.effectType === 'shield') {
  addBuff(... buffType: 'shield', value, expiresAt: Date.now() + (payload.durationMs ?? 15000) ...);
}
```

Existing ability handler (server/domains/index.ts ~L420-457) — has only damage, heal, taunt branches; the heal loop at ~L426-447 is byte-identical to the item heal loop at ~L338-356.

The 8 abilities and their effect routing (from 47-RESEARCH):
- warrior_berserker_rage (buff, damage_boost) → consumer exists (getDamageMultiplier)
- bard_inspire (buff, damage_boost) → consumer exists
- ranger_eagle_eye (buff, crit_boost) → store + emit only, // TODO: consumer
- rogue_shadow_step (buff, dodge) → store + emit only, // TODO: consumer
- wizard_time_warp (buff, cooldown_reduction) → store + emit only, // TODO: consumer
- paladin_holy_shield (shield) → consumer exists (getShieldAbsorption/reduceShield)
- paladin_divine_intervention (shield) → consumer exists
- oathbreaker_aura_of_dread (debuff, attack_slow) → store in activeDebuffs, // TODO: consumer
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract applyHealEffect helper and widen ActiveBuff.buffType for buff storage</name>
  <read_first>
    - server/domains/index.ts (item heal branch ~L338-356; ability heal branch ~L426-447; ActiveBuff interface ~L220-227; addBuff ~L236)
    - shared/abilityTypes.ts (BuffType union added in plan 47-01)
  </read_first>
  <behavior>
    - applyHealEffect(lobbyId, targetIds, value, healerId) performs the exact HP-clamp loop and emits combat:player_healed per target with actualHeal > 0 — byte-identical behavior to the current loops (combat:player_healed broadcast unchanged)
    - Both the item heal branch and the ability heal branch call applyHealEffect instead of inlining the loop
    - ActiveBuff.buffType is widened from 'damage_boost' | 'shield' to a union that includes BuffType members (damage_boost, crit_boost, dodge, cooldown_reduction) plus 'shield', so non-consumed buff types can be stored
    - getDamageMultiplier and getShieldAbsorption still compile and still match only their specific buffType
  </behavior>
  <action>
    In server/domains/index.ts add a module-private `function applyHealEffect(lobbyId: string, targetIds: string[], value: number, healerId: string)` alongside addBuff/reduceShield. Move the existing HP-clamp loop body (currently duplicated at ~L338-356 and ~L426-447) into it verbatim, emitting `combat:player_healed` with the same field shape (lobbyId, playerId, healerId, healAmount, newHealth). Replace BOTH inline heal loops (item handler heal branch and ability handler heal branch) with a single call to `applyHealEffect(payload.lobbyId, payload.targetIds, payload.value, payload.playerId)`. Widen `ActiveBuff.buffType` from `'damage_boost' | 'shield'` to `BuffType | 'shield'` (import BuffType from @shared/abilityTypes) so crit_boost/dodge/cooldown_reduction can be stored; leave getDamageMultiplier/getShieldAbsorption matching their literal types unchanged.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - server/domains/index.ts defines `function applyHealEffect` (verified by grep -v '^//' | grep -c applyHealEffect ≥ 1)
    - The item heal branch and ability heal branch both call applyHealEffect; neither contains its own inline `Math.min(targetState.maxHp` loop (verified by source read — only applyHealEffect has it)
    - ActiveBuff.buffType type includes BuffType members plus 'shield'
    - npm run check exits 0
  </acceptance_criteria>
  <done>Single applyHealEffect helper called by both handlers; ActiveBuff.buffType widened; tsc clean; heal broadcast unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add buff/shield/debuff branches to the ability:effect_applied handler with an activeDebuffs map</name>
  <read_first>
    - server/domains/index.ts (ability:effect_applied handler ~L420-457; item buff/shield branches ~L359-377 to mirror; addBuff ~L236; cleanupBuffs ~L289 and its call in session:lobby_destroyed ~L319-323)
    - shared/abilityTypes.ts (BuffType, DebuffType from plan 47-01)
  </read_first>
  <behavior>
    - The ability handler buff branch calls addBuff with buffType: payload.buffType (read from payload, NOT hardcoded), value: payload.value, expiresAt: Date.now() + (payload.durationMs ?? 10000)
    - The ability handler shield branch calls addBuff with buffType: 'shield', expiresAt default 15000 (mirroring the item shield branch)
    - The ability handler debuff branch stores the debuff (debuffType, value, expiresAt) in a separate activeDebuffs map keyed like activeBuffs; no crash when no consumer reads it
    - The item handler's hardcoded buffType: 'damage_boost' is audited: a comment confirms it is correct for the current item set (items only grant damage_boost), left as-is
    - activeDebuffs is cleaned up in the same session:lobby_destroyed path that calls cleanupBuffs
  </behavior>
  <action>
    In server/domains/index.ts add a module-private `const activeDebuffs = new Map<string, ...>()` and a small `addDebuff(lobbyId, playerId, debuffType, value, durationMs)` helper that stores entries with a setTimeout-based expiry mirroring addBuff (so no timer leak); add an `activeDebuffs` cleanup line inside the existing session:lobby_destroyed handler next to `cleanupBuffs(payload.lobbyId)`. In the `ability:effect_applied` handler (~L420), add three branches mirroring the item handler: a `buff` branch that calls `addBuff(payload.lobbyId, payload.playerId, { playerId, lobbyId, buffType: payload.buffType ?? 'damage_boost', value: payload.value, expiresAt: Date.now() + (payload.durationMs ?? 10000) })`; a `shield` branch identical to the item shield branch (buffType: 'shield', 15000 default); a `debuff` branch that calls addDebuff with payload.debuffType. Add `// TODO: consumer in future phase` comments on the crit_boost/dodge/cooldown_reduction and attack_slow paths (these are stored+emitted only — deferred per CONTEXT). Add a one-line audit comment at the item handler's `buffType: 'damage_boost'` confirming it is correct for the current item set. Do NOT refactor the shield monkey-patch at ~L394 (Phase 48's concern).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - server/domains/index.ts ability:effect_applied handler has a buff branch calling addBuff with buffType from payload (verified by source read — uses payload.buffType, not a literal)
    - The handler has a shield branch (buffType: 'shield') and a debuff branch (stores into activeDebuffs)
    - activeDebuffs is cleaned in the session:lobby_destroyed handler
    - npm run check exits 0 (the AbilityEffectHandler.test.ts assertions land in Task 3)
  </acceptance_criteria>
  <done>buff/shield/debuff branches added, debuffs stored in a dedicated map with cleanup, item hardcode audited, monkey-patch untouched, tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regression test all 8 abilities apply their effect + heal dedup</name>
  <read_first>
    - server/domains/CombatManager.test.ts (instantiation + stubbed eventBus pattern to mirror)
    - server/domains/AbilityManager.test.ts (ScopedEventBus usage; effect-emission tests ~L272-407)
    - server/domains/index.ts (the handler under test; getDamageMultiplier ~L261, getShieldAbsorption ~L269)
  </read_first>
  <behavior>
    - berserker_rage / inspire: after the buff branch runs, getDamageMultiplier(lobby, player) > 1.0
    - holy_shield / divine_intervention: after the shield branch runs, getShieldAbsorption(lobby, player) > 0
    - eagle_eye / shadow_step / time_warp: the buff branch runs without throwing and stores the buff (no consumer assertion needed beyond no-crash)
    - aura_of_dread: the debuff branch runs without throwing (stored in activeDebuffs)
    - heal dedup: a heal payload to a single target emits exactly one combat:player_healed (the helper is called once per branch, not twice)
  </behavior>
  <action>
    Create server/domains/AbilityEffectHandler.test.ts. Use the integration approach from 47-RESEARCH (import the wired module / event bus and drive ability:effect_applied through it, OR follow the CombatManager.test.ts direct-instantiation pattern with a stubbed combatManager exposing getCombatState with a fighting player). Cover: (1) a buff payload with buffType 'damage_boost' raises getDamageMultiplier above 1.0; (2) a shield payload raises getShieldAbsorption above 0; (3) buff payloads for crit_boost/dodge/cooldown_reduction and a debuff payload for attack_slow execute without throwing; (4) a single-target heal payload results in exactly one combat:player_healed emit (spy on eventBus.emit). Reference the 8 ability ids by name in test descriptions so the regression maps to CONTEXT's 8 affected abilities.
  </action>
  <verify>
    <automated>npx vitest run server/domains/AbilityEffectHandler.test.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - server/domains/AbilityEffectHandler.test.ts exists with assertions for getDamageMultiplier > 1.0 (buff), getShieldAbsorption > 0 (shield), no-throw for the 4 non-consumed buff/debuff types, and exactly-one combat:player_healed for a single-target heal
    - npx vitest run server/domains/AbilityEffectHandler.test.ts exits 0
    - npm run lint exits 0
    - npm test exits 0 (no regressions in AbilityManager / CombatManager suites)
  </acceptance_criteria>
  <done>New regression suite covers all 8 abilities applying their effect and heal-loop dedup; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ability:effect_applied bus event → server combat state mutation | The handler mutates player HP / buff state in response to a server-internal domain event (not directly client-triggered; use_ability is the validated client entry upstream) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-03 | Elevation of privilege | buff branch reading payload.buffType to addBuff | accept | payload originates from server-side AbilityManager driven by the already-validated use_ability client event (UseAbilityPayloadSchema); buffType comes from the trusted CLASS_ABILITY_CONFIGS, never from client input |
| T-47-04 | Denial of service | activeDebuffs / addBuff setTimeout handles | mitigate | addDebuff mirrors addBuff's setTimeout + cleanup; activeDebuffs is cleared in session:lobby_destroyed so no timer/map leak across lobbies |
| T-47-05 | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — pure code change |
</threat_model>

<verification>
- `npm run check` passes.
- `npx vitest run server/domains/AbilityEffectHandler.test.ts` passes.
- `npm test` passes (no regression in AbilityManager / CombatManager suites).
- Manual trace: only `applyHealEffect` contains the HP-clamp loop (no duplicate loop remains in either handler).
</verification>

<success_criteria>
- buff/shield/debuff branches present in the ability:effect_applied handler, reading payload fields.
- Single applyHealEffect helper called by both item and ability heal branches.
- activeDebuffs map stores boss debuffs with cleanup; ActiveBuff.buffType widened for non-consumed buffs.
- All 8 abilities apply their effect (consumed ones functionally, non-consumed stored without crash); regression suite green.
</success_criteria>

<output>
Create `.planning/phases/47-ability-effects-data-driven-registries/47-02-SUMMARY.md` when done.
</output>
