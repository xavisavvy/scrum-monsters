---
phase: 47-ability-effects-data-driven-registries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/abilityTypes.ts
  - shared/gameEvents.ts
  - server/domains/AbilityManager.ts
  - server/events/ClientEventEmitter.ts
autonomous: true
requirements: [EXT-01]
must_haves:
  truths:
    - "AbilityEffectAppliedPayload carries optional buffType, debuffType, durationMs"
    - "AbilityManager.applyAbilityEffect forwards abilityDef.buffType / debuffType / durationMs into the emitted event"
    - "The ability:effect_applied wire event and the ClientEventEmitter bridge carry the three new optional fields"
    - "buffType and debuffType are literal unions, not bare string"
    - "npm run check passes with no behavior change"
  artifacts:
    - path: "shared/abilityTypes.ts"
      provides: "BuffType / DebuffType unions; durationMs? on AbilityDefinition; buffType?/debuffType?/durationMs? on AbilityEffectAppliedPayload"
      contains: "export type BuffType"
    - path: "server/domains/AbilityManager.ts"
      provides: "emit site forwarding buffType/debuffType/durationMs"
  key_links:
    - from: "server/domains/AbilityManager.ts"
      to: "shared/abilityTypes.ts AbilityEffectAppliedPayload"
      via: "eventBus.emit('ability:effect_applied', ...)"
      pattern: "ability:effect_applied"
    - from: "server/events/ClientEventEmitter.ts"
      to: "shared/gameEvents.ts ServerToClientEvents['ability:effect_applied']"
      via: "emitToLobby spread"
      pattern: "ability:effect_applied"
---

<objective>
EXT-01a: Add the data the ability-effect handler needs, WITHOUT yet adding the handler branches. Extend the ability-effect payload (internal bus type AND wire type) and the `AbilityManager` emit site so `buffType`, `debuffType`, and a buff-specific `durationMs` flow through `ability:effect_applied`. Narrow `buffType`/`debuffType` from bare `string` to literal unions. This is a pure additive type/forwarding change — no runtime behavior changes (the handler in `server/domains/index.ts` is intentionally NOT touched here; that is plan 47-02).

Purpose: The locked sequencing requires payload fields + forwarding to land in a separate commit BEFORE the handler branches that read them (CONTEXT EXT-01 "Sequencing"). Without forwarded data, handler branches would have nothing to act on.
Output: Extended `AbilityDefinition`, `AbilityEffectAppliedPayload`, wire event, and `ClientEventEmitter` bridge; `BuffType`/`DebuffType` unions; `durationMs` populated on the 6 buff/debuff abilities (the 2 shield abilities use a handler default, not a payload field).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-ability-effects-data-driven-registries/47-CONTEXT.md
@.planning/phases/47-ability-effects-data-driven-registries/47-RESEARCH.md

<interfaces>
<!-- Extracted from source — executor uses these directly, no exploration needed. -->

From shared/abilityTypes.ts (current — to be extended):
```typescript
export type AbilityEffectType = 'damage' | 'heal' | 'buff' | 'debuff' | 'taunt' | 'shield';

export interface AbilityDefinition extends ClassAbilityDef {
  cooldownMs: number;
  effectType: AbilityEffectType;
  targetType: AbilityTargetType;
  power: number;
  buffType?: string;     // → narrow to BuffType
  debuffType?: string;   // → narrow to DebuffType
  // durationMs?: number  ← ADD
}

export interface AbilityEffectAppliedPayload {
  lobbyId: string;
  playerId: string;
  abilityId: string;
  effectType: AbilityEffectType;
  targetIds: string[];
  value: number;
  // buffType?, debuffType?, durationMs?  ← ADD all three (optional)
}
```

From server/domains/AbilityManager.ts (~L246, current emit — to forward 3 new fields):
```typescript
this.eventBus.emit('ability:effect_applied', {
  lobbyId, playerId, abilityId: abilityDef.id,
  effectType: abilityDef.effectType, targetIds, value: abilityDef.power,
  // buffType / debuffType / durationMs NOT forwarded ← ADD
});
```

From server/events/ClientEventEmitter.ts (~L487, current bridge spread — to add 3 fields):
```typescript
this.emitToLobby(payload.lobbyId, 'ability:effect_applied', {
  playerId, abilityId, effectType, targetIds, value,
  // buffType / debuffType / durationMs  ← ADD
});
```

Ability ids and their buff/debuff types (namespaced ids in CLASS_ABILITY_CONFIGS):
- warrior_berserker_rage → buffType 'damage_boost'
- bard_inspire → buffType 'damage_boost'
- ranger_eagle_eye → buffType 'crit_boost'
- rogue_shadow_step → buffType 'dodge'
- wizard_time_warp → buffType 'cooldown_reduction'
- oathbreaker_aura_of_dread → debuffType 'attack_slow'
- paladin_holy_shield, paladin_divine_intervention → effectType 'shield', NO buffType field
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BuffType/DebuffType unions, durationMs to AbilityDefinition, and extend the ability-effect payload + wire event</name>
  <read_first>
    - shared/abilityTypes.ts (AbilityEffectType ~L14, AbilityDefinition ~L35, AbilityEffectAppliedPayload ~L345, CLASS_ABILITY_CONFIGS for the buffType/debuffType values per ability)
    - shared/gameEvents.ts (~L550 ServerToClientEvents['ability:effect_applied'] wire payload)
  </read_first>
  <behavior>
    - BuffType union equals exactly the values used in CLASS_ABILITY_CONFIGS: 'damage_boost' | 'crit_boost' | 'dodge' | 'cooldown_reduction'
    - DebuffType union equals 'attack_slow'
    - AbilityDefinition.buffType is typed BuffType (optional); debuffType is typed DebuffType (optional); durationMs is optional number
    - AbilityEffectAppliedPayload gains optional buffType?: BuffType, debuffType?: DebuffType, durationMs?: number
    - The wire event ServerToClientEvents['ability:effect_applied'] gains the same three optional fields
    - durationMs is populated for warrior_berserker_rage, bard_inspire, ranger_eagle_eye, rogue_shadow_step, wizard_time_warp, oathbreaker_aura_of_dread (the buff/debuff abilities); the 2 shield abilities are left without durationMs (handler supplies a default)
  </behavior>
  <action>
    In shared/abilityTypes.ts: export `type BuffType = 'damage_boost' | 'crit_boost' | 'dodge' | 'cooldown_reduction'` and `type DebuffType = 'attack_slow'` (derive the members by reading the actual buffType/debuffType values in CLASS_ABILITY_CONFIGS — do not invent extras). Narrow `AbilityDefinition.buffType?` to `BuffType` and `debuffType?` to `DebuffType`. Add `durationMs?: number` to AbilityDefinition (the buff's active duration, distinct from cooldownMs). Add optional `buffType?: BuffType`, `debuffType?: DebuffType`, `durationMs?: number` to `AbilityEffectAppliedPayload`. Populate `durationMs` on the 6 buff/debuff ability definitions in CLASS_ABILITY_CONFIGS — pick a per-ability active-duration value (warrior_berserker_rage, bard_inspire, ranger_eagle_eye, rogue_shadow_step, wizard_time_warp ~10000; oathbreaker_aura_of_dread ~8000) consistent with the existing item-handler defaults of 10000ms for buffs. Do NOT add durationMs to paladin_holy_shield / paladin_divine_intervention (shields rely on the handler default). In shared/gameEvents.ts, add the same three optional fields to the `ability:effect_applied` entry of ServerToClientEvents so the wire type matches the bus payload.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - shared/abilityTypes.ts exports `BuffType` and `DebuffType` as the exact literal unions above (verified by source read)
    - AbilityEffectAppliedPayload has optional buffType, debuffType, durationMs fields
    - ServerToClientEvents['ability:effect_applied'] in shared/gameEvents.ts has the same three optional fields
    - All 6 buff/debuff abilities in CLASS_ABILITY_CONFIGS have a numeric durationMs; both shield abilities do not
    - npm run check exits 0
  </acceptance_criteria>
  <done>BuffType/DebuffType unions exist, AbilityDefinition and both payload types carry the new fields, durationMs populated on the 6 buff/debuff abilities, tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Forward buffType/debuffType/durationMs from AbilityManager and through the ClientEventEmitter bridge</name>
  <read_first>
    - server/domains/AbilityManager.ts (applyAbilityEffect ~L233-254, the eventBus.emit('ability:effect_applied', ...) call ~L246)
    - server/events/ClientEventEmitter.ts (~L487-495, the ability:effect_applied bridge that spreads payload fields)
    - server/domains/AbilityManager.test.ts (existing emission tests at ~L272-407 — extend rather than duplicate)
  </read_first>
  <behavior>
    - applyAbilityEffect's emit includes buffType: abilityDef.buffType, debuffType: abilityDef.debuffType, durationMs: abilityDef.durationMs
    - The ClientEventEmitter bridge for ability:effect_applied forwards buffType, debuffType, durationMs from the payload
    - A test asserts that using warrior_berserker_rage emits ability:effect_applied with buffType 'damage_boost'
    - A test asserts that using paladin_holy_shield emits effectType 'shield' with buffType/debuffType undefined (shields have no buffType)
  </behavior>
  <action>
    In server/domains/AbilityManager.ts applyAbilityEffect (~L246): add `buffType: abilityDef.buffType`, `debuffType: abilityDef.debuffType`, `durationMs: abilityDef.durationMs` to the `ability:effect_applied` emit object. In server/events/ClientEventEmitter.ts (~L488 bridge): add `buffType: payload.buffType`, `debuffType: payload.debuffType`, `durationMs: payload.durationMs` to the `emitToLobby('ability:effect_applied', {...})` object (Pitfall 2 — the bridge destructures explicitly, so unforwarded fields are silently dropped). Extend server/domains/AbilityManager.test.ts with assertions that the emitted ability:effect_applied payload carries buffType for a buff ability (warrior_berserker_rage → 'damage_boost') and carries no buffType for a shield ability (paladin_holy_shield).
  </action>
  <verify>
    <automated>npx vitest run server/domains/AbilityManager.test.ts</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - server/domains/AbilityManager.ts emit object includes buffType, debuffType, durationMs sourced from abilityDef (verified by source read)
    - server/events/ClientEventEmitter.ts ability:effect_applied bridge includes buffType, debuffType, durationMs from payload (verified by source read)
    - AbilityManager.test.ts has a test asserting warrior_berserker_rage emits buffType 'damage_boost'
    - npx vitest run server/domains/AbilityManager.test.ts exits 0
    - npm run check exits 0
  </acceptance_criteria>
  <done>Emit site and bridge both forward the three new fields; tests assert the forwarding for a buff ability and a shield ability; tsc and the AbilityManager suite pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server domain bus → wire (ClientEventEmitter) | Internal bus payload is bridged to the client over Socket.IO; only additive optional fields cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Information disclosure | ability:effect_applied wire payload gains buffType/debuffType/durationMs | accept | Buff metadata is non-sensitive gameplay data already implied by ability usage; no PII; additive optional fields only |
| T-47-02 | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — pure type/code change; nothing to verify |
</threat_model>

<verification>
- `npm run check` passes (wire type and bus payload both extended, no mismatch).
- `npx vitest run server/domains/AbilityManager.test.ts` passes.
- No change to `server/domains/index.ts` (handler branches are plan 47-02).
</verification>

<success_criteria>
- AbilityEffectAppliedPayload, AbilityDefinition, and the wire event all carry buffType?/debuffType?/durationMs?.
- buffType/debuffType are literal unions.
- AbilityManager and the ClientEventEmitter bridge forward all three fields.
- No runtime behavior change; tsc and the AbilityManager test suite green.
</success_criteria>

<output>
Create `.planning/phases/47-ability-effects-data-driven-registries/47-01-SUMMARY.md` when done.
</output>
