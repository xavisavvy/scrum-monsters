---
phase: 47-ability-effects-data-driven-registries
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - server/domains/boss-ai/types.ts
  - server/domains/boss-ai/boss-definitions/index.ts
  - server/domains/boss-ai/boss-definitions/bugHydra.ts
  - server/domains/boss-ai/boss-definitions/sprintDemon.ts
  - server/domains/boss-ai/boss-definitions/deadlineDragon.ts
  - server/domains/boss-ai/boss-definitions/techDebtGolem.ts
  - server/domains/boss-ai/boss-definitions/scopeCreepBeast.ts
  - server/gameState.ts
  - server/domains/boss-ai/BossAI.test.ts
autonomous: true
requirements: [EXT-03]
must_haves:
  truths:
    - "BossBehavior carries sprite and description fields, populated for all 5 bosses"
    - "SPRITE_TO_BOSS_TYPE is derived from Object.values(BOSS_BEHAVIORS), not hand-written"
    - "availableBosses in gameState.ts is derived from Object.values(BOSS_BEHAVIORS), not an inline array"
    - "getBossTypeFromSprite('technical-debt-golem.png') returns 'tech-debt-golem' (golem bug fixed)"
    - "All 5 boss sprite filenames round-trip through SPRITE_TO_BOSS_TYPE"
  artifacts:
    - path: "server/domains/boss-ai/types.ts"
      provides: "BossBehavior with sprite + description fields"
      contains: "sprite"
    - path: "server/domains/boss-ai/boss-definitions/index.ts"
      provides: "derived SPRITE_TO_BOSS_TYPE"
      contains: "Object.values(BOSS_BEHAVIORS)"
    - path: "server/domains/boss-ai/BossAI.test.ts"
      provides: "5-boss sprite round-trip + golem regression test"
  key_links:
    - from: "server/domains/boss-ai/boss-definitions/index.ts SPRITE_TO_BOSS_TYPE"
      to: "BOSS_BEHAVIORS"
      via: "Object.fromEntries(Object.values(BOSS_BEHAVIORS).map(b => [b.sprite, b.bossType]))"
      pattern: "Object.values\\(BOSS_BEHAVIORS\\)"
    - from: "server/gameState.ts availableBosses"
      to: "BOSS_BEHAVIORS"
      via: "Object.values(BOSS_BEHAVIORS)"
      pattern: "Object.values\\(BOSS_BEHAVIORS\\)"
---

<objective>
EXT-03: Make `SPRITE_TO_BOSS_TYPE` (boss-definitions/index.ts:45) and `availableBosses` (gameState.ts:1254) derive from `Object.values(BOSS_BEHAVIORS)` so the sprite→type map can never again drift from the behavior table. To enable derivation, add `sprite` and `description` fields directly to `BossBehavior` (types.ts:95 — Option A, fewest files) and populate them in each of the 5 boss definition files. This fixes the live golem bug at the root: `gameState.ts` emits sprite `'technical-debt-golem.png'` but the hand-written map keyed it `'tech-debt-golem.png'`, so the golem ran Bug Hydra AI. With derivation, the golem definition's `sprite: 'technical-debt-golem.png'` becomes the map key by construction (the PR #167 symptom, subsumed at the root).

Purpose: Two hand-maintained tables (sprite→type and availableBosses) duplicate data already in BOSS_BEHAVIORS and have drifted — the golem AI mismatch is the live consequence. Deriving both from the single behavior registry is the rank-1 follow-up.
Output: `BossBehavior` with sprite/description, all 5 definitions populated, derived `SPRITE_TO_BOSS_TYPE` and `availableBosses`, and a round-trip test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-ability-effects-data-driven-registries/47-CONTEXT.md
@.planning/phases/47-ability-effects-data-driven-registries/47-RESEARCH.md

<interfaces>
<!-- Extracted from source — executor uses these directly. -->

From server/domains/boss-ai/types.ts:95 (BossBehavior — ADD sprite + description):
```typescript
export interface BossBehavior {
  bossType: BossType;
  name: string;
  patterns: AttackPattern[];
  phaseTransitionMessages: Record<BossPhaseNumber, string>;
  // sprite: string;       ← ADD
  // description: string;  ← ADD
}
```

From server/domains/boss-ai/boss-definitions/index.ts:
```typescript
export const BOSS_BEHAVIORS: Record<BossType, BossBehavior> = {
  'bug-hydra': BUG_HYDRA_BEHAVIOR,
  'sprint-demon': SPRINT_DEMON_BEHAVIOR,
  'deadline-dragon': DEADLINE_DRAGON_BEHAVIOR,
  'tech-debt-golem': TECH_DEBT_GOLEM_BEHAVIOR,
  'scope-creep-beast': SCOPE_CREEP_BEAST_BEHAVIOR,
};

// HAND-WRITTEN (~L45) — DELETE, replace with derivation:
const SPRITE_TO_BOSS_TYPE: Record<string, BossType> = {
  'bug-hydra.png': 'bug-hydra',
  'sprint-demon.png': 'sprint-demon',
  'deadline-dragon.png': 'deadline-dragon',
  'tech-debt-golem.png': 'tech-debt-golem',     // ← wrong key (golem bug)
  'scope-creep-beast.png': 'scope-creep-beast',
};
export function getBossTypeFromSprite(sprite: string): BossType | null { ... }
```

Sprite filenames (from gameState.ts:1254-1280 availableBosses — the AUTHORITATIVE sprite strings to populate into each BossBehavior):
- bug-hydra → 'bug-hydra.png'
- sprint-demon → 'sprint-demon.png'
- deadline-dragon → 'deadline-dragon.png'
- tech-debt-golem → 'technical-debt-golem.png'   ← note: technical-, NOT tech-
- scope-creep-beast → 'scope-creep-beast.png'
(descriptions for each boss also live in the gameState.ts:1254 inline array — copy them onto the BossBehavior.description fields.)

From server/gameState.ts:1254-1283 — inline availableBosses array using { sprite, name, description }, consumed at ~L1283 via .sprite/.name/.description. gameState.ts already imports from server/domains/boss-ai (no new circular dep).
```
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sprite/description to BossBehavior and populate all 5 boss definitions (fixing the golem sprite)</name>
  <read_first>
    - server/domains/boss-ai/types.ts (BossBehavior ~L95-100)
    - server/domains/boss-ai/boss-definitions/bugHydra.ts, sprintDemon.ts, deadlineDragon.ts, techDebtGolem.ts, scopeCreepBeast.ts (the 5 *_BEHAVIOR objects to extend)
    - server/gameState.ts (availableBosses inline array ~L1254-1280 — source of the exact sprite filenames and descriptions; note the golem is 'technical-debt-golem.png')
  </read_first>
  <behavior>
    - BossBehavior has required `sprite: string` and `description: string` fields
    - Each of the 5 *_BEHAVIOR objects sets sprite + description (sprite values exactly matching gameState.ts: golem is 'technical-debt-golem.png', not 'tech-debt-golem.png'; descriptions copied from the inline array)
    - TECH_DEBT_GOLEM_BEHAVIOR.sprite === 'technical-debt-golem.png' (the corrected key)
    - tsc enforces all 5 definitions provide the new required fields
  </behavior>
  <action>
    In server/domains/boss-ai/types.ts add required `sprite: string` and `description: string` to the BossBehavior interface (~L95). Populate sprite + description on each of the 5 boss definition files (bugHydra, sprintDemon, deadlineDragon, techDebtGolem, scopeCreepBeast), copying the exact sprite filename and description text from the gameState.ts:1254 inline availableBosses array. Critically, set TECH_DEBT_GOLEM_BEHAVIOR.sprite to 'technical-debt-golem.png' (the filename gameState.ts actually emits) — this is the live golem-AI fix at the source. Because sprite/description are required, tsc will flag any definition that forgot them.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - server/domains/boss-ai/types.ts BossBehavior has required sprite and description fields (verified by source read)
    - All 5 boss definitions set sprite and description; golem sprite is 'technical-debt-golem.png' (verified by source read)
    - npm run check exits 0 (proves all 5 satisfy the required fields)
  </acceptance_criteria>
  <done>BossBehavior carries sprite/description; all 5 bosses populate them; golem sprite corrected to 'technical-debt-golem.png'; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive SPRITE_TO_BOSS_TYPE from BOSS_BEHAVIORS + round-trip/regression test</name>
  <read_first>
    - server/domains/boss-ai/boss-definitions/index.ts (BOSS_BEHAVIORS ~L19; hand-written SPRITE_TO_BOSS_TYPE ~L45; getBossTypeFromSprite ~L58)
    - server/domains/boss-ai/BossAI.test.ts (existing boss-type round-trip pattern to extend)
  </read_first>
  <behavior>
    - SPRITE_TO_BOSS_TYPE is `Object.fromEntries(Object.values(BOSS_BEHAVIORS).map(b => [b.sprite, b.bossType]))` — no hand-written entries
    - getBossTypeFromSprite('technical-debt-golem.png') === 'tech-debt-golem'
    - getBossTypeFromSprite returns the correct bossType for all 5 sprites; Object.keys(SPRITE_TO_BOSS_TYPE).length === 5
    - getBossTypeFromSprite('tech-debt-golem.png') (the OLD wrong key) now returns null (the stale key is gone)
  </behavior>
  <action>
    In server/domains/boss-ai/boss-definitions/index.ts, replace the hand-written SPRITE_TO_BOSS_TYPE literal (~L45) with a derivation from Object.values(BOSS_BEHAVIORS) mapping each behavior's sprite to its bossType; export it (the research notes it is currently a private const — exporting it enables the round-trip test). Leave getBossTypeFromSprite's body unchanged (it reads the derived map). Extend server/domains/boss-ai/BossAI.test.ts with: a test asserting all 5 sprite→bossType pairs round-trip (loop over BOSS_BEHAVIORS, assert getBossTypeFromSprite(b.sprite) === b.bossType); an explicit golem regression asserting getBossTypeFromSprite('technical-debt-golem.png') === 'tech-debt-golem'; and an assertion that Object.keys(SPRITE_TO_BOSS_TYPE).length === 5.
  </action>
  <verify>
    <automated>npx vitest run server/domains/boss-ai/BossAI.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - SPRITE_TO_BOSS_TYPE is derived via Object.values(BOSS_BEHAVIORS) (verified by source read — no literal table)
    - BossAI.test.ts asserts a 5-boss round-trip and getBossTypeFromSprite('technical-debt-golem.png') === 'tech-debt-golem'
    - npx vitest run server/domains/boss-ai/BossAI.test.ts exits 0
    - npm run check exits 0
  </acceptance_criteria>
  <done>SPRITE_TO_BOSS_TYPE derived from the behavior table; golem round-trips correctly; 5-boss round-trip test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Derive availableBosses in gameState.ts from BOSS_BEHAVIORS (lint included — final task)</name>
  <read_first>
    - server/gameState.ts (availableBosses inline array ~L1254-1280; selection/consumption at ~L1283 using .sprite/.name/.description; existing imports from server/domains/boss-ai)
    - server/domains/boss-ai/boss-definitions/index.ts (BOSS_BEHAVIORS export)
  </read_first>
  <behavior>
    - The inline availableBosses array in gameState.ts is replaced by Object.values(BOSS_BEHAVIORS)
    - The boss-selection logic at ~L1283 still reads .sprite, .name, .description off the selected entry (now provided by BossBehavior from Task 1)
    - The emitted battle-init sprite for the golem is 'technical-debt-golem.png' (unchanged from before — it always was; the fix is that the map now matches it)
    - No circular import introduced (gameState already imports from boss-ai)
  </behavior>
  <action>
    In server/gameState.ts, delete the inline availableBosses array (~L1254-1280) and replace it with `const availableBosses = Object.values(BOSS_BEHAVIORS)` (import BOSS_BEHAVIORS from the boss-ai barrel that gameState already imports from — confirm no new circular dependency per RESEARCH Open Question 4). Keep the selection logic at ~L1283 intact; it now reads sprite/name/description from the BossBehavior values populated in Task 1. Verify the random/selection behavior is unchanged (same 5 bosses, same fields).
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - server/gameState.ts availableBosses is `Object.values(BOSS_BEHAVIORS)`; the inline 5-entry array is gone (verified by source read)
    - Boss selection at ~L1283 still reads .sprite/.name/.description
    - npm run check exits 0
    - npm run lint exits 0
    - npm test exits 0 (no boss-AI or combat regressions)
  </acceptance_criteria>
  <done>availableBosses derived from BOSS_BEHAVIORS; inline array deleted; selection logic intact; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| boss registry → battle init (server) | sprite/bossType drive which AI runs; static trusted data, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-09 | Spoofing | sprite→bossType mapping drift (golem ran wrong AI) | mitigate | Deriving SPRITE_TO_BOSS_TYPE from BOSS_BEHAVIORS makes drift structurally impossible; round-trip test asserts all 5 map correctly |
| T-47-10 | Tampering | required sprite/description on BossBehavior | mitigate | Fields are required, so tsc rejects any new boss definition that omits them — the registry stays complete by construction |
| T-47-11 | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — pure type/code change |
</threat_model>

<verification>
- `npm run check` passes (required sprite/description enforced on all 5 definitions).
- `npx vitest run server/domains/boss-ai/BossAI.test.ts` passes (5-boss round-trip + golem regression).
- `npm test` passes (no boss-AI / combat regressions).
- Manual trace: no hand-written SPRITE_TO_BOSS_TYPE or inline availableBosses array remains.
</verification>

<success_criteria>
- BossBehavior carries sprite/description (required); all 5 bosses populate them; golem sprite is 'technical-debt-golem.png'.
- SPRITE_TO_BOSS_TYPE and availableBosses both derive from Object.values(BOSS_BEHAVIORS).
- getBossTypeFromSprite('technical-debt-golem.png') === 'tech-debt-golem'; 5-boss round-trip test green.
</success_criteria>

<output>
Create `.planning/phases/47-ability-effects-data-driven-registries/47-04-SUMMARY.md` when done.
</output>
