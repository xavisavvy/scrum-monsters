---
phase: 50-finish-the-gamestate-domain-manager-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/gameState.ts
  - server/domains/SessionManager.ts
  - server/websocket.ts
  - server/gameState.test.ts
  - server/domains/SessionManager.test.ts
  - server/gameState-characterization.test.ts
  - server/websocket.autoAdvance.reconnect.test.ts
autonomous: true
requirements: [MAINT-07]
nyquist_compliant: true

must_haves:
  truths:
    - "syncPlayerToLobby registers a playerToLobby alias for EVERY player in the lobby, not just the triggering player (closes latent reconnect staleness)"
    - "A reconnecting player's lobby-mates can resolve their lobby via getLobbyByPlayerId without an explicit per-player sync call"
    - "Timer, Jira, and estimation settings are mutated by SessionManager (host-guarded), not GameStateManager"
    - "session:settings_updated still emits from the websocket.ts handler (emit timing unchanged) after the settings migration"
    - "GameStateManager.joinLobby, updatePlayerTeam, updatePlayerAvatar, createLobby, updateTimerSettings, updateJiraSettings, updateEstimationSettings no longer exist on the class"
    - "GameStateManager.removePlayer STILL EXISTS (deferred: live internal caller in processDisconnectedPlayers)"
    - "Battle methods (attackBoss/startBattle/submitScore/revealScores) STILL EXIST (explicit non-goal — not migrated)"
    - "Full suite green (919+ tests), npm run check clean, npm run lint clean"
  artifacts:
    - path: "server/gameState.ts"
      provides: "syncPlayerToLobby alias-loop fix; 4 dead methods + 3 settings methods removed; removePlayer + battle methods retained"
      contains: "for (const player of lobby.players)"
    - path: "server/domains/SessionManager.ts"
      provides: "updateTimerSettings/updateJiraSettings/updateEstimationSettings public methods"
      contains: "updateTimerSettings"
    - path: "server/gameState-characterization.test.ts"
      provides: "pre-deletion shape comparison gate for createLobby/joinLobby vs SessionManager"
    - path: "server/domains/SessionManager.test.ts"
      provides: "settings delegation tests (host guard + mutation)"
  key_links:
    - from: "server/websocket.ts update_timer_settings handler"
      to: "sessionManager.updateTimerSettings"
      via: "direct call, emitFineGrained stays in handler"
      pattern: "sessionManager\\.updateTimerSettings"
    - from: "server/gameState.ts syncPlayerToLobby"
      to: "this.playerToLobby"
      via: "loop over lobby.players"
      pattern: "for \\(const player of lobby\\.players\\)"
---

<objective>
MAINT-07: Complete the ordered, reversible decommission of dead/duplicate GameStateManager methods and migrate the three settings setters into SessionManager — with the syncPlayerToLobby alias-staleness fix landing FIRST as an additive, non-destructive change.

Purpose: The monolith→domain-manager migration stalled with dead duplicate methods on GameStateManager (proven zero-caller by the Phase 50 Call-Site Audit) and a latent reconnect-staleness bug where syncPlayerToLobby only registered the triggering player's alias. This plan closes the alias bug, moves settings ownership to SessionManager, and deletes only the methods proven safe — each deletion gated on a green characterization test and a zero-caller grep.

Output: Fixed syncPlayerToLobby; SessionManager owns settings; GameStateManager.joinLobby/updatePlayerTeam/updatePlayerAvatar/createLobby/updateTimerSettings/updateJiraSettings/updateEstimationSettings deleted. removePlayer and battle methods explicitly retained.

CRITICAL SCOPE GUARD: removePlayer (gameState.ts:623) is NOT deleted — it has a live internal caller in processDisconnectedPlayers (line 193, driven by the 30s disconnectWatchdog). Battle methods are an EXPLICIT NON-GOAL. Do not touch either.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-finish-the-gamestate-domain-manager-migration/50-RESEARCH.md
@.planning/phases/50-finish-the-gamestate-domain-manager-migration/50-PATTERNS.md
@.planning/phases/50-finish-the-gamestate-domain-manager-migration/50-VALIDATION.md

<interfaces>
<!-- Extracted from codebase — use directly, no exploration needed. -->

GameStateManager.syncPlayerToLobby (server/gameState.ts:694) — CURRENT (buggy, single-player):
  syncPlayerToLobby(playerId: string, lobby: Lobby): void
  - only this.playerToLobby.set(playerId, lobby.id); does NOT register other lobby.players

GameStateManager dead methods (zero production callers per Call-Site Audit):
  joinLobby(lobbyId, playerName): { lobby, player } | null   // gameState.ts:574
  updatePlayerTeam(playerId, team: TeamType): Lobby | null   // gameState.ts:703
  updatePlayerAvatar(playerId, avatar: AvatarClass): Lobby | null  // gameState.ts:716
  createLobby(hostName, lobbyName, initialSettings?): Lobby  // gameState.ts:482 — 2 TEST-ONLY callers
  updateTimerSettings(playerId, timerSettings): Lobby | null      // gameState.ts:2006
  updateJiraSettings(playerId, jiraSettings): Lobby | null        // gameState.ts:2017
  updateEstimationSettings(playerId, estimationSettings): Lobby | null  // gameState.ts:2028

GameStateManager.removePlayer (gameState.ts:623) — DO NOT DELETE. Live internal caller at gameState.ts:193 (processDisconnectedPlayers, 30s disconnectWatchdog). DEFERRED to a future GameState-decommission phase.

SessionManager (server/domains/SessionManager.ts):
  createLobby(hostName, lobbyName, options?): Lobby   // line 119 — enforces MAX_LOBBIES, emits session:player_joined
  joinLobby(lobbyId, playerName): { lobby, player }   // line 240 — throws on error
  getPlayerLobby(playerId): Lobby | null              // line 438
  private updatePlayerTeam(playerId, team): Lobby     // line 566 — template for guard sequence
  Error classes (server/errors/SessionErrors): PlayerNotFoundError, PlayerNotHostError, LobbyNotFoundError, SessionError

websocket.ts settings handlers (current, call gameState.*):
  on('update_timer_settings', ({ timerSettings }) => ...)   // line 1621 — calls gameState.updateTimerSettings, then emitFineGrained(lobby.id, 'session:settings_updated', { timerSettings })
  on('update_jira_settings', ({ jiraSettings }) => ...)     // line 1632
  on('update_estimation_settings', ({ estimationSettings }) => ...)  // line 1643
  Both sessionManager and gameState are in scope in websocket.ts (imports at lines 14, plus combatManager at 16).

Phase 48 test seams:
  new GameStateManager(undefined, { startWatchdogs: false })  // no timer leak in tests
  new SessionManager({ eventBus: new ScopedEventBus() })       // from '../events'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix syncPlayerToLobby alias staleness (additive, lands FIRST) + regression test</name>
  <files>server/gameState.ts, server/gameState.test.ts</files>
  <read_first>
    - server/gameState.ts:690-701 (the method being changed)
    - server/domains/SessionManager.ts:999-1016 (lobby.players for-of iteration analog from PATTERNS.md)
    - server/gameState.test.ts:193-197 (constructable GameStateManager seam — `new GameStateManager(undefined, { startWatchdogs: false })`)
  </read_first>
  <behavior>
    - Test (RED first): construct GameStateManager with startWatchdogs:false; build a lobby with 3 players (host + 2); call syncPlayerToLobby with ONLY the host's id; assert getLobbyByPlayerId(player2.id) and getLobbyByPlayerId(player3.id) both resolve the lobby (fails today because only host alias is registered).
    - Test: calling syncPlayerToLobby again for an already-registered player is idempotent (alias unchanged, no throw).
    - Test: syncPlayerToLobby always refreshes this.lobbies.get(lobby.id) to the passed lobby reference (unconditional set, not has-guarded).
  </behavior>
  <action>
    Rewrite `syncPlayerToLobby(playerId: string, lobby: Lobby): void` at gameState.ts:694 to: (1) unconditionally `this.lobbies.set(lobby.id, lobby)` (drop the `if (!this.lobbies.has(...))` guard — always refresh the reference); (2) `this.playerToLobby.set(playerId, lobby.id)` for the triggering player; (3) iterate `for (const player of lobby.players)` and set the alias for each player only `if (!this.playerToLobby.has(player.id))` (idempotent — never overwrites a different lobby). This is additive only; no behavior removed. Add the regression test to server/gameState.test.ts following the constructable-seam pattern. This task lands FIRST and is NON-DESTRUCTIVE — no deletions here.
  </action>
  <verify>
    <automated>npx vitest run server/gameState.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run server/gameState.test.ts` exits 0; the new multi-player alias test passes (and demonstrably failed against the pre-fix method body).
    - `grep -n "for (const player of lobby.players)" server/gameState.ts` returns the new loop inside syncPlayerToLobby.
    - `npm run check` exits 0; `npm run lint` exits 0.
    - Phase 41 reconnect invariants in server/domains/SessionManager.test.ts remain green (`npx vitest run server/domains/SessionManager.test.ts`).
  </acceptance_criteria>
  <done>syncPlayerToLobby registers aliases for all lobby members idempotently; regression test green; no method deleted in this task.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate timer/jira/estimation settings to SessionManager; redirect websocket handlers</name>
  <files>server/domains/SessionManager.ts, server/domains/SessionManager.test.ts, server/websocket.ts</files>
  <read_first>
    - server/gameState.ts:2006-2037 (current GameState settings setters — the source behavior to preserve)
    - server/domains/SessionManager.ts:566-597 (updatePlayerTeam — the get-lobby / find-player / isHost-guard / mutate / return template per RESEARCH.md and Pitfall 5)
    - server/domains/SessionManager.ts:438-444 (getPlayerLobby signature)
    - server/domains/SessionManager.ts:30-34 (SessionErrors imports: PlayerNotFoundError, PlayerNotHostError)
    - server/websocket.ts:1621-1654 (the three handlers to redirect; emitFineGrained stays in handler per Phase 42-02b)
    - server/domains/SessionManager.test.ts:993-1018 (assignTeam host-guard `.toThrow(PlayerNotHostError)` test template)
  </read_first>
  <action>
    Add three PUBLIC methods to SessionManager (modeled on updatePlayerTeam guard sequence, but PUBLIC and returning Lobby, throwing instead of returning null): `updateTimerSettings(playerId: string, timerSettings: TimerSettings): Lobby`, `updateJiraSettings(playerId: string, jiraSettings: JiraSettings): Lobby`, `updateEstimationSettings(playerId: string, estimationSettings: EstimationSettings): Lobby`. Each: `getPlayerLobby(playerId)` → throw `PlayerNotFoundError` if null; find player → throw `PlayerNotHostError` if not `player?.isHost`; mutate `lobby.timerSettings` / `lobby.jiraSettings` / `lobby.estimationSettings`; return lobby. DO NOT emit any event inside SessionManager (emit stays in the websocket handler — Pitfall 5). In server/websocket.ts, change the three handlers (lines 1621, 1632, 1643) from `gameState.updateTimerSettings(...)` to `sessionManager.updateTimerSettings(...)` (and jira/estimation), wrapping in try/catch that emits `socket.emit('game_error', { message: (err as Error).message })`; the existing `emitFineGrained(lobby.id, 'session:settings_updated', {...})` call stays unchanged in the handler. Add SessionManager.test.ts blocks for each method: host happy-path (asserts the settings field equals input on the returned lobby) and non-host rejection (`.toThrow(PlayerNotHostError)`), following the assignTeam template. DO NOT delete the GameState settings methods yet — that is Task 4 (after this is green).
  </action>
  <verify>
    <automated>npx vitest run server/domains/SessionManager.test.ts server/websocket.handlers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - SessionManager exposes the three public settings methods returning Lobby and throwing PlayerNotHostError for non-hosts.
    - `grep -n "sessionManager.updateTimerSettings" server/websocket.ts` and the jira/estimation equivalents return hits; the corresponding `gameState.update*Settings` calls are GONE from websocket.ts (`grep -c "gameState.updateTimerSettings\|gameState.updateJiraSettings\|gameState.updateEstimationSettings" server/websocket.ts` returns 0).
    - `grep -v '^[[:space:]]*//' server/websocket.ts | grep -c "session:settings_updated"` confirms the emitFineGrained calls remain (3 expected).
    - New SessionManager.test.ts settings tests pass; `npm run check` exits 0; `npm run lint` exits 0.
  </acceptance_criteria>
  <done>SessionManager owns settings (host-guarded); websocket handlers call SessionManager; emit timing unchanged; tests green. GameState settings setters still present (deleted in Task 4).</done>
</task>

<task type="auto">
  <name>Task 3: Write characterization gate + migrate the 2 createLobby test callers to SessionManager</name>
  <files>server/gameState-characterization.test.ts, server/websocket.autoAdvance.reconnect.test.ts, server/gameState.test.ts</files>
  <read_first>
    - server/gameState.ts:482-572 (createLobby), :574-621 (joinLobby), :703-714 (updatePlayerTeam), :716-727 (updatePlayerAvatar) — the methods to characterize before deletion
    - server/domains/SessionManager.ts:119 (createLobby), :240 (joinLobby) — the equivalents
    - server/websocket.autoAdvance.reconnect.test.ts:17-22 (the createLobby caller to migrate)
    - server/gameState.test.ts:193-197 (the second createLobby caller — `gs = gameState as any` fixture)
    - 50-PATTERNS.md "server/websocket.autoAdvance.reconnect.test.ts" section (exact migration target showing sessionManager.createLobby + gameStateMgr.syncPlayerToLobby)
  </read_first>
  <action>
    Create server/gameState-characterization.test.ts (NEW, to be deleted after Phase 50 lands — note that in a header comment): construct `new GameStateManager(undefined, { startWatchdogs: false })` and `new SessionManager({ eventBus: new ScopedEventBus() })`; for createLobby and joinLobby, assert the SessionManager output shape matches the GameState output shape (same structural keys via `expect(Object.keys(gsLobby)).toEqual(expect.arrayContaining(Object.keys(smLobby)))`, players length 1 on create). This pins behavior BEFORE the deletions in Task 4. Migrate the two createLobby test callers: in server/websocket.autoAdvance.reconnect.test.ts replace the `gameState.createLobby('TestHost', 'Reconnect Test Lobby', { estimationSettings })` singleton call with constructed `new SessionManager({ eventBus })` + `sessionManager.createLobby(...)` then `gameStateMgr.syncPlayerToLobby(lobby.hostId, lobby)` (production pattern), and route subsequent reconnect calls through `sessionManager.handlePlayerDisconnect` / `sessionManager.attemptPlayerReconnect` per the PATTERNS.md target; in server/gameState.test.ts:197 replace the `gs.createLobby(...)` fixture with `sessionManager.createLobby(...)` + `gs.syncPlayerToLobby(...)`. The autoAdvance reconnect round-trip assertions MUST remain green (Phase 41 invariant). NO deletions in this task.
  </action>
  <verify>
    <automated>npx vitest run server/gameState-characterization.test.ts server/websocket.autoAdvance.reconnect.test.ts server/gameState.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - server/gameState-characterization.test.ts exists and passes (shape-match gate green) — this is the pre-deletion characterization required by 50-VALIDATION.md.
    - `grep -c "gameState.createLobby\|gs.createLobby" server/websocket.autoAdvance.reconnect.test.ts server/gameState.test.ts` returns 0 (both callers migrated to sessionManager.createLobby).
    - autoAdvance reconnect round-trip test still green (Phase 41 regression intact).
    - `npm run check` exits 0; `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Characterization gate green; both createLobby test callers use SessionManager + syncPlayerToLobby; no production behavior changed; nothing deleted yet.</done>
</task>

<task type="auto">
  <name>Task 4: Delete the 4 safe dead methods + 3 GameState settings setters (gated on green tests + zero-caller greps)</name>
  <files>server/gameState.ts</files>
  <read_first>
    - server/gameState.ts:482-572 (createLobby), :574-621 (joinLobby), :703-714 (updatePlayerTeam), :716-727 (updatePlayerAvatar), :2006-2037 (settings setters) — the deletion targets
    - server/gameState.ts:193 (processDisconnectedPlayers → this.removePlayer — the LIVE caller that keeps removePlayer alive; DO NOT TOUCH removePlayer)
    - 50-RESEARCH.md "Call-Site Audit" + "Deletion Pre-Condition Summary" (the SAFE/CONDITIONAL verdicts)
  </read_first>
  <action>
    Precondition (must hold BEFORE editing): Tasks 1-3 green — characterization test passing, settings migrated, createLobby test callers migrated. Delete from server/gameState.ts: `joinLobby` (574-621), `updatePlayerTeam` (703-714), `updatePlayerAvatar` (716-727), `createLobby` (482-572), `updateTimerSettings`/`updateJiraSettings`/`updateEstimationSettings` (2006-2037). Add a one-line code comment near where removePlayer is defined documenting the deferral: that GameStateManager.removePlayer is INTENTIONALLY RETAINED because processDisconnectedPlayers (the 30s disconnectWatchdog) still calls it, and it will be removed in a future GameState-decommission phase — so it is not mistaken for an oversight. DO NOT delete: `removePlayer`, `selectAvatar` (729 — separate, still used), and all battle methods (`attackBoss`/`startBattle`/`submitScore`/`revealScores` — explicit NON-GOAL). After deletion, fix any now-dead imports flagged by tsc. The characterization test (server/gameState-characterization.test.ts) for createLobby/joinLobby will now FAIL to compile (methods gone) — that is the expected point-of-no-return signal; delete those two characterization cases (or the file if it only held those), keeping the migrated SessionManager tests as the live regression coverage.
  </action>
  <verify>
    <automated>npm run check && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Zero-caller grep proof — all return 0: `grep -rn "gameState.joinLobby\|\.joinLobby(" server --include=*.ts | grep -v sessionManager | grep -v "SessionManager"`, `grep -rn "gameState.createLobby\|gs.createLobby" server`, `grep -rn "gameState.updatePlayerTeam\|gameState.updatePlayerAvatar" server`, `grep -rn "gameState.updateTimerSettings\|gameState.updateJiraSettings\|gameState.updateEstimationSettings" server`.
    - Method-absence grep on the class — these no longer appear as method definitions in server/gameState.ts: `grep -n "^  joinLobby(\|^  createLobby(\|^  updatePlayerTeam(\|^  updatePlayerAvatar(\|^  updateTimerSettings(\|^  updateJiraSettings(\|^  updateEstimationSettings(" server/gameState.ts` returns nothing.
    - removePlayer RETAINED: `grep -n "removePlayer(playerId: string)\|removePlayer(" server/gameState.ts` still returns the definition; the deferral comment is present.
    - Battle methods RETAINED: `grep -c "attackBoss\|startBattle\|submitScore\|revealScores" server/gameState.ts` is > 0.
    - `npm run check` exits 0; `npm test` exits 0 (919+); `npm run lint` exits 0.
    - Phase 41 reconnect + autoAdvance regressions still green.
  </acceptance_criteria>
  <done>4 dead methods + 3 settings setters deleted; removePlayer and battle methods retained with deferral documented; full suite + tsc + lint green.</done>
</task>

</tasks>

<verification>
- syncPlayerToLobby registers all lobby members' aliases (regression test, Task 1).
- SessionManager owns timer/jira/estimation settings; emit stays in websocket handler (Task 2).
- Characterization gate green BEFORE deletion; createLobby test callers migrated (Task 3).
- Dead methods deleted only after green tests + zero-caller greps; removePlayer + battle methods retained (Task 4).
- Full suite (`npm test`, 919+), `npm run check`, `npm run lint` all green after each task.
</verification>

<success_criteria>
- MAINT-07 Success Criterion 1 met: syncPlayerToLobby unconditional alias registration landed FIRST.
- MAINT-07 Success Criterion 2 met: proven-dead methods deleted after identical-shape audit; settings migrated to SessionManager; removePlayer DEFERRED (documented); battle methods confirmed NOT migrated.
- No regression in reconnection (Phase 41), revival, or host-transfer.
</success_criteria>

<output>
Create `.planning/phases/50-finish-the-gamestate-domain-manager-migration/50-01-SUMMARY.md` when done. In the summary, explicitly note: (1) removePlayer was DEFERRED (live caller in processDisconnectedPlayers / 30s disconnectWatchdog) — not an oversight; (2) battle methods were confirmed retained as an explicit non-goal.
</output>
