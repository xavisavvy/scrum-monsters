---
phase: 52-client-god-component-decomposition
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/game/Lobby.tsx
  - client/src/lib/hooks/useLobbyMovement.test.ts
autonomous: true
requirements: [MAINT-11]
must_haves:
  truths:
    - "The 16ms Lobby movement interval is created exactly once per movement session — buff Set and jumpHeight changes no longer recreate it"
    - "Movement, freeze, petrify, fly, speed, size, and dead-player logic read from refs inside movePlayer, not from state closures"
    - "Movement behavior (position emit, afterimage, screen-shake) is unchanged"
  artifacts:
    - path: "client/src/components/game/Lobby.tsx"
      provides: "6 promoted buff/jump refs + collapsed movement dep array"
      contains: "jumpHeightRef"
    - path: "client/src/lib/hooks/useLobbyMovement.test.ts"
      provides: "fake-timers one-interval-per-session test for the Lobby movement loop"
      contains: "setInterval"
  key_links:
    - from: "movePlayer (Lobby.tsx ~L483-593)"
      to: "frozenPlayersRef / speedBuffsRef / sizeBuffsRef / deadPlayersRef / petrifiedPlayersRef / jumpHeightRef"
      via: "ref reads replacing state reads"
      pattern: "(frozenPlayersRef|speedBuffsRef|jumpHeightRef)\\.current"
    - from: "movement useEffect dep array (~L593)"
      to: "[keys, currentLobby?.gamePhase, emit, currentPlayer?.id]"
      via: "collapsed dependency array"
      pattern: "currentLobby\\?\\.gamePhase, emit, currentPlayer\\?\\.id\\]"
---

<objective>
MAINT-11 (Lobby half): the 16ms movement `useEffect` (Lobby.tsx ~L483–593) currently recreates the interval ~60 times per jump and on every spell-cast Set mutation because `jumpState.jumpHeight` and the buff Sets/Records are in its dep array. Promote 6 values to refs mirroring the existing `flyingPlayersRef` pattern (Lobby.tsx:258–267) and collapse the dep array to `[keys, currentLobby?.gamePhase, emit, currentPlayer?.id]`.

This plan touches ONLY `Lobby.tsx` (+ a new test). It is the FIRST link in the Lobby.tsx dependency chain (02 → 03 → 04) and MUST land before `useLobbyMovement` is extracted in Plan 04 (the hook needs these refs in place — RESEARCH Pitfall 5).

Purpose: eliminate the per-frame interval churn (GC pressure + missed-position-update jank) without altering the movement loop's observable behavior. This is a prerequisite for the reducer (Plan 03) and the hook extraction (Plan 04).

Output: 6 new refs + collapsed dep array in `Lobby.tsx`; new `useLobbyMovement.test.ts` proving one interval per movement session.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-client-god-component-decomposition/52-RESEARCH.md
@.planning/phases/52-client-god-component-decomposition/52-PATTERNS.md

<interfaces>
<!-- VERIFIED against live Lobby.tsx (2852 lines). -->
- L226: `const [deadPlayers, setDeadPlayers] = useState<Set<string>>(new Set());`
- L233: `const [frozenPlayers, setFrozenPlayers] = useState<Set<string>>(new Set());`
- L236: `const [petrifiedPlayers, setPetrifiedPlayers] = useState<Set<string>>(new Set());`
- L271: `const [speedBuffs, setSpeedBuffs] = useState<Record<string,{type:'haste'|'slow';stacks:number}>>({});`
- L276: `const [sizeBuffs, setSizeBuffs] = useState<Record<string,{type:'enlarge'|'reduce';stacks:number}>>({});`
- L190: `const [jumpState, setJumpState] = useState({...})` (jumpState.jumpHeight is the per-frame offender, ~60 recreations/jump)
- L258–267: EXISTING ref-mirror block (flyingPlayersRef, playerPositionsRef, myPositionRef, invisiblePlayersRef) — the canonical pattern to copy
- L483–593: movement useEffect; `setInterval(movePlayer, 16)` at L591
- Current dep array L593: `}, [keys, currentLobby?.gamePhase, emit, deadPlayers, speedBuffs, sizeBuffs, currentPlayer?.id, jumpState.jumpHeight, flyingPlayers, frozenPlayers, petrifiedPlayers];`
- L564 (afterimage in movement tick): reads `jumpState.jumpHeight` → becomes `jumpHeightRef.current`
- L621 (jump-arc afterimage): uses rAF-local `height` — DEBUNKED seam, LEAVE UNCHANGED
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the fake-timers one-interval-per-session test (Wave 0 gap)</name>
  <read_first>
    - client/src/components/game/Lobby.tsx (L483–593 movement effect, L591 setInterval)
    - ANALOG: client/src/components/game/PlayerCharacter.test.tsx L56–77 (vi.useFakeTimers() try/finally pattern) and L104–151 (TrackingWrapper)
    - 52-RESEARCH.md §1 (fake-timers test pattern: spy on global setInterval, trigger buff/jump change, assert called once)
  </read_first>
  <behavior>
    - Test: spy on `global.setInterval`; render Lobby in lobby phase with active movement; advance timers; trigger a buff Set change (e.g. add to frozenPlayers) and a jumpState.jumpHeight change; assert `setInterval` was called exactly ONCE (not 3+).
    - Test (negative baseline before refactor optional): document that pre-refactor the count is > 1; after refactor it is 1.
  </behavior>
  <action>
    Create `client/src/lib/hooks/useLobbyMovement.test.ts` (the file useLobbyMovement.ts will be extracted into in Plan 04 — placing the movement-interval test here now means Plan 04 inherits coverage). For THIS plan the test targets the Lobby movement loop via rendering Lobby (or a minimal harness that mounts the movement useEffect). Use `vi.useFakeTimers()` in a try/finally (PlayerCharacter.test.tsx L56–77 pattern), `vi.spyOn(global, 'setInterval')`, render with `gamePhase: 'lobby'` and a movement key in `keys`, then `act(() => { ... trigger frozenPlayers + jumpState.jumpHeight change ... })`, then `expect(setIntervalSpy).toHaveBeenCalledTimes(1)`. Mock R3F (`@react-three/fiber` Canvas, `@react-three/drei`) per 52-PATTERNS.md so happy-dom does not choke on WebGL. This test will initially FAIL against the unmodified movement loop (which recreates the interval) — that RED state proves it measures the right thing before Task 2 makes it GREEN.
  </action>
  <verify>
    <automated>npx vitest run client/src/lib/hooks/useLobbyMovement.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists and runs (RED before Task 2: asserts 1 interval while the current loop recreates it).
    - The test mocks R3F so it executes in happy-dom without WebGL errors.
    - The assertion is `toHaveBeenCalledTimes(1)` for the movement-session `setInterval`.
  </acceptance_criteria>
  <done>Fake-timers one-interval test exists, mocks R3F, and pins the interval-once contract (RED state expected pre-Task-2).</done>
</task>

<task type="auto">
  <name>Task 2: Promote 6 buff/jump values to refs + collapse the movement dep array (MAINT-11)</name>
  <read_first>
    - client/src/components/game/Lobby.tsx (L258–267 existing ref-mirror block — the exact pattern; L483–593 movement effect; L564 afterimage jumpHeight read; L593 dep array)
    - 52-PATTERNS.md (the 6 new refs verbatim block; the list of state reads to convert at L491/L510/L511/L544/L564/L579)
    - 52-RESEARCH.md §1 + Pitfall 1 (keep keys + currentLobby?.gamePhase; jumpState.jumpHeight is the worst offender)
  </read_first>
  <action>
    Immediately after the existing invisiblePlayersRef sync effect (~L267), add 6 refs following the L258–267 pattern (one useRef + one sync useEffect each): `jumpHeightRef = useRef(0)` synced to `jumpState.jumpHeight`; `frozenPlayersRef` synced to `frozenPlayers`; `petrifiedPlayersRef` synced to `petrifiedPlayers`; `speedBuffsRef` synced to `speedBuffs`; `sizeBuffsRef` synced to `sizeBuffs`; `deadPlayersRef` synced to `deadPlayers`. Inside `movePlayer` (L483–593) replace every direct state read with the ref read: `frozenPlayers.has(...)`→`frozenPlayersRef.current.has(...)`, `petrifiedPlayers.has(...)`→`petrifiedPlayersRef.current.has(...)`, `speedBuffs[id]`→`speedBuffsRef.current[id]`, `sizeBuffs[id]`→`sizeBuffsRef.current[id]`, `deadPlayers.has(...)`→`deadPlayersRef.current.has(...)`, and the L564 afterimage `const currentJumpHeight = jumpState.jumpHeight`→`const currentJumpHeight = jumpHeightRef.current`. Collapse the L593 dep array to exactly `[keys, currentLobby?.gamePhase, emit, currentPlayer?.id]`. Do NOT remove `keys` or `currentLobby?.gamePhase` (guards). Do NOT touch the L621 jump-arc afterimage (DEBUNKED — uses rAF-local `height`). Do NOT touch `flyHeight`, `invisibleFlicker`, or `screenShake` (Plan 03 scope / separate state). Per MAINT-11.
  </action>
  <verify>
    <automated>npx vitest run client/src/lib/hooks/useLobbyMovement.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Ref = useRef\|Ref.current =" client/src/components/game/Lobby.tsx` increases by the 6 new refs + 6 sync effects (jumpHeightRef, frozenPlayersRef, petrifiedPlayersRef, speedBuffsRef, sizeBuffsRef, deadPlayersRef present).
    - The movement dep array reads exactly `[keys, currentLobby?.gamePhase, emit, currentPlayer?.id]` — no buff Sets, no jumpState.jumpHeight.
    - `movePlayer` contains `frozenPlayersRef.current`, `speedBuffsRef.current`, `sizeBuffsRef.current`, `deadPlayersRef.current`, `jumpHeightRef.current` reads.
    - The fake-timers test now passes (`setInterval` called exactly once per session).
    - L621 jump-arc afterimage unchanged; `flyHeight`/`invisibleFlicker`/`screenShake` untouched.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>6 refs promoted, dep array collapsed, all movePlayer reads use refs, jump-arc afterimage and the 3 special-case states untouched, interval-once test green, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Suite invariant + perf guardrail note (PERF)</name>
  <read_first>
    - client/src/lib/hooks/useLobbyMovement.test.ts (the test from Task 1, now green)
    - 52-VALIDATION.md (sampling: full suite per wave; profiler is the manual gate for 60fps loops)
  </read_first>
  <action>
    Run the full suite + tsc + lint. Record a manual React DevTools profiler note in the SUMMARY: open the lobby in dev, record a 5s session with movement + a jump + a spell cast that mutates a buff Set, and confirm (a) the movement interval is created once (not per-frame), and (b) Lobby's render count during the session is ≤ the pre-refactor baseline. No new state was lifted; Canvas/dpr is out of scope for this plan (Plan 04 handles dpr).
  </action>
  <verify>
    <automated>npm test && npm run check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with ≥ 963 tests passing.
    - `npm run check` exits 0; `npm run lint` exits 0.
    - SUMMARY records the manual profiler confirmation: one interval per session + render count not increased during movement/jump/spell.
  </acceptance_criteria>
  <done>Full suite green, tsc + lint clean, profiler note recorded confirming the 60fps movement loop is untouched and interval churn is gone.</done>
</task>

</tasks>

<verification>
- `npm test` ≥ 963 passing; `npm run check` 0; `npm run lint` 0
- Lobby movement interval created once per session (fake-timers; survives buff Set + jumpHeight changes)
- Dep array collapsed to [keys, currentLobby?.gamePhase, emit, currentPlayer?.id]
- jump-arc afterimage (L621) and flyHeight/invisibleFlicker/screenShake untouched
</verification>

<success_criteria>
MAINT-11 (Lobby) complete: 6 buff/jump values promoted to refs mirroring the L258–267 convention; movement dep array collapsed; interval created once per movement session; movement behavior byte-identical; debunked afterimage seam left alone; suite + tsc + lint clean.
</success_criteria>

<output>
Create `.planning/phases/52-client-god-component-decomposition/52-02-SUMMARY.md` when done. Record the manual profiler confirmation for the 60fps movement loop.
</output>
