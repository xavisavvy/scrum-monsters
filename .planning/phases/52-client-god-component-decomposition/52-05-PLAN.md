---
phase: 52-client-god-component-decomposition
plan: 05
type: execute
wave: 4
depends_on: ["52-04"]
files_modified:
  - client/src/components/game/Lobby.tsx
  - client/src/lib/utils/applySpellEffects.ts
  - client/src/lib/hooks/useLobbyMovement.ts
  - client/src/lib/hooks/useLobbyMovement.test.ts
autonomous: true
requirements: [MAINT-13]
must_haves:
  truths:
    - "applySpellEffects + resolveTargets are the single source for both the remote and local spell paths (dedup complete; the dual-path divergence rides isLocalCast, not duplicated code)"
    - "useLobbyMovement encapsulates the 16ms movement interval + jump animation, extracted LAST after the MAINT-11 refs are in place"
    - "The extracted hook creates exactly one interval per movement session (the collapsed dep array travels into the hook)"
    - "Movement, spell, and afterimage behavior is byte-identical; debunked seams remain untouched"
  artifacts:
    - path: "client/src/lib/hooks/useLobbyMovement.ts"
      provides: "side-effect hook wrapping the movement + jump effects, refs passed as props"
      contains: "useLobbyMovement"
    - path: "client/src/lib/hooks/useLobbyMovement.test.ts"
      provides: "one-interval-per-session test now targeting the extracted hook directly"
      contains: "setInterval"
  key_links:
    - from: "Lobby.tsx"
      to: "useLobbyMovement"
      via: "hook invocation passing the MAINT-11 refs + setters"
      pattern: "useLobbyMovement\\("
    - from: "useLobbyMovement movement effect"
      to: "collapsed dep array"
      via: "[keys, gamePhase, emit, currentPlayerId] with refs as stable props"
      pattern: "gamePhase, emit, currentPlayerId\\]"
---

<objective>
MAINT-13 (seams 4–5, the final seams): finish deduplicating the spell paths so `applySpellEffects` + `resolveTargets` are the ONLY implementation (reusing the pure utils from Plan 03), then extract `useLobbyMovement` — the LAST seam of the phase. The hook wraps the 16ms movement interval and jump animation; it MUST be extracted after the MAINT-11 refs (Plan 02) and reducer (Plan 03) are in place, or the dep-array collapse is meaningless (RESEARCH Pitfall 5).

This plan touches `Lobby.tsx` and depends_on Plan 04 (strict chain 02 → 03 → 04 → 05). `useLobbyMovement` is the final task of this plan and of the phase.

Purpose: complete the verified-seam extraction set, leaving Lobby.tsx materially smaller with movement and spell logic in testable modules, while keeping the 60fps loop behavior identical.

Output: `applySpellEffects.ts` confirmed as the single spell path (dedup verified); new `useLobbyMovement.ts` (hook) with its interval-once test migrated onto the extracted hook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-client-god-component-decomposition/52-RESEARCH.md
@.planning/phases/52-client-god-component-decomposition/52-PATTERNS.md
@.planning/phases/52-client-god-component-decomposition/52-03-SUMMARY.md
@.planning/phases/52-client-god-component-decomposition/52-04-SUMMARY.md

<interfaces>
<!-- VERIFIED. Plan 03 created applySpellEffects.ts/resolveTargets and wired both cascades to call it.
     Plan 02 promoted jumpHeightRef/frozenPlayersRef/petrifiedPlayersRef/speedBuffsRef/sizeBuffsRef/deadPlayersRef
     and collapsed the movement dep array to [keys, currentLobby?.gamePhase, emit, currentPlayer?.id]. -->
- Movement useEffect: Lobby.tsx L483–593 (post Plan-02 collapse); jump animation useEffect L596–648; keyboard handlers L386–459
- Refs available (Plan 02): jumpHeightRef, frozenPlayersRef, petrifiedPlayersRef, flyingPlayersRef, speedBuffsRef, sizeBuffsRef, deadPlayersRef, playerPositionsRef, myPositionRef, invisiblePlayersRef
- Setters used by movement: setMyPosition, setFlyHeight, setAfterimages, setScreenShake
- useLobbyMovement target shape: 52-PATTERNS.md L656–728 (UseLobbyMovementProps; pure side-effect hook, no return; dep array [keys, gamePhase, emit, currentPlayerId]; refs passed as stable props)
- ANALOG: useViewport.ts L43–165 (canonical custom-hook structure: state → refs → effects → useCallback → return); usePhaseInterstitial.ts
- DEBUNKED (leave inline / untouched): afterimage L565 (movement tick, reads jumpHeightRef.current) vs L621 (jump-arc rAF, reads rAF-local height); unified emote hook
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm + finish applySpellEffects/resolveTargets dedup (seam 4)</name>
  <read_first>
    - client/src/lib/utils/applySpellEffects.ts (created in Plan 03 — resolveTargets, buildAction, applySpellEffects)
    - client/src/components/game/Lobby.tsx (handleLobbyEmote remote path; handleEmoteSubmit local path — confirm both now call applySpellEffects after Plan 03)
    - 52-RESEARCH.md §2 + Seam Audit row 4 (resolveTargets at L817 remote / L1310 local were the duplicates; the dedup target is applySpellEffects.ts)
    - 52-PATTERNS.md L305–367 (the pure-function dedup)
  </read_first>
  <action>
    Verify Plan 03 left both spell paths routing through `applySpellEffects(...)` and that NO inline `resolveTargets` closure remains in Lobby.tsx (the remote L817 and local L1310 copies must both be gone, replaced by the imported pure `resolveTargets`). If any inline target-resolution or per-effect dispatch logic remains duplicated in Lobby, move it into `applySpellEffects.ts`/`buildAction` so the dual path is expressed ONLY by the `isLocalCast` flag (RESEARCH: the dual-path is deliberate but must not be duplicated CODE — it is one function parameterized by isLocalCast). Do NOT merge the two CALL SITES into one handler (the unified-emote-hook seam is DEBUNKED) — handleLobbyEmote and handleEmoteSubmit stay as distinct entry points that each call applySpellEffects with their respective isLocalCast value. Per MAINT-13 seam 4.
  </action>
  <verify>
    <automated>npx vitest run client/src/lib/utils/applySpellEffects.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const resolveTargets" client/src/components/game/Lobby.tsx` equals 0 (no inline resolveTargets closure — only the imported pure fn is used).
    - Both handleLobbyEmote and handleEmoteSubmit still exist as distinct functions (unified-emote-hook stays DEBUNKED) and each calls `applySpellEffects(...)` with isLocalCast false/true respectively.
    - applySpellEffects.test.ts still green.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>resolveTargets/applySpellEffects is the single spell implementation; the dual-path is expressed only via isLocalCast; the two call sites remain distinct; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extract useLobbyMovement (seam 5, LAST) + migrate interval-once test</name>
  <read_first>
    - client/src/components/game/Lobby.tsx (movement useEffect L483–593; jump animation L596–648; keyboard handlers L386–459; the MAINT-11 refs from Plan 02)
    - 52-PATTERNS.md L656–728 (UseLobbyMovementProps interface; pure side-effect hook; dep array [keys, gamePhase, emit, currentPlayerId]; refs as props; CRITICAL PREREQUISITE note)
    - 52-RESEARCH.md Pitfall 5 (extract LAST — refs must be in place) and §1 (collapsed dep array)
    - ANALOG: useViewport.ts L43–165 (custom hook structure)
    - client/src/lib/hooks/useLobbyMovement.test.ts (the test created in Plan 02 — now retarget it at the extracted hook)
  </read_first>
  <behavior>
    - Test: invoking useLobbyMovement with active movement keys creates exactly ONE interval per session; changing a buff ref's underlying state or jumpHeight does NOT recreate the interval (the refs are stable MutableRefObjects, not deps).
    - Test: the hook emits position updates and drives afterimage/screen-shake side effects identically to the pre-extraction inline effect.
  </behavior>
  <action>
    Create `client/src/lib/hooks/useLobbyMovement.ts` exporting `useLobbyMovement(props: UseLobbyMovementProps)` per 52-PATTERNS.md L656–728: a pure side-effect hook (no return) that contains the 16ms movement `useEffect` (L483–593) and the jump animation `useEffect` (L596–648). Accept the MAINT-11 refs (jumpHeightRef, frozenPlayersRef, petrifiedPlayersRef, flyingPlayersRef, speedBuffsRef, sizeBuffsRef, deadPlayersRef, plus playerPositionsRef/myPositionRef as needed), the setters (setMyPosition, setFlyHeight, setAfterimages, setScreenShake), and `keys`/`gamePhase`/`emit`/`currentPlayerId`/`characterSize`/`moveSpeed`/`movementAreaRef`/`jumpState` as props. The movement effect's dep array inside the hook is exactly `[keys, gamePhase, emit, currentPlayerId]` — refs are stable props, not deps (52-PATTERNS.md L724–727). Move the movement + jump effects out of Lobby.tsx and replace with a single `useLobbyMovement({ ... })` call passing the refs/setters/values. Leave the keyboard handlers and the DEBUNKED afterimage triggers intact (movement-tick afterimage reads jumpHeightRef.current; jump-arc afterimage reads the rAF-local height — do NOT merge). Migrate the Plan-02 interval-once test in `useLobbyMovement.test.ts` to render/invoke the extracted hook directly (renderHook or a minimal host) and assert `setInterval` called once across a session with buff/jumpHeight changes. EXTRACT LAST — this is the final task of the phase (Pitfall 5: refs from Plan 02 must already be in place). Per MAINT-13 seam 5.
  </action>
  <verify>
    <automated>npx vitest run client/src/lib/hooks/useLobbyMovement.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - useLobbyMovement.ts exists as a pure side-effect hook (no return); `grep -c "useLobbyMovement(" client/src/components/game/Lobby.tsx` equals 1 (single call site).
    - The movement + jump useEffects no longer appear inline in Lobby.tsx (moved into the hook).
    - The hook's movement dep array is `[keys, gamePhase, emit, currentPlayerId]`; refs are passed as props (not in the dep array).
    - Interval-once test (now against the extracted hook) is green: `setInterval` called exactly once per session despite buff/jumpHeight changes.
    - DEBUNKED afterimage dual-trigger preserved (both setAfterimages call sites intact, different data sources).
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>useLobbyMovement extracted LAST with the collapsed dep array; refs passed as stable props; interval-once test green against the hook; debunked afterimage triggers untouched; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-final suite invariant + perf guardrail + debunked-seams audit (PERF)</name>
  <read_first>
    - 52-VALIDATION.md (phase gate: full suite green + tsc + lint; profiler is the manual 60fps gate)
    - 52-PATTERNS.md "Debunked Seams (Leave Alone)" + "Perf Acceptance Criteria" tables
  </read_first>
  <action>
    Run the full suite + tsc + lint as the phase gate. Verify the perf guardrails hold end-to-end: `grep 'const \[dpr' client/src/components/game/Lobby.tsx` returns 0; TavernScene render-count test green; both movement interval-once tests (Lobby hook + PlayerController) green. Audit that all DEBUNKED seams remain inline and unmodified: unified emote hook (handleLobbyEmote + handleEmoteSubmit both still present as distinct functions), descriptor settings form (not abstracted), afterimage dual-trigger (both setAfterimages sites present with distinct data sources). Record the final manual React DevTools profiler confirmation in the SUMMARY: a 5s lobby session (movement + jump + spell cast + a dpr decline trigger) shows (a) one movement interval, (b) TavernScene 0–1 renders, (c) Lobby render count ≤ pre-phase baseline — confirming the 60fps loops are untouched.
  </action>
  <verify>
    <automated>npm test && npm run check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` ≥ 963 passing; `npm run check` 0; `npm run lint` 0.
    - `grep -c "const \[dpr" client/src/components/game/Lobby.tsx` equals 0.
    - DEBUNKED audit: `grep -c "handleLobbyEmote\|handleEmoteSubmit" Lobby.tsx` ≥ 2 (both distinct); `grep -c "setAfterimages" Lobby.tsx` ≥ 2 (dual-trigger intact); descriptor settings form still inline JSX.
    - SUMMARY records the final profiler confirmation (one interval, TavernScene isolated, render counts not increased) for the 60fps loops.
  </acceptance_criteria>
  <done>Phase gate green; perf guardrails verified; all debunked seams confirmed untouched; final profiler note recorded.</done>
</task>

</tasks>

<verification>
- `npm test` ≥ 963 passing; `npm run check` 0; `npm run lint` 0
- applySpellEffects/resolveTargets is the single spell path (no inline resolveTargets in Lobby)
- useLobbyMovement extracted LAST; interval created once per session
- Debunked seams (unified emote hook, descriptor form, afterimage dual-trigger) all confirmed inline + untouched
- dpr still absent from Lobby; perf guardrails green end-to-end
</verification>

<success_criteria>
MAINT-13 seams 4–5 complete: spell paths deduped to one pure applySpellEffects/resolveTargets (dual-path via isLocalCast only); useLobbyMovement extracted LAST with the collapsed dep array and refs-as-props; interval created once per session; the 60fps loops behave identically; all debunked seams left alone; phase gate (suite + tsc + lint) green; perf guardrail confirmed.
</success_criteria>

<output>
Create `.planning/phases/52-client-god-component-decomposition/52-05-SUMMARY.md` when done. Record the final phase-level profiler confirmation for the 60fps loops.
</output>
