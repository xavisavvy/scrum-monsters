---
phase: 52-client-god-component-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/game/PlayerController.tsx
  - client/src/components/game/PlayerController.test.tsx
autonomous: true
requirements: [MAINT-11, MAINT-14]
must_haves:
  truths:
    - "PlayerController movement interval is created exactly once per movement session (turning no longer recreates it)"
    - "All three Ctrl-shoot sites produce byte-identical emit payloads through one handleShootAtTarget helper"
    - "Both cooldown tickers behave identically through one startCooldown helper"
    - "PlayerController re-render count did not increase versus pre-refactor"
  artifacts:
    - path: "client/src/components/game/PlayerController.tsx"
      provides: "currentDirectionRef, handleShootAtTarget, startCooldown; collapsed movement dep array"
      contains: "currentDirectionRef"
    - path: "client/src/components/game/PlayerController.test.tsx"
      provides: "fake-timers one-interval test + handleShootAtTarget/startCooldown equivalence + render-count guardrail"
      contains: "setInterval"
  key_links:
    - from: "movePlayer closure (PlayerController.tsx ~L419)"
      to: "currentDirectionRef.current"
      via: "ref read replacing currentDirection state read"
      pattern: "currentDirectionRef\\.current"
    - from: "three Ctrl-shoot sites (~L129, ~L730, ~L941)"
      to: "handleShootAtTarget"
      via: "useCallback invocation with mode 'projectile' | 'direct'"
      pattern: "handleShootAtTarget\\("
---

<objective>
MAINT-11 (PlayerController half) + MAINT-14: stop the 16ms movement interval from recreating when the player turns by promoting `currentDirection` to a ref and collapsing the movement dep array; deduplicate the three verbatim Ctrl-shoot blocks into one `handleShootAtTarget` and the two identical cooldown tickers into one `startCooldown`.

This plan touches ONLY `PlayerController.tsx` (+ its new test). It shares no file with the Lobby stream and runs in parallel with Plan 02 (Wave 1).

Purpose: PlayerController is one of the two god-components; this is the file-independent slice of the phase, removing per-turn interval churn and ~190 lines of triplicated shoot logic without changing the 60fps loop's behavior.

Output: Refactored `PlayerController.tsx` (post-51 baseline, 1191 lines) + new `PlayerController.test.tsx` covering the interval-once guarantee, shoot-emit equivalence, cooldown equivalence, and a render-count guardrail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-client-god-component-decomposition/52-RESEARCH.md
@.planning/phases/52-client-god-component-decomposition/52-PATTERNS.md

<interfaces>
<!-- Post-51 anchors verified against live PlayerController.tsx (1191 lines). Use directly; no exploration needed. -->

PlayerController.tsx (post-51, VERIFIED):
- L12: `import { useViewport, worldToPercent, percentToWorld } from '@/lib/hooks/useViewport';` — DO NOT re-add
- L25: `export const PlayerController = React.memo(function PlayerController(...)` — DO NOT re-wrap
- L57: `const [currentDirection, setCurrentDirection] = useState<SpriteDirection>('down');`
- L88: keyboard `handleKeyDown` useEffect; Ctrl-shoot Site 1 at L129–193; cooldown ticker Site 1 at L205–215
- L412–490: movement `useEffect`; `movePlayer` reads `currentDirection` at L419; `setInterval(movePlayer, 16)` at L488; dep array L490: `}, [keys, viewport, characterSize, moveSpeed, emit, currentDirection];`
- L716–724: cooldown ticker Site 2 (inside `handleMobileKeyDown`)
- L730–780: Ctrl-shoot Site 2 (mobile `handleMobileKeyDown`, `code === 'ControlLeft'`) — emits `player_projectile`
- L941–993: Ctrl-shoot Site 3 (inline container `onKeyDown` prop) — emits `attack_player`/`attack_boss` (battle-phase variant, NOT player_projectile)
- Phase 49: `currentPlayer`/`currentLobby` via `useShallow` — DO NOT change selector shape
- Phase 51 (MAINT-10): all coordinate conversions already use `worldToPercent` — DO NOT re-implement the math
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: currentDirection ref-mirror + collapse movement dep array (MAINT-11)</name>
  <read_first>
    - client/src/components/game/PlayerController.tsx (L57 currentDirection state; L412–490 movement effect; L419 closure read; L490 dep array)
    - ANALOG: client/src/components/game/Lobby.tsx L258–267 (flyingPlayersRef ref-mirror — one useRef + one useEffect per promoted value, declared right after the state)
  </read_first>
  <behavior>
    - Test: with active movement keys, advancing fake timers and then changing currentDirection (turning) results in `setInterval` having been called exactly ONCE (interval not recreated on turn).
    - Test: movement still emits position updates (the loop is not broken by removing currentDirection from the dep array).
  </behavior>
  <action>
    Add `const currentDirectionRef = useRef<SpriteDirection>('down');` immediately after the L57 `currentDirection` useState, followed by `useEffect(() => { currentDirectionRef.current = currentDirection; }, [currentDirection]);` (mirrors the Lobby.tsx:258-267 pattern exactly). Inside `movePlayer` at L419, change `let direction: SpriteDirection = currentDirection;` to `let direction: SpriteDirection = currentDirectionRef.current;`. Collapse the L490 dep array from `[keys, viewport, characterSize, moveSpeed, emit, currentDirection]` to `[keys, viewport, characterSize, moveSpeed, emit]` — remove ONLY `currentDirection`. Per MAINT-11 / RESEARCH §1 PlayerController. Do NOT touch the rAF jump physics or any other effect. Do NOT remove `keys`, `viewport`, `characterSize`, `moveSpeed`, or `emit` (Pitfall 1).
  </action>
  <verify>
    <automated>npx vitest run client/src/components/game/PlayerController.test.tsx -t "interval" && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "currentDirectionRef" client/src/components/game/PlayerController.tsx` shows the ref declaration, the sync useEffect, and the closure read at ~L419.
    - The L490 dep array no longer contains `currentDirection` (and still contains keys, viewport, characterSize, moveSpeed, emit).
    - Fake-timers test asserts `setInterval` spy called exactly 1 time across a movement session that includes a `currentDirection` change.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>currentDirection is a ref read inside movePlayer; the movement interval is created once per session and survives turns; type check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract handleShootAtTarget + startCooldown (MAINT-14)</name>
  <read_first>
    - client/src/components/game/PlayerController.tsx (Ctrl-shoot Sites L129–193, L730–780, L941–993; cooldown tickers L205–215, L716–724)
    - 52-PATTERNS.md (handleShootAtTarget signature with `mode: 'projectile' | 'direct'`; startCooldown signature)
    - 52-RESEARCH.md §3 (Site 1/2 emit `player_projectile`; Site 3 emits `attack_player`/`attack_boss` — the `direct` mode branch; Pitfall 6: call worldToPercent, do NOT re-implement math)
  </read_first>
  <behavior>
    - Test: handleShootAtTarget(targetX, targetY, targetPlayerId, 'projectile') pushes a projectile and emits `player_projectile` with worldToPercent-converted start/target coords and the player's projectile emoji (matches the pre-refactor Site 1/2 payload).
    - Test: handleShootAtTarget(..., 'direct') with spectator team + targetPlayerId emits `attack_player`; otherwise emits `attack_boss` (matches the pre-refactor Site 3 payload).
    - Test: startCooldown sets cooldown to 5000 and decrements by 100 every 100ms to 0 (fake timers), identical to both inline tickers.
  </behavior>
  <action>
    Create a `handleShootAtTarget = useCallback((targetX: number, targetY: number, targetPlayerId: string | null, mode: 'projectile' | 'direct') => {...}, [playerPosition, characterSize, viewport, currentPlayer, emit])` per the 52-PATTERNS.md signature: compute characterCenterX/Y, build the `Projectile` with `getProjectileEmoji(currentPlayer!.avatar)`, `setProjectiles(prev => [...prev, newProjectile])`; in `'projectile'` mode call `viewport.screenToWorld` + `worldToPercent` (the helper already imported at L12 — Pitfall 6) and `emit('player_projectile', {...})`; in `'direct'` mode emit `attack_player` (spectator + targetPlayerId) or `attack_boss` (`damage: Math.floor(Math.random()*3)+1`). Replace the body of all three Ctrl-shoot sites (L129–193, L730–780, L941–993) with target-resolution + a single `handleShootAtTarget(...)` call passing `mode='projectile'` for Sites 1/2 and `mode='direct'` for Site 3 — preserving each site's existing target-resolution (spectator → `findNearestTargetPlayer`, else boss coords). Create `startCooldown = useCallback(() => { setSpecialAttackCooldown(5000); const cooldownInterval = setInterval(() => setSpecialAttackCooldown(prev => prev <= 100 ? (clearInterval(cooldownInterval), 0) : prev - 100), 100); }, [])` and replace both inline ticker blocks (L205–215, L716–724) with `startCooldown()`. Do NOT merge the projectile/direct emit paths (RESEARCH §3 — Site 3 is the battle-phase variant).
  </action>
  <verify>
    <automated>npx vitest run client/src/components/game/PlayerController.test.tsx && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "handleShootAtTarget(" client/src/components/game/PlayerController.tsx` ≥ 4 (1 definition + 3 call sites).
    - `grep -c "startCooldown()" client/src/components/game/PlayerController.tsx` ≥ 2 call sites; the inline `setSpecialAttackCooldown(5000)` ticker blocks no longer appear twice — `grep -v '^#' client/src/components/game/PlayerController.tsx | grep -c "setSpecialAttackCooldown(5000)"` equals 1 (only inside startCooldown).
    - Tests assert projectile-mode emits `player_projectile` and direct-mode emits `attack_player`/`attack_boss`, matching pre-refactor payloads.
    - `npm run check` exits 0, `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Three Ctrl-shoot sites route through handleShootAtTarget; both tickers route through startCooldown; projectile vs direct branching preserved; emits byte-identical to pre-refactor.</done>
</task>

<task type="auto">
  <name>Task 3: Suite invariant + render-count perf guardrail (PERF)</name>
  <read_first>
    - client/src/components/game/PlayerController.test.tsx (the file created in Tasks 1–2)
    - ANALOG: client/src/components/game/PlayerCharacter.test.tsx L104–151 (TrackingWrapper render-count via mirror Zustand subscription)
    - 52-PATTERNS.md (Vitest mock pattern for WebGL/image deps; fake-timers pattern)
  </read_first>
  <action>
    Add a TrackingWrapper render-count test to PlayerController.test.tsx mirroring PlayerCharacter.test.tsx L109–151: the wrapper subscribes to the same Phase-49 `useShallow` fields PlayerController reads (`currentPlayer` {id,team,avatar,name}, `currentLobby` {id,gamePhase,players,playerPositions,playerCombatStates}); mutate an UNSUBSCRIBED field (e.g. `currentLobby.boss.currentHealth` — boss is intentionally excluded from the selector per Phase 49) and assert PlayerController's render count does NOT increase (React.memo + scoped selector bail-out). Record a manual React DevTools profiler note in the SUMMARY: record a 5s movement+turn session, confirm the movement interval is created once and PlayerController re-render count ≤ pre-refactor baseline. Run the full suite + tsc + lint as the plan's final gate.
  </action>
  <verify>
    <automated>npm test && npm run check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Render-count test: mutating `currentLobby.boss.currentHealth` (unsubscribed field) leaves PlayerController renderCount unchanged after mount.
    - `npm test` exits 0 with ≥ 963 tests passing (baseline + new PlayerController tests).
    - `npm run check` exits 0; `npm run lint` exits 0.
    - SUMMARY records the manual profiler confirmation (interval created once; render count not increased) for the 60fps loop.
  </acceptance_criteria>
  <done>Full suite green, tsc + lint clean, render-count guardrail proven, profiler note recorded.</done>
</task>

</tasks>

<verification>
- `npm test` ≥ 963 passing; `npm run check` 0; `npm run lint` 0
- PlayerController movement interval created once per session (fake-timers, survives turns)
- handleShootAtTarget covers all 3 sites with projectile/direct branching preserved
- startCooldown covers both tickers
- Render-count guardrail: PlayerController does not re-render on unsubscribed (boss) field changes
</verification>

<success_criteria>
MAINT-11 (PlayerController) and MAINT-14 complete: currentDirection promoted to ref + dep array collapsed; Ctrl-shoot deduped to handleShootAtTarget; cooldown tickers deduped to startCooldown; behavior byte-identical; perf guardrail green; suite + tsc + lint clean.
</success_criteria>

<output>
Create `.planning/phases/52-client-god-component-decomposition/52-01-SUMMARY.md` when done. Record the manual profiler confirmation for the 60fps movement loop.
</output>
