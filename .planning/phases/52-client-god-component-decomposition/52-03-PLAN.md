---
phase: 52-client-god-component-decomposition
plan: 03
type: execute
wave: 2
depends_on: ["52-02"]
files_modified:
  - client/src/components/game/Lobby.tsx
  - client/src/lib/reducers/buffReducer.ts
  - client/src/lib/reducers/buffReducer.test.ts
  - client/src/lib/utils/applySpellEffects.ts
  - client/src/lib/utils/applySpellEffects.test.ts
autonomous: true
requirements: [MAINT-12]
must_haves:
  truths:
    - "10 of the 13 magic-effect useState slots are replaced by one useReducer; flyHeight, invisibleFlicker, screenShake remain separate useState"
    - "The ~300-line if-cascade in both handleLobbyEmote and handleEmoteSubmit becomes detectedEffects.forEach(e => dispatch(buildAction(e, resolveTargets(e), ...)))"
    - "DISPEL_ALL is a single action that clears all buff state (replaces the 7-setter dispel)"
    - "The isLocalCast divergence is preserved: local cast calls setFlyHeight(0) on earthbind/dispel-self; remote cast does not"
    - "buffReducer is a pure module with no React imports, unit-tested"
  artifacts:
    - path: "client/src/lib/reducers/buffReducer.ts"
      provides: "BuffState, BuffAction, initialBuffState, buffReducer (pure)"
      contains: "DISPEL_ALL"
    - path: "client/src/lib/reducers/buffReducer.test.ts"
      provides: "reducer unit tests incl. DISPEL_ALL, multi-effect composition, stacking caps"
      contains: "DISPEL_ALL"
    - path: "client/src/lib/utils/applySpellEffects.ts"
      provides: "pure resolveTargets + applySpellEffects (forEach dispatch with isLocalCast)"
      contains: "resolveTargets"
    - path: "client/src/components/game/Lobby.tsx"
      provides: "useReducer wiring; both cascades migrated to forEach dispatch"
      contains: "useReducer(buffReducer"
  key_links:
    - from: "Lobby.tsx buff state"
      to: "buffReducer via useReducer"
      via: "const [buffState, dispatch] = useReducer(buffReducer, initialBuffState); destructure 10 slots"
      pattern: "useReducer\\(buffReducer"
    - from: "handleLobbyEmote (remote) + handleEmoteSubmit (local)"
      to: "applySpellEffects/dispatch"
      via: "detectedEffects.forEach with isLocalCast threaded"
      pattern: "isLocalCast"
---

<objective>
MAINT-12: collapse 10 of the 13 magic-effect `useState` slots into ONE `useReducer` backed by a pure `buffReducer.ts`, and replace the ~300-line if-cascade (duplicated in `handleLobbyEmote` remote path and `handleEmoteSubmit` local path) with `detectedEffects.forEach(e => dispatch(buildAction(e, resolveTargets(e), ...)))`. `DISPEL_ALL` becomes one action closing the 7-setter dispel race. The 3 special-case slots (`flyHeight`, `invisibleFlicker`, `screenShake`) stay as separate `useState`.

This plan touches `Lobby.tsx` and depends_on Plan 02 (same file — strict chain 02 → 03 → 04). It must merge after Plan 02's refs are in place.

Purpose: make spell effects unit-testable (pure reducer + pure resolveTargets) and eliminate the async-socket-handler batching gap that the 7-setter dispel suffers from.

Output: new `buffReducer.ts` (+ test), new `applySpellEffects.ts` (+ test), and Lobby.tsx wired to the reducer with both cascades migrated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-client-god-component-decomposition/52-RESEARCH.md
@.planning/phases/52-client-god-component-decomposition/52-PATTERNS.md
@.planning/phases/52-client-god-component-decomposition/52-02-SUMMARY.md

<interfaces>
<!-- VERIFIED against live Lobby.tsx. The 10 reducer slots + 3 that stay separate. -->
Reducer slots (10): deadPlayers (L226), flyingPlayers (L229), frozenPlayers (L233), petrifiedPlayers (L236), tavernDarkMode (L239), chaosMode (L242), invisiblePlayers (L245), dragonAttack (L250), speedBuffs (L271), sizeBuffs (L276).
STAY SEPARATE useState (do NOT put in reducer): flyHeight (L230, continuous 16ms), invisibleFlicker (L247, 2s timer L352–371), screenShake (L279, movement tick L580–583).
Cascade locations: handleLobbyEmote (remote) resolveTargets L817, if-cascade L840–1096; handleEmoteSubmit (local) resolveTargets L1310, if-cascade L1332–1620.
isLocalCast divergence (RESEARCH §2 + PATTERNS): local path calls setFlyHeight(0) on earthbind/dispel when targetId === currentPlayer.id (reads myPosition.x); remote path does NOT (reads playerPositionsRef.current). flyingPlayersRef/invisiblePlayersRef already exist (L258–267).
BuffState/BuffAction shape: see 52-PATTERNS.md L188–224 and 52-RESEARCH.md §2 (full union, 23 action types incl. DISPEL_ALL, PHASE_RESET).
NOTE: Plan 02 promoted frozenPlayers/speedBuffs/sizeBuffs/deadPlayers/petrifiedPlayers to refs synced from these state values. After this plan replaces them with reducer-destructured values, the Plan-02 sync effects must point at the destructured buffState fields (same variable names, so the sync `useEffect(() => { frozenPlayersRef.current = frozenPlayers; }, [frozenPlayers])` keeps working — verify the deps still reference the destructured names).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pure buffReducer.ts + unit tests (MAINT-12, Wave 0 gap)</name>
  <read_first>
    - 52-PATTERNS.md L180–301 (buffReducer target shape: BuffState, BuffAction union, initialBuffState, switch cases; the test shape with DISPEL_ALL + multi-effect composition)
    - 52-RESEARCH.md §2 (the 10 slots, the action union, Pitfall 2 multi-effect ordering: die→revive leaves player alive; enlarge×3 caps at stacks 3)
    - ANALOG: client/src/components/game/PlayerCharacter.test.tsx L1–6 (Vitest import structure; NO React needed — pure function)
    - client/src/components/game/Lobby.tsx L840–1096 (the current if-cascade — source of each effect's exact state mutation, e.g. enlarge stacking math, frozen auto-unfreeze, dragon target shape)
  </read_first>
  <behavior>
    - DISPEL_ALL: a state with populated deadPlayers/flyingPlayers/speedBuffs returns initialBuffState (all cleared) in one call.
    - PHASE_RESET: identical clear to DISPEL_ALL.
    - Multi-effect composition: buffReducer(buffReducer(initial, DIE p1), REVIVE p1) leaves p1 NOT in deadPlayers.
    - Stacking caps: three ENLARGE on the same target caps stacks at 3 (matches current cascade math); REDUCE/HASTE/SLOW stacking matches current behavior.
    - HOLD adds to frozenPlayers; UNHOLD removes; PETRIFY/INVISIBILITY/FLY mutate their respective Sets; EARTHBIND clears flyingPlayers (lobby-wide); MASSACRE/MASSREVIVE/DRAGON/DRAGON_END/CHAOS/CHAOS_END/TAVERN_DARK_END/BREAK_INVISIBILITY transitions match the cascade.
  </behavior>
  <action>
    Create `client/src/lib/reducers/buffReducer.ts` as a PURE module (no React imports) exporting `BuffState`, `BuffAction` (the full 23-variant union from 52-PATTERNS.md L201–224), `initialBuffState`, and `buffReducer(state, action)`. Each case must reproduce the EXACT state mutation from the current if-cascade (read Lobby.tsx L840–1096 for the precise per-effect math: deadPlayers add/delete, frozenPlayers add + the 5s auto-unhold is a side-effect NOT in the reducer, speedBuffs/sizeBuffs stacking with caps, dragonAttack object shape, tavernDarkMode/chaosMode booleans, invisiblePlayers add/delete). `DISPEL_ALL` and `PHASE_RESET` both `return { ...initialBuffState }` with fresh Sets/objects. Use immutable updates (`new Set(state.x)`, spread Records) so React bails correctly. Create `client/src/lib/reducers/buffReducer.test.ts` (no React import) covering DISPEL_ALL, PHASE_RESET, die→revive composition, enlarge×3 cap, HOLD/UNHOLD, and EARTHBIND clearing flyers. Per MAINT-12.
  </action>
  <verify>
    <automated>npx vitest run client/src/lib/reducers/buffReducer.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -L "from 'react'\|from \"react\"" client/src/lib/reducers/buffReducer.ts` confirms NO React import (pure module).
    - Reducer exports BuffState, BuffAction, initialBuffState, buffReducer; BuffAction includes DISPEL_ALL and PHASE_RESET.
    - Tests prove: DISPEL_ALL clears all 10 slots in one dispatch; die→revive leaves player alive; enlarge×3 caps at stacks 3; EARTHBIND empties flyingPlayers.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>Pure buffReducer with full action union and exact per-effect mutations; unit tests green incl. DISPEL_ALL and multi-effect composition; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create pure applySpellEffects.ts + resolveTargets + tests (MAINT-12)</name>
  <read_first>
    - 52-PATTERNS.md L305–367 (applySpellEffects target shape; resolveTargets pure function; isLocalCast earthbind/dispel setFlyHeight(0) branch)
    - 52-RESEARCH.md §2 ("Key difference between the two cascade copies" — the isLocalCast distinction)
    - client/src/components/game/Lobby.tsx L817–837 (remote resolveTargets) and L1310–1329 (local resolveTargets) — confirm they are identical so one pure fn covers both
  </read_first>
  <behavior>
    - resolveTargets: with a message naming a matching player, returns that player's id; with no match, returns [casterPlayerId] (fallback); case-insensitive name match.
    - applySpellEffects: for each detected effect, dispatches buildAction(effect, resolveTargets(effect), caster, isLocalCast); when isLocalCast && effect is earthbind/dispel && caster in targets → calls setFlyHeight(0); when !isLocalCast → never calls setFlyHeight.
    - buildAction maps a MagicEffectType + targets (+ caster/isLocalCast) to the correct BuffAction variant.
  </behavior>
  <action>
    Create `client/src/lib/utils/applySpellEffects.ts` (pure — no hooks): export `resolveTargets(effectType, message, casterPlayerId, lobbyPlayers): string[]` mirroring Lobby.tsx L817–837 (getSpellWords → extractSpellTargets → name→id map → caster fallback); export `buildAction(effect, targets, caster, isLocalCast): BuffAction` mapping each MagicEffectType to its action variant (DIE/REVIVE/HASTE/SLOW/ENLARGE/REDUCE/FLY/HOLD/PETRIFY/INVISIBILITY/EARTHBIND/MASSACRE/DRAGON/CHAOS/DISPEL/etc.); export `applySpellEffects(detectedEffects, message, casterPlayerId, lobbyPlayers, isLocalCast, dispatch, setFlyHeight, setMagicEffects, setEmotes)` that runs the forEach dispatch and applies the `isLocalCast` setFlyHeight(0) branch for earthbind/dispel-self. Thread `isLocalCast: boolean` — do NOT elide it (RESEARCH §2; this is the ONE real divergence between the two cascades). Create `applySpellEffects.test.ts` (pure, mock dispatch/setFlyHeight as vi.fn()) asserting resolveTargets fallback + the isLocalCast vs remote setFlyHeight divergence. Per MAINT-12.
  </action>
  <verify>
    <automated>npx vitest run client/src/lib/utils/applySpellEffects.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - resolveTargets test: named-match returns the matched id; no-match returns [casterPlayerId].
    - applySpellEffects test: isLocalCast=true + earthbind targeting self → setFlyHeight called with 0; isLocalCast=false → setFlyHeight NOT called.
    - `grep -c "isLocalCast" client/src/lib/utils/applySpellEffects.ts` ≥ 1 (threaded, not elided).
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>Pure resolveTargets + buildAction + applySpellEffects with isLocalCast divergence; tests prove fallback + setFlyHeight divergence; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire useReducer into Lobby.tsx + migrate both cascades + suite/perf guardrail</name>
  <read_first>
    - client/src/components/game/Lobby.tsx (L226–279 the 13 useState; L840–1096 remote cascade; L1332–1620 local cascade; L957–995 remote dispel 7-setters; L1467–1509 local dispel 7-setters; Plan-02 ref sync effects ~L268+)
    - 52-PATTERNS.md L113–176 (useReducer wiring + destructure preserves JSX refs; isLocalCast divergence; DISPEL_ALL collapses the 7-setter race)
    - ANALOG: client/src/lib/hooks/usePhaseInterstitial.ts (closest state-machine hook analog)
  </read_first>
  <action>
    In Lobby.tsx replace the 10 reducer-bound useState declarations (deadPlayers, flyingPlayers, frozenPlayers, petrifiedPlayers, tavernDarkMode, chaosMode, invisiblePlayers, dragonAttack, speedBuffs, sizeBuffs) with `const [buffState, dispatch] = useReducer(buffReducer, initialBuffState);` and `const { deadPlayers, flyingPlayers, frozenPlayers, petrifiedPlayers, tavernDarkMode, chaosMode, invisiblePlayers, dragonAttack, speedBuffs, sizeBuffs } = buffState;` — destructuring preserves ALL existing JSX reads and the Plan-02 ref-sync effects (which reference these names). KEEP `flyHeight`, `invisibleFlicker`, `screenShake` as separate useState (A2/Q2 resolved). Replace the remote if-cascade (L840–1096) with `applySpellEffects(detectedEffects, message, casterPlayerId, currentLobby.players, false, dispatch, setFlyHeight, setMagicEffects, setEmotes)` and the local if-cascade (L1332–1620) with the same call passing `true` for isLocalCast. Map any setter calls that previously fired (setDeadPlayers etc.) to dispatch — the 7-setter dispel blocks (L957–995, L1467–1509) collapse to a single `dispatch({ type: 'DISPEL_ALL' })`. Add a `PHASE_RESET` dispatch wherever `gamePhase !== 'lobby'` currently triggers the multi-setter reset. Verify the auto-unfreeze 5s setTimeout still fires an `UNHOLD` dispatch. Do NOT touch the DEBUNKED dual-path structure beyond routing through applySpellEffects with the isLocalCast flag (RESEARCH: the two paths stay distinct via the flag, not merged into one handler).
  </action>
  <verify>
    <automated>npm test && npm run check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useReducer(buffReducer" client/src/components/game/Lobby.tsx` equals 1; the 10 useState declarations are gone; `flyHeight`, `invisibleFlicker`, `screenShake` useState remain (`grep -c "const \[flyHeight\|const \[invisibleFlicker\|const \[screenShake" Lobby.tsx` equals 3).
    - Both cascades call `applySpellEffects(...)` — remote with `false`, local with `true`; `grep -c "applySpellEffects(" Lobby.tsx` ≥ 2.
    - Dispel routes through `dispatch({ type: 'DISPEL_ALL' })`; the 7-setter blocks no longer exist (`grep -v '^//' Lobby.tsx | grep -c "setDeadPlayers\|setFlyingPlayers\|setFrozenPlayers"` is 0 — all replaced by dispatch).
    - Plan-02 ref-sync effects still compile and reference the destructured names.
    - `npm test` ≥ 963 passing; `npm run check` 0; `npm run lint` 0.
    - SUMMARY records a manual profiler note: a dispel now causes ONE re-render (not 7); movement loop render count unchanged.
  </acceptance_criteria>
  <done>10 slots consolidated into useReducer; both cascades migrated via applySpellEffects with isLocalCast preserved; DISPEL_ALL is one action; 3 special-case states kept separate; suite + tsc + lint green; profiler note recorded.</done>
</task>

</tasks>

<verification>
- `npm test` ≥ 963 passing; `npm run check` 0; `npm run lint` 0
- buffReducer pure (no React import), DISPEL_ALL one action, multi-effect composition correct
- resolveTargets pure + caster fallback; isLocalCast setFlyHeight(0) divergence preserved
- 3 special-case slots (flyHeight, invisibleFlicker, screenShake) remain separate useState
- Plan-02 movement refs still sync correctly after destructure
</verification>

<success_criteria>
MAINT-12 complete: 10 magic-effect slots → one useReducer (pure buffReducer, unit-tested); both cascades → forEach dispatch via pure applySpellEffects with isLocalCast threaded and preserved; DISPEL_ALL collapses the 7-setter race; flyHeight/invisibleFlicker/screenShake stay separate; suite + tsc + lint clean; perf unchanged.
</success_criteria>

<output>
Create `.planning/phases/52-client-god-component-decomposition/52-03-SUMMARY.md` when done. Record the dispel single-re-render and movement-loop profiler confirmations.
</output>
