---
phase: 26-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - shared/schema.ts
  - shared/socket-schemas.ts
autonomous: true

must_haves:
  truths:
    - "Developer runs `npm run check` and sees zero TypeScript errors in shared/schema.ts"
    - "All existing Zod schemas in shared/socket-schemas.ts compile and validate correctly after upgrade"
    - "Full test suite passes after Zod version change"
  artifacts:
    - path: "package.json"
      provides: "Zod 4.x dependency"
      contains: "zod"
    - path: "shared/schema.ts"
      provides: "Drizzle insert schemas with Zod 4 compatibility"
      contains: "createInsertSchema"
    - path: "shared/socket-schemas.ts"
      provides: "Socket event validation schemas"
      contains: "from 'zod'"
  key_links:
    - from: "shared/schema.ts"
      to: "drizzle-zod"
      via: "createInsertSchema generates Zod 4 schemas"
      pattern: "createInsertSchema"
    - from: "shared/socket-schemas.ts"
      to: "zod"
      via: "z.object/z.enum/z.infer all use Zod 4 API"
      pattern: "z\\.(object|enum|infer|string|number|boolean)"
---

<objective>
Upgrade Zod from 3.23.8 to 4.x to resolve TypeScript compilation errors in shared/schema.ts caused by drizzle-zod 0.8.3 requiring Zod 4 peer dependency.

Purpose: DEBT-01 blocks `npm run check` — the project cannot pass TypeScript validation until this version mismatch is resolved. This is the highest-impact tech debt item.
Output: Clean TypeScript compilation, passing test suite, Zod 4.x in package.json.
</objective>

<execution_context>
@C:/Users/Preston/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Preston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-tech-debt-cleanup/26-RESEARCH.md
@shared/schema.ts
@shared/socket-schemas.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Zod to 4.x and fix compatibility</name>
  <files>package.json, shared/schema.ts, shared/socket-schemas.ts</files>
  <action>
1. Run `npm install zod@^4.0.0` to upgrade Zod from 3.23.8 to 4.x.

2. After install, verify drizzle-zod peer dependency is satisfied: `npm list drizzle-zod zod`.

3. Check for Zod 4 breaking changes in `shared/schema.ts`:
   - The `z.infer` usage on lines 157-173 currently fails because drizzle-zod 0.8.3 returns Zod 4 types but the project has Zod 3 installed. After upgrading, these should resolve automatically.
   - Verify `import { z } from "zod"` still works (Zod 4 maintains this import path).
   - The `createInsertSchema` calls and `.pick()` chains should work unchanged.

4. Check for Zod 4 breaking changes in `shared/socket-schemas.ts`:
   - This file uses standard Zod API: `z.object`, `z.enum`, `z.string`, `z.number`, `z.boolean`, `z.union`, `z.literal`, `z.array`, `z.record`, `z.infer`, `z.ZodError`, `z.ZodSchema`.
   - Key Zod 4 breaking change to watch for: `z.ZodError` and `z.ZodSchema` may have been renamed or restructured.
   - If `z.ZodError` is renamed, update the `ValidationResult` type on line 573 and the `parsePayload` catch block on line 618.
   - If `z.ZodSchema` is renamed to `z.ZodType` or similar, update `validatePayload` (line 594) and `parsePayload` (line 624) signatures.

5. Check `zod-validation-error` package in package.json (^3.4.0) — it's listed as a dependency but not imported anywhere. Verify it installs without peer dependency errors. If it conflicts with Zod 4, remove it since it's unused.

6. Run `npm run check` (TypeScript compilation). Target: zero errors in shared/schema.ts and shared/socket-schemas.ts.

7. Run `npm test` to verify the full test suite passes.

8. Run `npm run build` to verify production build succeeds.

IMPORTANT: Do NOT change the Zod import style to `import { z } from "zod/v4"` — use the standard `import { z } from "zod"` which works with Zod 4.x installed as the main package.
  </action>
  <verify>
- `npm run check` exits with zero errors
- `npm test` passes all tests
- `npm run build` succeeds
- `npm list zod` shows 4.x
- `npm list drizzle-zod` shows 0.8.3 with no peer dependency warnings
  </verify>
  <done>
TypeScript compilation succeeds with zero errors in shared/schema.ts and shared/socket-schemas.ts. Test suite passes. Production build succeeds. Zod 4.x is the installed version.
  </done>
</task>

</tasks>

<verification>
```bash
# Verify DEBT-01 is fully resolved
npm run check         # Zero TypeScript errors
npm test              # All tests pass
npm run build         # Production build succeeds
npm list zod          # Shows 4.x
```
</verification>

<success_criteria>
- `npm run check` reports zero errors (specifically zero errors in shared/schema.ts)
- `npm test` passes with no regressions
- `npm run build` produces valid production output
- Zod version in node_modules is 4.x
</success_criteria>

<output>
After completion, create `.planning/phases/26-tech-debt-cleanup/26-01-SUMMARY.md`
</output>
