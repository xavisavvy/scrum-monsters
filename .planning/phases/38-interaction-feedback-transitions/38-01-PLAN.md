---
phase: 38-interaction-feedback-transitions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/ui/GameButton.tsx
  - client/src/components/game/ScoreSubmission.tsx
autonomous: true

must_haves:
  truths:
    - "User sees spring-back press animation on all GameButton/RetroButton clicks"
    - "User sees glow and bounce animation on selected vote card in ScoreSubmission"
    - "Vote card glow resets when a new ticket loads (no stale glow)"
    - "Disabled buttons do not animate on press"
    - "Reduced-motion users see no spring/glow animations"
  artifacts:
    - path: "client/src/components/ui/GameButton.tsx"
      provides: "Spring press animation via framer-motion whileTap"
      contains: "motion.button"
    - path: "client/src/components/game/ScoreSubmission.tsx"
      provides: "Glow/bounce on selected vote card"
      contains: "motion.div"
  key_links:
    - from: "client/src/components/ui/GameButton.tsx"
      to: "framer-motion"
      via: "motion.button with whileTap"
      pattern: "whileTap.*scale"
    - from: "client/src/components/game/ScoreSubmission.tsx"
      to: "framer-motion"
      via: "motion.div animate on selectedScore"
      pattern: "animate.*boxShadow|glow"
---

<objective>
Add spring-back press animations to all game buttons and glow/bounce feedback when selecting a vote card.

Purpose: FEED-01 requires immediate visual feedback on button presses and vote card selection. Currently buttons use CSS-only `:active` transform which has no spring physics. Vote cards have no selection animation at all beyond a color change.

Output: GameButton uses framer-motion `motion.button` with `whileTap` spring. ScoreSubmission vote cards bounce and glow when selected.
</objective>

<execution_context>
@C:/Users/Preston/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Preston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-interaction-feedback-transitions/38-RESEARCH.md
@client/src/components/ui/GameButton.tsx
@client/src/components/ui/retro-button.tsx
@client/src/components/game/ScoreSubmission.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add framer-motion spring press to GameButton</name>
  <files>client/src/components/ui/GameButton.tsx</files>
  <action>
Convert the `<button>` element in GameButton to `motion.button` from framer-motion.

1. Import `{ motion, useReducedMotion }` from 'framer-motion'.
2. Replace `<button>` with `<motion.button>`.
3. Add `whileTap={{ scale: 0.92 }}` and `whileHover={{ scale: 1.03 }}` — but only when NOT disabled. When `props.disabled` is truthy, pass `undefined` for both whileTap and whileHover.
4. Add `transition={{ type: 'spring', stiffness: 400, damping: 17 }}`.
5. Check `useReducedMotion()` — if true, skip whileTap/whileHover (pass undefined).
6. Remove the `transition-all duration-200` from the CVA base classes since framer-motion now handles the transform transition. Keep other Tailwind classes as-is (colors, borders still use CSS transitions).
7. The `ref` forwarding must still work — `motion.button` supports `React.forwardRef` natively.
8. RetroButton delegates to GameButton, so it inherits the spring automatically — no changes needed to retro-button.tsx.

Important: Do NOT change the GameButtonProps interface. The component's external API stays identical.
  </action>
  <verify>Run `npm run check` — no TypeScript errors. Visually: all buttons in the app should spring on click.</verify>
  <done>GameButton renders as motion.button with spring-back whileTap animation. Disabled buttons do not animate. Reduced-motion users see no animation.</done>
</task>

<task type="auto">
  <name>Task 2: Add glow/bounce animation to vote card selection</name>
  <files>client/src/components/game/ScoreSubmission.tsx</files>
  <action>
Wrap each vote card (the fibonacci buttons and the "?" button) in a `motion.div` that animates on selection.

1. Import `{ motion, useReducedMotion }` from 'framer-motion'.
2. Wrap each `<RetroButton>` in the fibonacci grid with a `<motion.div>` that has:
   - `key={option}` (already on RetroButton — move to motion.div wrapper)
   - `animate` that checks `selectedScore === option`:
     - Selected: `{ scale: 1.05, boxShadow: '0 0 20px rgba(234, 179, 8, 0.6)' }` (yellow-500 glow)
     - Unselected: `{ scale: 1, boxShadow: '0 0 0px rgba(0, 0, 0, 0)' }`
   - `transition={{ type: 'spring', stiffness: 300, damping: 20 }}`
3. Apply the same wrapper to the "?" button.
4. Key the entire vote card grid section on `currentTicket?.id` so animation state resets when ticket changes. This prevents stale glow from persisting across tickets.
5. If `useReducedMotion()` returns true, set animate to `{}` (no animation) for both states.
6. Remove the existing `bg-yellow-600` conditional class on the selected button — the glow effect replaces it as the selection indicator. Keep the `variant={selectedScore === option ? 'accent' : 'primary'}` as it changes the button's base color.

Important: Do NOT change the submit button or any other part of ScoreSubmission. Only the vote card grid buttons get the motion wrapper.
  </action>
  <verify>Run `npm run check` — no TypeScript errors. In battle phase, clicking a fibonacci number shows glow + slight scale-up with spring physics.</verify>
  <done>Selected vote card shows yellow glow and bounce animation. Unselected cards return to normal. Glow resets on ticket change. Reduced-motion users see no animation.</done>
</task>

</tasks>

<verification>
- `npm run check` passes with no errors
- `npm run build` succeeds
- All game buttons (lobby, battle, victory, etc.) show spring-back on click
- Vote cards in battle phase bounce and glow on selection
- Disabled buttons (e.g., submit with no selection) do not animate
- Vote card glow clears when ticket changes
</verification>

<success_criteria>
FEED-01 satisfied: Users see press/spring-back animation on game buttons and glow/bounce feedback when selecting a vote card. Reduced-motion respected throughout.
</success_criteria>

<output>
After completion, create `.planning/phases/38-interaction-feedback-transitions/38-01-SUMMARY.md`
</output>
