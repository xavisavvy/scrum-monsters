---
phase: 38-interaction-feedback-transitions
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - client/src/components/ui/sonner.tsx
  - client/src/components/game/ScoreSubmission.tsx
  - client/src/components/game/AbilityButton.tsx
  - client/src/lib/socket/eventHandlers.ts
  - client/src/lib/stores/useAbilities.tsx
autonomous: true

must_haves:
  truths:
    - "User sees JRPG-themed toast when score is submitted"
    - "User sees toast when ability is activated"
    - "User sees toast when settings are saved in lobby"
    - "User sees confirmation flash overlay on ability button when activated"
    - "Toasts render with dark JRPG theme (not light/system theme)"
    - "Toasts appear at top-right to avoid overlap with AbilityBar at bottom-right"
  artifacts:
    - path: "client/src/components/ui/sonner.tsx"
      provides: "JRPG-themed dark Toaster config"
      contains: "theme=\"dark\""
    - path: "client/src/components/game/AbilityButton.tsx"
      provides: "Activation flash overlay"
      contains: "justActivated"
  key_links:
    - from: "client/src/components/game/ScoreSubmission.tsx"
      to: "sonner"
      via: "toast.success call on submit"
      pattern: "toast\\.success"
    - from: "client/src/lib/stores/useAbilities.tsx"
      to: "sonner"
      via: "toast call on ability:used event"
      pattern: "toast\\("
    - from: "client/src/components/game/AbilityButton.tsx"
      to: "framer-motion"
      via: "motion.div flash overlay"
      pattern: "justActivated"
---

<objective>
Add toast notifications for key game events and a confirmation flash when activating combat abilities.

Purpose: FEED-02 requires toast notifications for score submission, reconnection, settings saved, and ability used. FEED-03 requires a brief confirmation flash and cooldown indicator on ability activation. The cooldown indicator already exists (conic-gradient overlay) — this plan adds the missing flash and toast.

Output: sonner.tsx fixed for JRPG dark theme. Toast calls added at key event points. AbilityButton shows flash overlay on activation.
</objective>

<execution_context>
@C:/Users/Preston/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Preston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-interaction-feedback-transitions/38-RESEARCH.md
@client/src/components/ui/sonner.tsx
@client/src/components/game/ScoreSubmission.tsx
@client/src/components/game/AbilityButton.tsx
@client/src/lib/socket/eventHandlers.ts
@client/src/lib/stores/useAbilities.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix sonner.tsx for JRPG dark theme and add toast calls</name>
  <files>
    client/src/components/ui/sonner.tsx
    client/src/components/game/ScoreSubmission.tsx
    client/src/lib/socket/eventHandlers.ts
  </files>
  <action>
**sonner.tsx:**
1. Remove the `"use client"` directive (not a Next.js project).
2. Remove the `import { useTheme } from "next-themes"` and the `useTheme()` call entirely. This import is from the shadcn/ui template and is not needed — the app is always dark.
3. Hardcode `theme="dark"` on the `<Sonner>` component.
4. Add `position="top-right"` to avoid overlap with AbilityBar (fixed bottom-right) and TierUpToast (fixed bottom-right).
5. Replace the generic shadcn classNames with JRPG-themed classes:
   - toast: `"group toast bg-gray-900/95 border-2 border-amber-500/40 text-gray-100 font-jrpg shadow-lg shadow-amber-500/10"`
   - description: `"text-gray-400 font-jrpg text-xs"`
   - actionButton: `"bg-amber-600 text-white font-jrpg"`
   - cancelButton: `"bg-gray-700 text-gray-300 font-jrpg"`

**ScoreSubmission.tsx:**
1. Import `{ toast } from 'sonner'`.
2. In `handleScoreSubmit()`, after `setHasSubmitted(true)`, add:
   ```
   toast.success('Estimate Submitted!', {
     description: `You voted ${selectedScore} story points`,
     duration: 2000,
   });
   ```

**eventHandlers.ts:**
1. Import `{ toast } from 'sonner'`.
2. In the `session:reconnected` or reconnection success handler (wherever the client confirms reconnection to a lobby), add:
   ```
   toast.info('Reconnected!', {
     description: 'You have rejoined the session',
     duration: 2500,
   });
   ```
   Find the reconnection handler by searching for 'reconnect' in the file. If there's already a console.log for reconnection, add the toast next to it.
3. Find where lobby settings are saved/updated (look for `update_settings` or `lobby_settings` event handler). Add:
   ```
   toast.success('Settings Saved', {
     description: 'Lobby settings have been updated',
     duration: 2000,
   });
   ```
   If settings saving is handled purely on the server with a lobby_updated broadcast (no specific client handler), add the toast in the settings form component instead (wherever the emit('update_settings',...) call lives on the client).

Important: Use unique toast IDs for phase-related toasts to prevent stacking during rapid transitions. For example: `toast.success('...', { id: 'score-submitted' })`.
  </action>
  <verify>Run `npm run check` — no TypeScript errors. Submit a score in battle phase and see a JRPG-themed dark toast at top-right.</verify>
  <done>Sonner renders dark JRPG-themed toasts at top-right. Score submission shows "Estimate Submitted!" toast. Reconnection shows info toast. Settings save shows success toast.</done>
</task>

<task type="auto">
  <name>Task 2: Add ability activation flash and toast</name>
  <files>
    client/src/components/game/AbilityButton.tsx
    client/src/lib/stores/useAbilities.tsx
  </files>
  <action>
**AbilityButton.tsx:**
1. Import `{ motion, useReducedMotion }` from 'framer-motion' and `{ useState }` (already imported).
2. Add state: `const [justActivated, setJustActivated] = useState(false)`.
3. In `handleClick()`, after `onActivate(ability.id)`, add:
   ```
   setJustActivated(true);
   setTimeout(() => setJustActivated(false), 300);
   ```
4. Inside the button element, add a flash overlay (as a sibling after the lock overlay):
   ```jsx
   {justActivated && (
     <motion.div
       className="absolute inset-0 rounded-lg bg-white/40 pointer-events-none"
       initial={{ opacity: 1 }}
       animate={{ opacity: 0 }}
       transition={{ duration: 0.3 }}
     />
   )}
   ```
5. If `useReducedMotion()` is true, skip the flash overlay (don't render the motion.div).

**useAbilities.tsx:**
1. Import `{ toast } from 'sonner'`.
2. Find the `ability:used` socket event handler (or wherever ability activation is confirmed by server). Add:
   ```
   toast(`${abilityName} Activated!`, {
     description: ability.description || 'Ability is now in effect',
     duration: 2500,
     id: `ability-${abilityId}`,
   });
   ```
   Use the ability name from the event data. If the event only has an abilityId, look up the name from the ability definitions.

Important: The cooldown progress indicator already exists in AbilityButton (conic-gradient overlay). Do NOT modify or rebuild it. FEED-03 asks for "cooldown progress indicator" which is already implemented. This task only adds the missing "confirmation flash".
  </action>
  <verify>Run `npm run check` — no TypeScript errors. Activate an ability and see white flash on the button plus a toast notification.</verify>
  <done>AbilityButton shows brief white flash overlay on activation. Toast notification appears confirming ability usage. Cooldown indicator continues working as before. Reduced-motion users skip the flash animation.</done>
</task>

</tasks>

<verification>
- `npm run check` passes with no errors
- `npm run build` succeeds
- Toasts appear at top-right with dark JRPG styling (amber border, dark bg, jrpg font)
- Score submission triggers "Estimate Submitted!" toast
- Ability activation triggers flash on button + toast notification
- No `next-themes` import remains in sonner.tsx
- Rapid phase changes don't cause toast spam (unique IDs prevent stacking)
</verification>

<success_criteria>
FEED-02 satisfied: Users receive toast notifications for score submitted, reconnected, settings saved, and ability used. FEED-03 satisfied: Users see confirmation flash on ability activation. Cooldown indicator already existed and continues working.
</success_criteria>

<output>
After completion, create `.planning/phases/38-interaction-feedback-transitions/38-02-SUMMARY.md`
</output>
