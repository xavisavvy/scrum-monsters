---
phase: 42-v5-0-pre-ship-fixes-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/progressionTypes.ts
  - server/domains/ProgressionManager.ts
  - server/domains/ProgressionManager.test.ts
autonomous: true
requirements: [BAL-01]
tags: [balance, progression, xp, tuning]
must_haves:
  truths:
    - "XP_RATES.boss_damage is 1 (was 2)"
    - "DEFAULT_CURVE_CONFIG.exponent is 1.8 (was 1.5)"
    - "A 30-minute average session reaches level 4-5 (was level 6-7)"
    - "Reaching level 10 requires ~13 sessions of average activity (was ~3)"
    - "ProgressionManager XPCurve tests pass with the new exponent expectations"
    - "LevelUpCelebration + TierUpToast still fire correctly (no new spam, no breakage)"
  artifacts:
    - path: shared/progressionTypes.ts
      provides: "XP_RATES with boss_damage=1"
      contains: "boss_damage: 1"
    - path: server/domains/ProgressionManager.ts
      provides: "DEFAULT_CURVE_CONFIG with exponent=1.8 and XP_RATE_VALUES.boss_damage=1 (mirrors shared)"
      contains: "exponent: 1.8"
    - path: server/domains/ProgressionManager.test.ts
      provides: "Updated XPCurve threshold expectations matching exponent=1.8"
  key_links:
    - from: shared/progressionTypes.ts (XP_RATES)
      to: server/domains/ProgressionManager.ts (XP_RATE_VALUES duplicate)
      via: "Both must mirror; researcher noted optional refactor to import from shared"
      pattern: "boss_damage"
---

<objective>
Tune XP pacing per BAL-01: progression currently trivializes (a single 50-damage boss attack = full level 1→2). Apply both knobs from RESEARCH.md:
- Drop `XP_RATES.boss_damage` from 2 → 1 (halves dominant XP source)
- Raise `DEFAULT_CURVE_CONFIG.exponent` from 1.5 → 1.8 (steepens late-level requirements)

Target feel (RESEARCH.md): 30-min session reaches L4-5; reaching L10 requires ~13 sessions; level cap unreachable in a single session.

Purpose: Close BAL-01 with a single small, well-tested commit.
Output: Two number changes (one shared, one server) + duplicate-rate sync + updated curve test expectations + before/after table in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-v5-0-pre-ship-fixes-polish/42-CONTEXT.md
@.planning/phases/42-v5-0-pre-ship-fixes-polish/42-RESEARCH.md
@.planning/phases/42-v5-0-pre-ship-fixes-polish/42-PATTERNS.md

# Pattern source-of-truth (DO NOT modify mechanics — only values)
@shared/progressionTypes.ts
@server/domains/ProgressionManager.ts
@server/domains/ProgressionManager.test.ts

<interfaces>
<!-- Existing values (BEFORE) -->
```typescript
// shared/progressionTypes.ts:16-21
export const XP_RATES = {
  vote: 10,
  boss_damage: 2,    // CHANGE → 1
  consensus: 50,
  revival: 30,
} as const;

// server/domains/ProgressionManager.ts:43-46
const DEFAULT_CURVE_CONFIG: XPCurveConfig = {
  baseXP: 100,
  exponent: 1.5,     // CHANGE → 1.8
};

// server/domains/ProgressionManager.ts:52-57 (DUPLICATE — must stay in sync)
const XP_RATE_VALUES: typeof XP_RATES = {
  vote: 10,
  boss_damage: 2,    // CHANGE → 1
  consensus: 50,
  revival: 30,
};
```

<!-- Curve formula: getLevelThreshold(level) = floor(baseXP * (level-1)^exponent) -->

<!-- Recomputed thresholds at exponent=1.8, baseXP=100: -->
| Level | exponent=1.5 (BEFORE) | exponent=1.8 (AFTER) |
|-------|-----------------------|-----------------------|
| 2     | 100                   | 100                   |
| 3     | 282                   | 348                   |
| 4     | 519                   | 723                   |
| 5     | 800                   | 1223                  |
| 6     | 1118                  | 1843                  |
| 7     | 1469                  | 2580                  |
| 8     | 1851                  | 3434                  |
| 10    | 2700                  | ~5099                 |
(Cumulative-to-level expectations — recompute exact values from `Math.floor(100 * (level-1)**1.8)` when updating tests.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Update curve test expectations to exponent=1.8 (RED before changing values)</name>
  <files>server/domains/ProgressionManager.test.ts</files>
  <read_first>
    - server/domains/ProgressionManager.test.ts (full file — locate XPCurve describe block lines 15-80; locate boss_damage rate test around line 175 per RESEARCH.md)
    - server/domains/ProgressionManager.ts (lines 43-57 — current DEFAULT_CURVE_CONFIG + XP_RATE_VALUES)
    - .planning/phases/42-v5-0-pre-ship-fixes-polish/42-PATTERNS.md (lines 629-660 — exact test pattern)
  </read_first>
  <behavior>
    - Test 1: getLevelThreshold(2) === 100 (unchanged)
    - Test 2: getLevelThreshold(3) === 348 (was 282)
    - Test 3: getLevelThreshold(5) === 1223 (was 800)
    - Test 4: getLevelThreshold(10) computed as `Math.floor(100 * 9**1.8)` (compute exact value when writing test)
    - Test 5: awardXP for boss_damage with damage=10 awards 10 XP (was 20)
    - Test 6: calculateLevel(2000) returns the level matching the new curve
  </behavior>
  <action>
    1. In `server/domains/ProgressionManager.test.ts`, update the XPCurve `beforeEach` constructor to use `exponent: 1.8`:
       ```typescript
       beforeEach(() => {
         curve = new XPCurve({ baseXP: 100, exponent: 1.8 });
       });
       ```

    2. Recompute every `expect(curve.getLevelThreshold(N)).toBe(...)` value using `Math.floor(100 * (N-1)**1.8)`. Add a small helper at the top of the test file or inline literal values:
       - L2: 100
       - L3: `Math.floor(100 * 2**1.8)` = 348
       - L4: `Math.floor(100 * 3**1.8)` = 723
       - L5: `Math.floor(100 * 4**1.8)` = 1223
       - L10: `Math.floor(100 * 9**1.8)` (compute and inline)

    3. Update the boss_damage rate test (~line 175) to expect rate=1:
       ```typescript
       it('awards boss_damage XP at rate 1', () => {
         const result = manager.awardXP(playerId, 'boss_damage', 10);
         expect(result.xpDelta).toBe(10);  // was 20 (damage * 2)
       });
       ```
       (Adjust to match actual existing assertion shape — read the file first.)

    4. Update `calculateLevel` cumulative expectations: a player with totalXP=2000 used to be ~level 5 (curve 1.5); under curve 1.8 cumulative-to-L4 is 100+348+723 = 1171, cumulative-to-L5 is 1171+1223 = 2394 — so totalXP=2000 maps to level 4. Update any existing `calculateLevel` assertion accordingly.

    5. Run vitest — these tests SHOULD FAIL now (RED — values still 2/1.5 in source). This proves the test gate works.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('server/domains/ProgressionManager.test.ts','utf8'); if(!/exponent:\s*1\.8/.test(s)){console.error('test file not updated to exponent=1.8');process.exit(1)}"</automated>
    <automated>node -e "console.log('Expected RED state — tests fail before Task 1 lands values')"</automated>
  </verify>
  <done>
    - Test file expectations updated to exponent=1.8 + boss_damage rate=1
    - Tests fail (RED) when run against unchanged source — proves the test gate
  </done>
  <acceptance_criteria>
    - `grep -c "exponent: 1.8" server/domains/ProgressionManager.test.ts` returns at least 1
    - Test file references the new boss_damage rate (1) in at least one assertion
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Apply value changes (GREEN) — XP_RATES.boss_damage=1, exponent=1.8</name>
  <files>shared/progressionTypes.ts, server/domains/ProgressionManager.ts</files>
  <read_first>
    - shared/progressionTypes.ts (lines 16-21 XP_RATES)
    - server/domains/ProgressionManager.ts (lines 43-57 — DEFAULT_CURVE_CONFIG + duplicate XP_RATE_VALUES)
  </read_first>
  <behavior>
    - All Task 0 tests now pass (GREEN)
    - awardXP applied to boss_damage uses rate=1
    - getLevelThreshold uses exponent=1.8
  </behavior>
  <action>
    1. In `shared/progressionTypes.ts:16-21`, change `boss_damage: 2` to `boss_damage: 1`:
       ```typescript
       export const XP_RATES = {
         vote: 10,
         boss_damage: 1,  // CHANGED from 2 (Phase 42 BAL-01)
         consensus: 50,
         revival: 30,
       } as const;
       ```

    2. In `server/domains/ProgressionManager.ts:43-46`, change `exponent: 1.5` to `exponent: 1.8`:
       ```typescript
       const DEFAULT_CURVE_CONFIG: XPCurveConfig = {
         baseXP: 100,
         exponent: 1.8,  // CHANGED from 1.5 (Phase 42 BAL-01)
       };
       ```

    3. In `server/domains/ProgressionManager.ts:52-57`, mirror the shared change in the duplicate:
       ```typescript
       const XP_RATE_VALUES: typeof XP_RATES = {
         vote: 10,
         boss_damage: 1,  // CHANGED — must mirror shared/progressionTypes.ts
         consensus: 50,
         revival: 30,
       };
       ```
       (Researcher noted this duplicate exists. Do NOT refactor to import from shared in this plan — defer to a separate cleanup; we want a minimal, ship-safe diff.)
  </action>
  <verify>
    <automated>npx vitest run server/domains/ProgressionManager.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>node -e "const s=require('fs').readFileSync('shared/progressionTypes.ts','utf8'); if(!/boss_damage:\s*1\b/.test(s)){process.exit(1)}"</automated>
    <automated>node -e "const s=require('fs').readFileSync('server/domains/ProgressionManager.ts','utf8'); if(!/exponent:\s*1\.8/.test(s)||!/boss_damage:\s*1\b/.test(s)){process.exit(1)}"</automated>
  </verify>
  <done>
    - All ProgressionManager tests pass (GREEN)
    - tsc --noEmit clean
    - Both shared/progressionTypes.ts AND ProgressionManager.ts reflect the new values
  </done>
  <acceptance_criteria>
    - `grep -E "boss_damage:\s*1\b" shared/progressionTypes.ts` returns at least 1 match
    - `grep -E "exponent:\s*1\.8" server/domains/ProgressionManager.ts` returns at least 1 match
    - `grep -E "boss_damage:\s*1\b" server/domains/ProgressionManager.ts` returns at least 1 match (duplicate sync)
    - `npx vitest run server/domains/ProgressionManager.test.ts` passes
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build before/after pacing table for SUMMARY.md (ROADMAP success criterion)</name>
  <files>.planning/phases/42-v5-0-pre-ship-fixes-polish/42-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/42-v5-0-pre-ship-fixes-polish/42-RESEARCH.md (XP Awards Inventory + XP Curve Current Values sections — lines 418-472)
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY template structure)
  </read_first>
  <action>
    Author the 42-03-SUMMARY.md including BOTH required tables (per ROADMAP.md Phase 42 success criterion #4 — "before/after curve documented in the plan SUMMARY").

    **Table 1 — Per-action XP rates (BEFORE/AFTER):**
    | Source     | BEFORE | AFTER | Change |
    |------------|--------|-------|--------|
    | vote       | 10     | 10    | unchanged |
    | boss_damage| 2 × dmg | 1 × dmg | halved |
    | consensus  | 50     | 50    | unchanged |
    | revival    | 30     | 30    | unchanged |

    **Table 2 — Per-level XP thresholds (BEFORE/AFTER):**
    | Level | BEFORE (exp=1.5) | AFTER (exp=1.8) | Cumulative BEFORE | Cumulative AFTER |
    |-------|-------------------|------------------|---------------------|--------------------|
    | 2     | 100               | 100              | 100                 | 100                |
    | 3     | 282               | 348              | 382                 | 448                |
    | 4     | 519               | 723              | 901                 | 1171               |
    | 5     | 800               | 1223             | 1701                | 2394               |
    | 6     | 1118              | 1843             | 2819                | 4237               |
    | 7     | 1469              | 2580             | 4288                | 6817               |
    | 8     | 1851              | 3434             | 6139                | 10251              |
    | 10    | ~2700             | ~5099            | ~11700              | ~24000             |
    (Recompute and replace ~ with exact values in the SUMMARY: `Math.floor(100 * (L-1)**1.8)`.)

    **Table 3 — Projected per-session pacing:**
    | Metric                          | BEFORE              | AFTER          |
    |---------------------------------|---------------------|----------------|
    | Avg XP per 30-min session       | ~3300               | ~1860          |
    | Level reached after 1 session   | 6-7                 | 4-5            |
    | Sessions to reach Level 10      | ~3                  | ~13            |

    **Section "Verification of side effects":**
    - LevelUpCelebration test still passes (frequency reduced, not eliminated; no spam risk)
    - TierUpToast unchanged
    - Existing players' totalXP recalibrates to new curve on next XP award (verified safe per RESEARCH.md Pitfall 4)

    **Section "Files touched":**
    - shared/progressionTypes.ts:18 (XP_RATES.boss_damage 2→1)
    - server/domains/ProgressionManager.ts:45 (DEFAULT_CURVE_CONFIG.exponent 1.5→1.8)
    - server/domains/ProgressionManager.ts:54 (XP_RATE_VALUES.boss_damage 2→1, mirror)
    - server/domains/ProgressionManager.test.ts (curve threshold + boss_damage rate expectations updated)
  </action>
  <verify>
    <automated>node -e "const f='.planning/phases/42-v5-0-pre-ship-fixes-polish/42-03-SUMMARY.md'; const s=require('fs').readFileSync(f,'utf8'); ['BEFORE','AFTER','boss_damage','exponent','Cumulative'].forEach(t=>{if(!s.includes(t)){console.error('SUMMARY missing',t);process.exit(1)}})"</automated>
  </verify>
  <done>
    - 42-03-SUMMARY.md exists with all three tables + side-effect verification + files-touched section
    - Per-level thresholds use exact computed values (no `~` placeholders for L2-L8)
  </done>
  <acceptance_criteria>
    - `grep -c BEFORE .planning/phases/42-v5-0-pre-ship-fixes-polish/42-03-SUMMARY.md` returns at least 3
    - `grep -c boss_damage .planning/phases/42-v5-0-pre-ship-fixes-polish/42-03-SUMMARY.md` returns at least 1
    - `grep -c "exp=1.8" .planning/phases/42-v5-0-pre-ship-fixes-polish/42-03-SUMMARY.md` returns at least 1
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This plan changes constants only — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-10 | Tampering | Client could mirror XP curve and report higher level | accept | Server is authoritative on level computation (ProgressionManager). Client mirrors are advisory only. No new attack surface from this change. |
| T-42-11 | Repudiation | Existing players' levels recalibrate on next XP event | accept | RESEARCH.md Pitfall 4 verified safe — LevelUpCelebration diffs within single awardXP call, not cross-session |
</threat_model>

<verification>
- `npx vitest run server/domains/ProgressionManager.test.ts` passes (curve + rate assertions)
- `npx tsc --noEmit` clean
- Phase 40 + Phase 41 tests pass (no overlap with this plan)
- LevelUpCelebration.test.tsx still passes
- 42-03-SUMMARY.md contains both before/after curve table AND per-action rate table
</verification>

<success_criteria>
1. Two value changes land in source: XP_RATES.boss_damage=1 and DEFAULT_CURVE_CONFIG.exponent=1.8
2. Duplicate XP_RATE_VALUES in ProgressionManager.ts kept in sync
3. ProgressionManager curve tests pass with new exponent=1.8 expectations
4. SUMMARY contains before/after tables (per-action rates + per-level thresholds + per-session pacing)
5. No regressions: full Vitest suite green
</success_criteria>

<output>
The SUMMARY at `.planning/phases/42-v5-0-pre-ship-fixes-polish/42-03-SUMMARY.md` IS the deliverable per ROADMAP success criterion #4. Task 2 produces it directly.
</output>
