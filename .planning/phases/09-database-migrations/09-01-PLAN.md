---
phase: 09-database-migrations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drizzle.config.ts
  - package.json
  - migrations/meta/_journal.json
  - migrations/0000_init_schema.sql
autonomous: true

must_haves:
  truths:
    - "npm run db:migrate:generate creates timestamped SQL migration files"
    - "npm run db:migrate applies pending migrations to database"
    - "migrations/ directory contains snapshot metadata for diffing"
  artifacts:
    - path: "drizzle.config.ts"
      provides: "Migration output directory configuration"
      contains: "out: \"./migrations\""
    - path: "package.json"
      provides: "Migration npm scripts"
      contains: "db:migrate"
    - path: "migrations/meta/_journal.json"
      provides: "Migration journal for drizzle-kit tracking"
    - path: "migrations/0000_init_schema.sql"
      provides: "Initial schema migration"
  key_links:
    - from: "package.json"
      to: "drizzle.config.ts"
      via: "drizzle-kit CLI reads config"
      pattern: "drizzle-kit (generate|migrate)"
    - from: "drizzle.config.ts"
      to: "shared/schema.ts"
      via: "schema source of truth"
      pattern: "schema:.*schema\\.ts"
---

<objective>
Configure Drizzle migration workflow with npm scripts and generate initial migration from current schema.

Purpose: Replace db:push workflow with versioned migrations. Migrations provide an audit trail, enable CI validation, and ensure production deployments are repeatable.

Output: Working local migration workflow with npm scripts (db:migrate:generate, db:migrate) and initial migration file representing current schema state.
</objective>

<execution_context>
@C:\Users\Preston\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Preston\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-database-migrations/09-CONTEXT.md
@.planning/phases/09-database-migrations/09-RESEARCH.md

@drizzle.config.ts
@package.json
@shared/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure migration output and add npm scripts</name>
  <files>drizzle.config.ts, package.json</files>
  <action>
1. Update drizzle.config.ts:
   - Change `out: "./migrations"` (already set, confirm it's correct)
   - This directory will hold SQL files and meta/ snapshot folder

2. Update package.json scripts:
   - Add "db:migrate:generate": "drizzle-kit generate"
   - Add "db:migrate": "drizzle-kit migrate"
   - Keep existing "db:push" script for now (will be used to create initial state)
   - Note: db:push is still valid for development, but migrations are preferred

3. Verify drizzle-kit version supports these commands:
   - drizzle-kit@0.31.4 includes generate and migrate commands (confirmed in research)
  </action>
  <verify>
Run `npm run db:migrate:generate -- --help` and `npm run db:migrate -- --help` to confirm scripts are configured correctly and drizzle-kit CLI responds.
  </verify>
  <done>
package.json contains db:migrate:generate and db:migrate scripts. drizzle.config.ts outputs to ./migrations directory.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate initial migration from current schema</name>
  <files>migrations/meta/_journal.json, migrations/0000_init_schema.sql</files>
  <action>
1. Ensure DATABASE_URL is available (use local PostgreSQL or skip database connection for generate):
   - Note: drizzle-kit generate compares schema.ts to snapshots, doesn't need live DB
   - First run creates baseline snapshot in migrations/meta/

2. Run initial migration generation:
   - Run: npm run db:migrate:generate
   - This creates migrations/ directory with:
     - 0000_*.sql (initial schema SQL)
     - meta/_journal.json (migration tracking)
     - meta/0000_snapshot.json (schema state)

3. Review generated SQL:
   - Verify it matches shared/schema.ts tables: users, oauth_accounts, user_profiles, user_stats, estimation_history, sessions
   - All CREATE TABLE statements should be present

4. Ensure migrations/meta/ is not gitignored:
   - Check .gitignore doesn't exclude migrations/ or *.json in migrations/
   - These files MUST be committed for team consistency

5. Note for existing databases:
   - If production already has tables via db:push, the __drizzle_migrations table won't exist
   - First deployment will need manual baseline: mark initial migration as applied
   - Document this in SUMMARY.md under "Production Baseline Note"
  </action>
  <verify>
- `ls migrations/` shows at least one .sql file
- `ls migrations/meta/` shows _journal.json and at least one snapshot.json
- `cat migrations/meta/_journal.json` shows entries array with migration info
- Generated SQL contains CREATE TABLE statements for all tables in schema.ts
  </verify>
  <done>
migrations/ directory contains initial migration SQL file, meta/_journal.json for tracking, and snapshot.json for diffing. All files ready to commit.
  </done>
</task>

</tasks>

<verification>
1. npm run db:migrate:generate responds with "No schema changes detected" (since we just generated)
2. migrations/ directory structure:
   - At least one .sql file with CREATE TABLE statements
   - meta/_journal.json with version and entries
   - meta/*.snapshot.json with schema state
3. package.json has both db:migrate:generate and db:migrate scripts
</verification>

<success_criteria>
- db:migrate:generate script exists and creates migration files from schema changes
- db:migrate script exists for applying pending migrations
- Initial migration captures all 6 tables from shared/schema.ts
- migrations/meta/ contains journal and snapshot for drizzle-kit diffing
</success_criteria>

<output>
After completion, create `.planning/phases/09-database-migrations/09-01-SUMMARY.md`

Include in SUMMARY:
- Files created/modified
- Migration naming convention used
- Production Baseline Note (for existing databases without __drizzle_migrations table)
</output>
