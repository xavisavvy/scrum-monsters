---
phase: 09-database-migrations
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - .github/workflows/ci.yml
autonomous: true

must_haves:
  truths:
    - "CI fails if schema.ts changes without corresponding migration file"
    - "CI applies all migrations to fresh PostgreSQL and catches SQL errors"
    - "PRs cannot merge if migration validation fails"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "validate-migrations job with PostgreSQL service container"
      contains: "validate-migrations"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "npm run db:migrate"
      pattern: "npm run db:migrate"
    - from: ".github/workflows/ci.yml"
      to: "ci-success job"
      via: "needs array includes validate-migrations"
      pattern: "needs:.*validate-migrations"
---

<objective>
Add CI validation job that detects missing migrations and tests migration SQL against fresh PostgreSQL.

Purpose: Prevent schema drift between schema.ts and migration files. Catch SQL syntax errors before deployment. Enforce migration discipline by blocking PRs that modify schema without generating migrations.

Output: New validate-migrations job in ci.yml with PostgreSQL service container, drift detection, and integration with ci-success gate.
</objective>

<execution_context>
@C:\Users\Preston\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Preston\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-database-migrations/09-CONTEXT.md
@.planning/phases/09-database-migrations/09-RESEARCH.md
@.planning/phases/09-database-migrations/09-01-SUMMARY.md

@.github/workflows/ci.yml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add validate-migrations job with PostgreSQL service</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Add new job `validate-migrations` after the `license-check` job:

```yaml
validate-migrations:
  name: Validate Migrations
  runs-on: ubuntu-latest
  timeout-minutes: 15

  services:
    postgres:
      image: postgres:16-alpine
      env:
        POSTGRES_PASSWORD: postgres
        POSTGRES_DB: test
      ports:
        - 5432:5432
      options: >-
        --health-cmd pg_isready
        --health-interval 10s
        --health-timeout 5s
        --health-retries 5

  steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: "npm"

    - name: Install dependencies
      run: npm ci

    - name: Apply migrations to fresh database
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test
      run: npm run db:migrate

    - name: Check for schema drift
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test
      run: |
        # Generate would create files if schema changed without migration
        npx drizzle-kit generate

        # Check if any new files were generated
        if [ -n "$(git status --porcelain migrations/)" ]; then
          echo "::error::Schema changes detected without corresponding migration"
          echo "Run: npm run db:migrate:generate"
          echo ""
          echo "Uncommitted changes in migrations/:"
          git status migrations/
          echo ""
          echo "Diff:"
          git diff migrations/
          exit 1
        fi

        echo "Schema and migrations are in sync"
```

Key design decisions:
1. postgres:16-alpine - matches production, Alpine for smaller image
2. Health checks ensure PostgreSQL ready before steps run
3. Apply migrations first - catches SQL syntax errors
4. Then check for drift - detects missing migration files
5. git status --porcelain gives machine-readable output
6. ::error:: prefix shows in GitHub Actions annotations
  </action>
  <verify>
Read .github/workflows/ci.yml and confirm:
- validate-migrations job exists
- postgres service is configured with health checks
- Both db:migrate and drift check steps are present
  </verify>
  <done>
validate-migrations job exists with PostgreSQL service container, migration application step, and drift detection step.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add validate-migrations to ci-success gate</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Update the ci-success job to include validate-migrations:

1. Add to needs array:
   ```yaml
   needs: [lint-and-typecheck, test, build, security-audit, license-check, validate-migrations]
   ```

2. Add to the result check in the run step:
   ```bash
   [[ "${{ needs.validate-migrations.result }}" != "success" ]] || \
   ```

This ensures:
- PRs cannot merge if migration validation fails
- Branch protection (via ci-success status check) gates on migration health
- Consistent with how other jobs (security-audit, license-check) are gated
  </action>
  <verify>
Read .github/workflows/ci.yml and confirm:
- ci-success job needs array includes validate-migrations
- Result check includes validate-migrations.result
  </verify>
  <done>
ci-success job gates on validate-migrations success. PRs with schema drift cannot merge.
  </done>
</task>

</tasks>

<verification>
1. YAML syntax validation: Run `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` or similar
2. Job structure: validate-migrations appears in jobs section
3. Service container: postgres service with health checks configured
4. Drift detection: Step checks git status of migrations/ directory
5. Gate integration: ci-success needs array includes validate-migrations
</verification>

<success_criteria>
- validate-migrations job runs PostgreSQL service container
- Migrations are applied to fresh database (catches SQL errors)
- Schema drift is detected via drizzle-kit generate + git status
- ci-success gates on validate-migrations (PRs blocked if validation fails)
</success_criteria>

<output>
After completion, create `.planning/phases/09-database-migrations/09-02-SUMMARY.md`

Include in SUMMARY:
- Job name and purpose
- PostgreSQL service container configuration
- How drift detection works
- How gating is enforced
</output>
