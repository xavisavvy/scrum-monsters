---
phase: 34-cicd-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy-lightsail.yml
  - .github/workflows/deploy.yml
  - playwright.config.ts
  - e2e/smoke.spec.ts
autonomous: true

must_haves:
  truths:
    - "A deploy-lightsail.yml workflow file exists with staging (workflow_run after Docker) and production (workflow_dispatch) jobs"
    - "The staging job only runs when docker.yml completes successfully on main — never on push directly"
    - "The production job only runs on workflow_dispatch — never on push to any branch"
    - "The SSH deploy step runs pull, migrate (drizzle-kit push --force), up --no-deps, health check — in that exact order"
    - "Playwright config reads BASE_URL from environment and skips webServer when BASE_URL is set"
    - "A smoke.spec.ts file exists with @smoke-tagged tests for health, home page, and ws-health"
    - "The old K8s/ArgoCD deploy.yml is disabled"
  artifacts:
    - path: ".github/workflows/deploy-lightsail.yml"
      provides: "Complete CI/CD deploy workflow with staging + prod + smoke test jobs"
      min_lines: 100
    - path: "e2e/smoke.spec.ts"
      provides: "Lightweight smoke tests tagged @smoke for post-deploy verification"
      min_lines: 15
    - path: "playwright.config.ts"
      provides: "Updated config supporting live URL via BASE_URL env var"
      contains: "process.env.BASE_URL"
  key_links:
    - from: ".github/workflows/deploy-lightsail.yml"
      to: ".github/workflows/docker.yml"
      via: "workflow_run trigger"
      pattern: "workflow_run.*Docker.*completed"
    - from: ".github/workflows/deploy-lightsail.yml"
      to: "e2e/smoke.spec.ts"
      via: "npx playwright test --grep @smoke"
      pattern: "playwright test.*@smoke"
    - from: "e2e/smoke.spec.ts"
      to: "playwright.config.ts"
      via: "Playwright uses baseURL from config"
      pattern: "process\\.env\\.BASE_URL"
---

<objective>
Create the deploy-lightsail.yml GitHub Actions workflow, Playwright smoke tests, and supporting config changes.

Purpose: This is the code foundation for the entire CI/CD pipeline. It creates the workflow file that orchestrates staging auto-deploy (via workflow_run after docker.yml) and production manual deploy (via workflow_dispatch), wires Drizzle migrations into the SSH deploy sequence, and adds post-deploy Playwright smoke tests against the live URL. It also disables the dead K8s/ArgoCD deploy.yml.

Output: 4 files created/modified — the workflow, smoke tests, updated Playwright config, and disabled old deploy.yml.
</objective>

<execution_context>
@C:/Users/Preston/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Preston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-cicd-pipeline/34-RESEARCH.md
@.github/workflows/docker.yml
@.github/workflows/deploy.yml
@playwright.config.ts
@docker-compose.prod.yml
@deploy.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deploy-lightsail.yml workflow and disable old deploy.yml</name>
  <files>.github/workflows/deploy-lightsail.yml, .github/workflows/deploy.yml</files>
  <action>
Create `.github/workflows/deploy-lightsail.yml` with the following structure:

**Triggers:**
- `workflow_run`: fires after the "Docker" workflow completes on main branch
- `workflow_dispatch`: manual trigger with `environment` input (choice: "production" only)

**Concurrency:** `deploy-${{ github.event.workflow_run.head_branch || github.ref }}`, `cancel-in-progress: false` (never cancel in-progress deploys)

**Permissions:** `id-token: write` (for OIDC), `contents: read`

**Environment variables (workflow-level):**
- `REMOTE_HOST: "34.199.135.244"`
- `REMOTE_USER: "ubuntu"`
- `REMOTE_DIR: "/opt/scrummonsters"`

**Job 1: deploy-staging**
- `runs-on: ubuntu-latest`, `timeout-minutes: 15`
- `if: github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success'`
- `environment: { name: staging, url: https://scrummonsters.com }`
- Steps:
  1. Configure AWS credentials (OIDC) using `aws-actions/configure-aws-credentials@v4` with `role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}`, `aws-region: us-east-1`, `role-session-name: GitHubActions-Staging-Deploy`
  2. Deploy via SSH using `appleboy/ssh-action@v1` with `host: ${{ env.REMOTE_HOST }}`, `username: ${{ env.REMOTE_USER }}`, `key: ${{ secrets.SSH_PRIVATE_KEY }}`, script:
     ```
     set -e
     cd /opt/scrummonsters
     echo "[1/4] Pulling latest Docker image from GHCR..."
     docker compose -f docker-compose.prod.yml pull app
     echo "[2/4] Running Drizzle migrations..."
     docker compose -f docker-compose.prod.yml run --rm app npx drizzle-kit push --force
     echo "[3/4] Restarting app container (no-deps preserves postgres + NPM)..."
     docker compose -f docker-compose.prod.yml up -d --no-deps app
     echo "[4/4] Health check..."
     sleep 15
     curl --fail --silent --show-error https://scrummonsters.com/api/health
     echo ""
     echo "Staging deploy complete."
     ```

**Job 2: deploy-prod**
- `runs-on: ubuntu-latest`, `timeout-minutes: 15`
- `if: github.event_name == 'workflow_dispatch'`
- `environment: { name: production, url: https://scrummonsters.com }`
- Same two steps as deploy-staging (OIDC + SSH deploy), with `role-session-name: GitHubActions-Prod-Deploy` and final echo "Production deploy complete."
- **Migration step also uses `npx drizzle-kit push --force`** (same as staging — prevents interactive prompts hanging in CI)

**Job 3: smoke-test**
- `runs-on: ubuntu-latest`, `timeout-minutes: 10`
- `needs: [deploy-staging, deploy-prod]`
- `if: always() && (needs.deploy-staging.result == 'success' || needs.deploy-prod.result == 'success')`
- Steps:
  1. `actions/checkout@v4`
  2. `actions/setup-node@v4` with `node-version: "20"`, `cache: "npm"`
  3. `npm ci`
  4. `npx playwright install --with-deps chromium`
  5. Run smoke tests: `env: { BASE_URL: https://scrummonsters.com }`, command: `npx playwright test --grep @smoke --project=chromium`
  6. Upload test results: `actions/upload-artifact@v4`, `if: always()`, path `playwright-report/`, retention 7 days, name `smoke-test-report`

**IMPORTANT version notes:**
- Use `aws-actions/configure-aws-credentials@v4` (not v6.0.0 — the research may have an incorrect version; v4 is the current stable major that supports OIDC)
- Use `actions/setup-node@v4` (not v6 — v4 is the current stable)
- Use `actions/upload-artifact@v4` (not v6 — v4 is the current stable)
- Verify these are correct by checking what other workflows in the repo use (docker.yml uses `actions/checkout@v4`, `actions/upload-artifact@v6`)
- Match whatever version patterns are already used in the repo's other workflows for consistency

**Disable old deploy.yml:**
Rename `.github/workflows/deploy.yml` to `.github/workflows/deploy.yml.disabled` OR add a comment at the top and change the `on:` trigger to only `workflow_dispatch` with a note that this is superseded by deploy-lightsail.yml. The simpler approach: just rename the file so GitHub Actions ignores it. Use `git mv .github/workflows/deploy.yml .github/workflows/deploy.yml.disabled`.
  </action>
  <verify>
Validate YAML syntax: `python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy-lightsail.yml'))"` or `npx yaml-lint .github/workflows/deploy-lightsail.yml` — should parse without errors.

Verify the workflow_run trigger references the correct workflow name: `grep -q 'workflows:.*Docker' .github/workflows/deploy-lightsail.yml && echo "OK"`.

Verify deploy-prod only triggers on workflow_dispatch: `grep -A1 'deploy-prod' .github/workflows/deploy-lightsail.yml | grep -q 'workflow_dispatch' && echo "OK"`.

Verify old deploy.yml is disabled: `ls .github/workflows/deploy.yml.disabled` should exist and `.github/workflows/deploy.yml` should not.

Verify the SSH script contains the 4-step deploy sequence in correct order (pull, migrate, up --no-deps, health check): `grep -c 'pull app\|drizzle-kit push --force\|--no-deps app\|/api/health' .github/workflows/deploy-lightsail.yml` should be 8 (4 per job x 2 jobs).
  </verify>
  <done>deploy-lightsail.yml exists with three jobs (deploy-staging, deploy-prod, smoke-test), the staging job triggers only after Docker workflow succeeds on main, the prod job triggers only on workflow_dispatch, both jobs SSH to 34.199.135.244 with the 4-step deploy sequence (pull, migrate with --force, up --no-deps, health check), and the old K8s deploy.yml is disabled. IMPORTANT: Do not push these changes to the remote main branch until Plan 02 infrastructure provisioning is complete — pushing triggers the deploy pipeline which requires secrets that don't exist yet.</done>
</task>

<task type="auto">
  <name>Task 2: Update Playwright config for live URL support and create smoke tests</name>
  <files>playwright.config.ts, e2e/smoke.spec.ts</files>
  <action>
**Modify `playwright.config.ts`:**

1. Change `baseURL` to read from environment variable:
   ```typescript
   baseURL: process.env.BASE_URL || "http://localhost:5000",
   ```

2. Make `webServer` conditional — skip local server startup when targeting a live URL:
   ```typescript
   webServer: process.env.BASE_URL
     ? undefined
     : {
         command: "npm run dev",
         url: "http://localhost:5000",
         reuseExistingServer: !process.env.CI,
         timeout: 120 * 1000,
         stdout: "pipe",
         stderr: "pipe",
       },
   ```

All other config (projects, reporter, timeouts, snapshotPathTemplate, etc.) remains unchanged.

**Create `e2e/smoke.spec.ts`:**

```typescript
import { test, expect } from "@playwright/test";

test.describe("Smoke Tests", () => {
  test("health endpoint returns 200 @smoke", { tag: "@smoke" }, async ({ request }) => {
    const response = await request.get("/api/health");
    expect(response.status()).toBe(200);
  });

  test("home page loads without error @smoke", { tag: "@smoke" }, async ({ page }) => {
    await page.goto("/");
    await expect(page.getByRole("heading", { level: 1 })).toBeVisible({ timeout: 15000 });
  });

  test("WebSocket health endpoint responds @smoke", { tag: "@smoke" }, async ({ request }) => {
    const response = await request.get("/api/ws-health");
    expect(response.status()).toBe(200);
  });
});
```

These are lightweight, read-only checks safe to run against production. They verify:
- The app is responding to HTTP requests (/api/health)
- The frontend loads and renders its main heading
- The WebSocket subsystem is healthy (/api/ws-health)
  </action>
  <verify>
Verify playwright.config.ts reads BASE_URL from env: `grep -q 'process.env.BASE_URL' playwright.config.ts && echo "OK"`.

Verify webServer is conditional: `grep -q 'process.env.BASE_URL.*undefined' playwright.config.ts && echo "OK"` (or check the ternary pattern).

Verify smoke.spec.ts exists and has @smoke tags: `grep -c '@smoke' e2e/smoke.spec.ts` should return 3 (one per test).

Verify existing E2E tests still work locally (no regression from config change): `npx playwright test --grep @smoke --project=chromium` should pass against local dev server (when BASE_URL is not set, the webServer block spins up the dev server).

Run TypeScript type check: `npm run check` passes.
  </verify>
  <done>playwright.config.ts reads baseURL from `process.env.BASE_URL` with localhost fallback, webServer is skipped when BASE_URL is set, e2e/smoke.spec.ts exists with 3 @smoke-tagged tests (health, home page, ws-health), and existing E2E tests are not broken by the config change.</done>
</task>

</tasks>

<verification>
1. `.github/workflows/deploy-lightsail.yml` has valid YAML syntax
2. Workflow has exactly 3 jobs: deploy-staging, deploy-prod, smoke-test
3. deploy-staging triggers only on workflow_run (Docker success on main)
4. deploy-prod triggers only on workflow_dispatch
5. Both deploy jobs SSH to 34.199.135.244 with 4-step sequence: pull, migrate, up --no-deps, health check
6. smoke-test job needs both deploy jobs, runs after either succeeds
7. playwright.config.ts has `process.env.BASE_URL` in baseURL and conditional webServer
8. e2e/smoke.spec.ts has 3 @smoke-tagged tests
9. Old deploy.yml is disabled (renamed)
10. `npm run check` passes (no TypeScript regressions)
</verification>

<success_criteria>
All workflow files, Playwright config changes, and smoke tests are committed to the repo. The deploy-lightsail.yml workflow file is syntactically valid and structurally correct for the three-job pattern. The old K8s deploy.yml is disabled. Playwright smoke tests can run against a live URL when BASE_URL is set.
</success_criteria>

<output>
After completion, create `.planning/phases/34-cicd-pipeline/34-01-SUMMARY.md`
</output>
