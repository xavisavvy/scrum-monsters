apiVersion: v1
kind: ConfigMap
metadata:
  name: scrumquest-config
  namespace: scrumquest
  labels:
    app.kubernetes.io/name: scrumquest
    app.kubernetes.io/component: config
data:
  NODE_ENV: "production"
  PORT: "5000"
  # CORS - adjust for your domain
  ALLOWED_ORIGINS: "https://scrumquest.local,https://scrummonsters.com"
