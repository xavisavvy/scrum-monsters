apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Prometheus deployment for metrics collection
# For production, consider using kube-prometheus-stack Helm chart

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
