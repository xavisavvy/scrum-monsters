apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Loki for log aggregation
# For production, consider using the Loki Helm chart

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
