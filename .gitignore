# Dependencies
node_modules

# Build outputs
dist
server/public

# Environment variables
.env
.env.local
.env.*.local

# Grafana Cloud remote_write token (mounted into the prometheus container
# as password_file; populated on the VPS from .env, never committed)
docker/prometheus/grafana_remote_write_token

# OS files
.DS_Store
Thumbs.db

# IDE files
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea
*.swp
*.swo
*~

# Temporary files
*.tar.gz
vite.config.ts.*
*.log
*.tmp

# Claude Code files (conversation history and sessions)
.claude/*
# Project-shared skills are checked in so they ride along with the repo
!.claude/skills/

# Local-only AI/agent state directories — never commit
.hivemind/
.beads/

# Test coverage
coverage
*.lcov

# Playwright
playwright-report/
test-results/
.playwright-mcp/

# Local debug/verification artifacts (ad-hoc screenshots, crash dumps)
*.stackdump
verify-*.png
repro-*.jpeg
repro-*.png
avatar-test-*.png

# Database
*.sqlite
*.db

# Versioning (CHANGELOG is auto-generated, don't manually edit)
# CHANGELOG.md is tracked but should only be modified by standard-version
bd.exe

# Load test results
tests/load/results/
results-*.json
